# Known-Good Baselines

## Purpose

Record the latest verified project state so future AI sessions can tell whether a failure is new or pre-existing.

This file is lightweight. Record only meaningful baselines, not every local command run.

## Baselines

| Date | Source | Git State | Scope | Commands Passed | Known Failures | Evidence | Notes |
| ---- | ------ | --------- | ----- | --------------- | -------------- | -------- | ----- |
| 2026-06-28 | enhanced-roadmap Phase 32 优惠券体系增强（计划 `2026-06-27-2321-3`）全量交付 | working tree（uncommitted；含本计划 + Phase 15/16/23/26/27/28/33/34 已提交基线） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS；`./mvnw test` **35.742s SUCCESS**（含 `TestLitemallCouponBizModel` 扩展至 7 例：listCouponsForGoods ALL/GOODS/CATEGORY 三范围命中 / 不匹配排除 / 已领 claimedByMe + claimable=false / 匿名 claimedByMe=false；`TestLitemallCouponUserBizModel` 扩展至 11 例：CATEGORY 券命中结算 / CATEGORY 券不匹配拒）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-2321-3` Phase 5 | Phase 32 全 5 Phase completed。**无模型改动**（Coupon 字段 + 字典均已预置）。`listCouponsForGoods`（`@BizQuery @Auth(publicAccess=true)`，复用 listAvailableCoupons 过滤 + 按 goodsType 范围匹配 + 附 claimedByMe 匿名恒 false / claimable 按 limit 推算）；**Fix（rule #13 非降级）**：`selectCouponForOrder` 增加 CATEGORY 分支（经 ILitemallGoodsBiz 取 categoryId 集合与 goodsValue 分类列表取交集，修复前把分类 ID 当商品 ID 比较永不命中 ERR_COUPON_GOODS_NOT_MATCH），GOODS/ALL 既有不变，展示侧与兑换侧自洽。前端 goods-detail.page.yaml 加领券入口 service + each 卡片 + 已领置灰/可领按钮；LitemallCoupon.view.xml grid 补 goodsType/goodsValue 列 + edit/add/view form（原空骨架）充分暴露 DIY 配置字段 + add x:prototype=edit。 |
| 2026-06-28 | enhanced-roadmap Phase 33 商品评价结构化（计划 `2026-06-27-2321-2`）全量交付 | working tree（uncommitted；含本计划 + Phase 15/16/23/26/27/28/34 已提交基线） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS；`./mvnw test -pl app-mall-service` **194 测试全绿**（含 `TestLitemallCommentBizModel` 扩展至 14 例：原 6 + 新增 8 例：结构化字段写入/语义评级越界拒绝/showType 筛选 all+hasPicture+good+bad/标签云聚合/空聚合/积分联动（配置 10 发）/积分联动（默认 0 关）/向后兼容）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-2321-2` Phase 5 | Phase 33 全 5 Phase completed。**无模型改动**（pros/cons/semanticRating propId 13/14/15 已预置）。`submitComment` 加 3 个 @Optional 结构化参数 + semanticRating 1-5 越界校验（ERR_COMMENT_SEMANTIC_RATING_INVALID）；`commentList` 加 @Optional showType 互斥枚举（all/hasPicture/good/bad，star xmeta 仅 eq/in 故 good/bad 用 `in([4,5])/in([1,2])`）；`getCommentSummary` @BizQuery 返回结构化 Map（totalCount/goodRate=round(good×100/total)/starDistribution 1-5/prosTags/consTags Top10 应用层聚合，total=0 goodRate=0）；评价得积分联动复用 P27 earnPoints（sourceType=comment-reward，sourceId=comment.id，幂等继承 (sourceType,sourceId)），全局配置 `mall_points_comment_reward` 默认 0 关闭。comment ErrorCode 新增 2 项。前端 goods-detail.page.yaml 商品评论 tab：我要评价 dialog form（星级+语义 5 级文案+优缺 input-tag+图片+文本）+ getCommentSummary 头部（好评率大数字 + 总评数 + pros/cons 色块标签云 each）+ button-group 4 筛选 tab + 每条评价星级+语义文案+优缺色块+文本。闭合 P27 复合残留「签到/评价/分享得积分触发」中的**评价子项**（分享不在基线，复合残留待分享 disposition 后方完全闭合）。 |
| 2026-06-27 | enhanced-roadmap Phase 28 签到（计划 `2026-06-27-2321-1`）全量交付 | working tree（uncommitted；含本计划） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS；`./mvnw test -pl app-mall-service` **186 测试全绿**（含新增 `TestLitemallCheckInRecordBizModel` 10 例：首签/连签+1/断签归 1/周期重置归 1/无循环保持顶档/防同日重签拒绝/无规则拒绝/零档位兜底/积分流水写入 sourceType=check-in/状态查询连续天数延续）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-2321-1` Phase 5 | Phase 28 全 5 Phase completed。签到即时计算（Decision A，查昨日记录 +1 断签归 1，无需 nop-job 定时扫描，推翻 roadmap 陈述）；resetCycle 循环发奖（max 取规则表 resetCycle，达此天数归 1）；阶梯累进档位匹配（≤consecutiveDays 最大 daySeq）；防同日重签应用层查（DB 唯一键 deferred）；积分联动复用 P27 earnPoints（sourceType=check-in，sourceId=record.id，幂等继承）；check-in ErrorCode 2 项 + DTO 3 个。**无模型改动**（CheckInRule/CheckInRecord 已预置，sourceType 复用 P27 常量）。个人中心签到 card + feedback 弹窗 + 后台规则管理页。闭合 P27 deferred「签到得积分触发残留」。 |
| 2026-06-27 | enhanced-roadmap Phase 34 首页运营打标（计划 `2026-06-27-2029-3`）全量交付 | working tree（uncommitted；含本计划） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **176 测试全绿**（含 `TestLitemallGoodsExtendedBizModel` 新增 `testFrontListByFlagsRecommend` 1 例：isRecommend 命中/向后兼容/×isHot 组合）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-2029-3` Phase 3 | Phase 34 全 3 Phase completed。isRecommend（propId=22，dormant）接线启用：frontListByFlags 加 @Optional isRecommend 过滤（向后兼容，frontList 包装器 positional 调用同步）；首页新增「为你推荐」第三楼层；后台 LitemallGoods.view.xml grid bounded-merge 加 isRecommend col + edit 开关 + query 三标记过滤。**无模型改动**（三标记位 isNew/isHot/isRecommend 均已建模，仅 Java 接口+impl + AMIS delta + docs）。命名漂移订正：product-catalog.md 字段映射表 + roadmap :435/:584 stale 两处。 |
| 2026-06-27 | enhanced-roadmap Phase 23 限时折扣（计划 `2026-06-27-2029-2`）全量交付 | working tree（uncommitted；含本计划 + Phase 15/16/26/27 已提交基线） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **175 测试全绿**（含新增 `TestLitemallTimeDiscountBizModel` 11 例 + `TestLitemallOrderBizModel` 扩展至 22 例：限时折扣单价/直降/×满减共存/stockLimit 售罄与扣减 6 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-2029-2` Phase 4 | Phase 23 全 4 Phase completed。限时折扣裁决为商品单价层（goodsPrice 汇总，不复用 promotionPrice，消解 P15 遗留共存点）；`selectTimeDiscountForProduct` @BizQuery 返回结构化 Map（促销价/直降/倒计时/剩余库存）；submit 接线 min(retail,vip,timeDiscountPrice)；新增 `reduceTimeDiscountStock` 原子扣减 mapper；time-discount ErrorCode 4 项；详情页促销横幅 + 列表促销价 + 后台管理页。 |
| 2026-06-27 | enhanced-roadmap Phase 27 积分体系（计划 `2026-06-27-2029-1`）全量交付 | working tree（uncommitted；含本计划 + Phase 15/16/26 已提交基线） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **159 测试全绿**（含新增 `TestLitemallPointsAccountBizModel` 7 例 + `TestLitemallOrderBizModel` 扩展至 17 例：积分抵扣/超上限拒绝/确认收货赠送/取消返还 4 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-2029-1` Phase 4 | Phase 27 全 4 Phase completed。积分账户 earn/spend 原子（乐观锁 mapper + 流水 balanceAfter 快照）；submit usePoints 抵扣接线（actualPrice 减项层 + 上限）；confirm/cancel/refund/groupon 赠送/返还；changeType×sourceType 分类法（不改模型）；points ErrorCode 5 项；我的积分页 + 结算抵扣勾选 + 后台调账/流水管理。 |
| 2026-06-27 | enhanced-roadmap Phase 16 订单项级售后增强（计划 `2026-06-27-1742-3`）全量交付 | working tree（uncommitted；含本计划 + Phase 15/26 已提交基线 36ce3f5） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **148 测试全绿**（含 `TestLitemallAftersaleBizModel` 扩展至 12 例：item 级申请/退款/互斥/类型映射/字典校验/部分退款副作用/全部退款转 203 + 整单兼容）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-1742-3` Phase 5 | Phase 16 全 5 Phase completed。售后升级为 OrderGoods 项级（一订单多项独立售后）；新增 `mall/aftersale-reason` 字典 + reason 列 ext:dict；item 退款额上限=number×price；部分退款副作用（状态/还库/券）按 Decision；order.aftersaleStatus 转 aggregate 视图；前台按项申请售后入口 + 后台 item 审核 + 时间线。 |
| 2026-06-27 | enhanced-roadmap Phase 26 会员等级体系（计划 `2026-06-27-1742-2`）全量交付 | working tree（uncommitted；含本计划 + Phase 15 已提交基线） | app-mall-service 全量 + app-mall-web 编译 + 全模块 uber-jar | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **141 测试全绿**（含新增 `TestLitemallMemberLevelBizModel` 6 例 + `TestLitemallOrderBizModel` vipPrice 2 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-1742-2` Phase 5 | Phase 26 全 5 Phase completed。MemberLevel 实体 + vipPrice（GoodsProduct/OrderGoods 快照）模型；等级评估（累计消费）+ 升降级 + 个人中心进度 `@BizQuery`；submit vipPrice 接线（goodsPrice 汇总层）；后台等级管理页 + 个人中心展示 + SKU vipPrice 录入。 |
| 2026-06-27 | enhanced-roadmap Phase 15 满减送（计划 `2026-06-27-1742-1`）全量交付 | working tree（uncommitted；含本计划 + 既有 codegen 基线） | app-mall-service 全量 + app-mall-web 编译 | `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **133 测试全绿**（含新增 `TestLitemallPromotionActivityBizModel` 7 例 + `TestLitemallOrderBizModel` 满减/叠加 3 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 无 | logs/2026/06-27.md；计划 `2026-06-27-1742-1` Phase 4 | Phase 15 全 4 Phase completed。满减价格接线、叠加配置（`mall_promotion_coupon_stacking` 默认允许）、前端动态预览、后台 tiers 子表均已落地。 |
| 2026-06-19 | 测试加固计划 `2026-06-19-2100` Phase 1 执行 + clean baseline 验证 | working tree（本计划改动）+ `git stash` 验证 HEAD `f98d1ee` clean | app-mall-service 全量 | 新增 3 测试单独跑通过：`TestLitemallOrderGoodsBizModel`（expireCommentWindow 过期/未过期 ×2）+ `TestLitemallGrouponBizModel.testPublishRulesGoodsNotOnSale` ×1 | **21 个预存在失败**（clean baseline `git stash` 同样失败，非本计划引入）：auth 10（`TestPasswordReset`/`TestNopAuthUserProfile`/`TestLoginApiSignUp`，userType 字典 `非法的字典项:0`）+ aftersale 5（`TestLitemallAftersaleBizModel`）+ comment 6（`TestLitemallCommentBizModel`）；后两组根因：image domain picUrl 回归（`submit` 时 orderGoods.picUrl 为 null 违反 NOT NULL） | docs/logs/2026/06-19.md; 计划 `2026-06-19-2100` Deferred 区 | **此前 06-17 记录"核心 36 全过"已过时**：平台 `DataInitInitializer` 改动（e2e 计划 06-19）后 aftersale/comment 测试失效。e2e storefront happy-path 不受影响（种子含 `nop_file_record.csv`，39 passed）。 |
| 2026-06-17 | live repo (Plan 2 completed + Plan 3 in progress) | working tree (uncommitted) | app-mall-service 全量 + app-mall-web 编译 | `./mvnw clean package -DskipTests`：全 10 模块 BUILD SUCCESS；`./mvnw -pl app-mall-service test`：111 跑 / 101 过（含新增 TestMallNotificationService 3 + Plan 2 域逻辑加固后测试）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS | 10 auth 测试预存在失败（TestPasswordReset/TestNopAuthUserProfile/TestLoginApiSignUp，`非法的字典项:0`，审计 02-01 userType 字典冲突） | logs/2026/06-17.md | e2e 未实跑。Plan 2 闭合审计通过。 |
| 2026-06-17 | live repo (Plan 1 Phase 1/2/3/4/5 全 completed) | working tree (uncommitted) | app-mall-service 全量 | `./mvnw test -pl app-mall-service -am`：110 测试，100 过（含新增 testConfirmPaidByNotify + testDeleteGoodsWithOrderHistoryRejected，核心 36 全过）；`./mvnw install -pl app-mall-dao -am` BUILD SUCCESS（_app.orm.xml 重新生成，goods→orderGoods cascadeDelete 已移除） | 10 auth 测试预存在失败（TestPasswordReset/TestNopAuthUserProfile/TestLoginApiSignUp，`非法的字典项:0`，审计 02-01 userType 字典冲突，与本变更无关；git stash 验证 clean baseline 同样失败） | logs/2026/06-17.md | 全量 `./mvnw test -pl app-mall-service -am` 已实跑；e2e 未实跑。 |
| 2026-06-16 | live repo (Plan 1 Phase 2/3/5) | working tree (uncommitted) | app-mall-service core (order/aftersale/cart/groupon/coupon) | `./mvnw package -DskipTests -pl app-mall-app -am` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am -Dtest=TestLitemallOrderBizModel,TestLitemallAftersaleBizModel,TestLitemallCartBizModel,TestLitemallGrouponBizModel,TestLitemallGrouponExpireBizModel,TestLitemallCouponUserBizModel` 33 passed (含新增 testCancelExpiredOrders + testApplyAmountExceedsActualPrice) | 10 auth 测试预存在失败（TestPasswordReset/TestNopAuthUserProfile/TestLoginApiSignUp，`非法的字典项:0`，审计 02-01 userType 字典冲突，与本变更无关；git stash 验证 clean baseline 同样失败） | logs/2026/06-16.md | `./mvnw test`（全量）未实跑（auth 失败为预存在）；e2e 未实跑。下次完整绿时补 row。 |

## When To Update

Update this file when:

- full typecheck/build/lint/test verification passes after a meaningful change
- a previously failing command becomes green and should be remembered
- a team intentionally accepts a known failing command and records it as a known failure, not as a passed command

## Rule

Do not mark a command as passed unless it actually ran in the current repository state.

`Commands Passed` must contain only passing commands. Put accepted failures in `Known Failures` with the reason and evidence.

`full` means all real verification commands configured in `docs/context/project-context.md`. Commands explicitly marked `none` are excluded and should be noted.
