# 2026-06-29 Dev Log

## 营销参与退款额度回滚（关闭 P22 deferred「满减参与记录退款回滚」）

**计划：** `docs/plans/2026-06-29-1921-2-promotion-usage-refund-rollback-plan.md`（单 Phase）

### Phase 1 — PromotionUsage 回滚后端（6 处全额退款镜像）

- **Skill Loading：** 加载 `nop-backend-dev` + `nop-testing`，读完 `05-examples/ibiz-and-bizmodel.java`、`02-core-guides/service-layer.md`、`02-core-guides/error-handling.md`、`04-reference/safe-api-reference.md`、`04-reference/bizmodel-method-selfcheck.md`。确认本仓「内部 helper 无 biz 注解」先例（`ILitemallPromotionActivityBiz.resolvePromotionForOrderInternal`、`ILitemallPointsAccountBiz.resolveEarnPerYuan` 均不经 GraphQL 暴露，仅供跨实体调用），`releasePromotionUsage` 据此声明为普通接口方法。
- **Decision D1/D2/D3：** 软删除（实体 `useLogicalDelete` + `deleted` flag，`deleteEntity` 软删，`findCount`/`findList` 自动 `deleted=false` 过滤→额度释放）；仅全额取消/退款回滚（镜像还券/还积分 6 处先例，复用 `isWholeOrder` 边界）；按 `(userId,orderId)` UK 定位，重复软删 0 行幂等。
- **Add：** `ILitemallOrderBiz` 新增 `releasePromotionUsage(orderId, ctx)`（无 biz 注解，匹配 `resolvePromotionForOrderInternal` 内部 helper 先例；`@BizMutation` GraphQL 入口经调用方传递覆盖，符合 Rule #15）；`LitemallOrderBizModel` 实现按 `orderId` 查 PromotionUsage 并 `promotionUsageBiz.deleteEntity` 软删除。
- **Add（6 hook 全接线）：** (1) `cancel`、(2) `cancelExpiredOrders` 在 `returnDeductedPoints` 后调 `releasePromotionUsage`；(3) 售后 `refund`、(4) 售后 `confirmReturnReceived` 在 `isWholeOrder` 分支 `returnOrderDeductedPoints` 后调 `orderBiz.releasePromotionUsage`；(5) `LitemallGrouponBizModel.refundGrouponOrder`、(6) `LitemallPinTuanActivityBizModel.refundMemberOrder` 在 `returnOrderDeductedPoints` 后调 `orderBiz.releasePromotionUsage`（满减可与团购/拼团共存于同一订单）。
- **Proof：** `TestLitemallOrderBizModel` 新增 4 测试（(a) 命中满减下单写 usage→cancel→软删+同活动可再次参与；(b) `cancelExpiredOrders` 批量取消释放；(h) 重复释放幂等 no-op；(i) 非满减订单无 usage 可删无副作用）；`TestLitemallAftersaleBizModel` 新增 3 测试（(c) 售后 refund 全额释放；(d) confirmReturnReceived 全额退货退款释放；(g) 部分项退款**不**释放）；`TestLitemallGrouponExpireBizModel` (e) 团购失败退款释放共存满减 usage；`TestLitemallPinTuanActivityBizModel` (f) 拼团成员退款释放共存满减 usage。
- **Owner doc：** `docs/design/marketing-and-promotions.md` 满减「与取消/退款的关系」+「限购强一致与参与记录」节更新退款回滚语义（区分折扣金额不回退 vs 参与额度释放；6 处镜像先例 + 全额/部分项边界 + 软删除幂等）。
- **验证：** `mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全 10 模块）；`mvn test -pl app-mall-service` **494 测试 0 失败**（+9 新增）。

## 商品成本价与毛利分析报表（关闭 P19 + 用户优惠券报表导出 双源 deferred model-gap）

**计划：** `docs/plans/2026-06-29-1921-1-goods-cost-price-margin-report-plan.md`（三 Phase）

### Phase 1 — ORM 模型新增 costPrice 列

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`，读完 ORM 建模/命名/propId 连续性/stdSqlType 规范。
- **Decision D1/D3：** 毛利口径取「销售额 − Σ(销量 × 当前 costPrice)」（无订单快照，历史精确成本 successor）；costPrice 为空或 0 视为「未维护成本」，毛利列输出空、不计入毛利率分母（避免 0 成本→100% 毛利失真）。
- **Add：** `model/app-mall.orm.xml:717` LitemallGoods `</columns>` 前新增 `COST_PRICE`（name=costPrice，propId=25，precision=10 scale=2，stdDataType=decimal/stdSqlType=DECIMAL，nullable，`ui:show="X"` 前台不可见，displayName「成本价」+ i18n-en「Cost Price」），紧跟 SAFETY_STOCK(propId=24) 之后。
- **Proof：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（10/10 模块）；`_gen/_LitemallGoods.java` 含 costPrice getter/setter + `PROP_NAME_costPrice`/`PROP_ID_costPrice=25` 常量。

### Phase 2 — 毛利分析后端（报表 query + SQL-lib + 鉴权）

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`，读完 service-layer/selfcheck/error-handling/IGraphQLEngine 测试 + auth-and-permissions（@Auth(roles) 经 `userContext.isUserInAnyRole` 检查，仅 `enableActionAuth=true` 时启用，默认关闭→生产开启）。
- **Decision D2：** 扩展 `product` 主题（`buildProductDataSet` + `product-analysis.xpt.xml` 加毛利列），不新增独立 margin 主题（避免商品销量与毛利割裂）。
- **Fix（鉴权回归 M1）：** `getProductAnalysis`（impl + `ILitemallOrderBiz` 接口声明镜像）补 `@Auth(roles="admin")`，与 `exportReport` 同 admin 边界，杜绝成本/毛利经 GraphQL 泄漏给非 admin 认证用户。
- **Add（SQL-lib M2）：** `getGoodsSalesRankingByCategory`（`LitemallOrder.sql-lib.xml:285`）将 `litemall_goods g` 的 INNER JOIN 由 `<c:if categoryId>` 条件化改为**无条件**（始终 join + `g.DELETED=0`），SELECT 加 `SUM(og.NUMBER * g.COST_PRICE) AS COST_AMOUNT`，categoryId 降级为 WHERE 过滤。
- **Add（DTO）：** `GoodsStatisticsBean` 新增 `costAmount`/`grossProfit`/`marginRate`。
- **Add：** `getProductAnalysis` 取 ranking 后调 `enrichMarginFields`（D1/D3：grossProfit=salesAmount−costAmount，marginRate=grossProfit/salesAmount[HALF_UP 4位]；costAmount null/≤0 视为未维护→三列均 null，不计入毛利率分母）；`buildProductDataSet` 每行加 costAmount/grossProfit/marginRate（空/未维护输出空串）。
- **Proof：** `TestLitemallOrderStatisticsBizModel` 新增 4 IGraphQLEngine 测试：(a) `testGetProductAnalysisMarginWithCostPrice`（costPrice=60→costAmount=120/grossProfit=80/marginRate=0.4；null/0→三列 null）；(b) `testGetProductAnalysisCategoryFilterDowngradedToWhere`（JOIN 降 WHERE 后 categoryId 过滤结果一致）；(c) `testExportReportProductWithMarginData`（xlsx/pdf 含毛利数据不报错）；(d) `testGetProductAnalysisRequiresAdminAuth`（反射守卫 interface+impl @Auth(roles="admin") 元数据，运行时强制由平台 enableActionAuth 控制）。全工作区 `./mvnw test` **493 测试 0 失败**（485 service + 8 web，+4 新增）；`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS。

### Phase 3 — nop-report 模板 + 前后台展示

- **Skill loading：** 加载 `nop-frontend-dev`，读完 XView 三层模型 + bounded-merge + x:prototype + view-and-page-customization。
- **Add（xpt 模板）：** `product-analysis.xpt.xml` 表头行 + 数据行（`*=^productList!field`）各加 costAmount/grossProfit/marginRate 三列（Latin 列名，与既有 funnel/product 先例一致，PDFBox Helvetica 仅 Latin）。
- **Add（admin 报表页）：** `stat-product.page.yaml`「销量排行 TOP20」表格补「成本额/毛利额/毛利率」三列（消费 `getProductAnalysis` 返回的新字段）。
- **Add（admin 商品编辑）：** `LitemallGoods.view.xml`（bounded-merge 定制层）edit form layout 加 `costPrice[成本价（内部，前台不可见）]`（与 counterPrice 同行）+ grid `<col id="costPrice"/>`（retailPrice 之后）。`ui:show="X"` 仅排除出 `_gen` 自动生成，xmeta 仍含 prop（`queryable="true"`），显式引用生效；`add` form 经 `x:prototype="edit"` 自动继承。
- **Add（owner doc）：** `system-configuration.md` 商品分析指标口径补「毛利维度」bullet（D1/D3 口径 + admin 边界 + 导出 + 残留风险）；`product-catalog.md` 新增「商品成本价（内部字段）」子节（字段语义 + 用途 + 非订单快照 successor）。
- **Proof：** `./mvnw -pl app-mall-web -am compile` BUILD SUCCESS；`TestReportExportPageLoad.testStatProductPageLoads` 加载 stat-product.page.yaml 通过；全工作区 `./mvnw test` 493 全绿；`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` 全 10 模块 BUILD SUCCESS。

### 验证

- `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全 10 模块）。
- `./mvnw test` BUILD SUCCESS（app-mall-service 485 + app-mall-web 8，known-good baseline = 493，无回归）。
- **Commit-time full-green verification（2026-06-29）：** 493 测试 known-good baseline。

### 关闭的 deferred 项

- `docs/plans/2026-06-28-1027-2-phase19-report-system-extension-plan.md:155`（毛利/成本报表，缺 costPrice，model-gap）
- `docs/plans/2026-06-29-0119-1-user-coupon-report-export-plan.md:146`（毛利/成本报表，缺 costPrice，同 model-gap）

---

## 积分有效期与自动过期（关闭 P27 deferred successor「积分有效期」）

**计划：** `docs/plans/2026-06-29-1200-1-points-validity-auto-expiry-plan.md`（三 Phase 全部完成，489 测试全绿）

### Phase 1 — ORM：PointsExpireBatch 有效期批次实体

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`。
- **Add（ORM）：** `model/app-mall.orm.xml` 新增 `LitemallPointsExpireBatch`（tableName `litemall_points_expire_batch`，`versionProp=version`）：字段 id/accountId/userId/totalPoints/remainingPoints/expireTime/sourceType/sourceId + version/addTime/updateTime；to-one account 关系；索引 `idx_points_expire_batch_user_expire(userId,expireTime)`；唯一键 `uk_points_expire_batch_source(sourceType,sourceId)`（与 earn PointsFlow 同源幂等）。对齐 PointsAccount/PointsFlow 约定（userId/accountId 用 INTEGER+string、version domain）。codegen 重生成 entity/_gen/IBiz/xbiz。
- **Decision D1：** `mall_points_validity_days` 缺失默认 730 天（2 年），后台可覆盖。
- **Proof：** `mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（10/10 模块，ORM/DDL 校验无错）。

### Phase 2 — 后端：批次 FIFO 账本 + 过期编排 + 定时 job + IGraphQLEngine 测试

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`；读 selfcheck（19 项）+ concurrency-and-transactions（CAS 模式）。
- **Decision D2/D3：** balance 为真相源，批次为「可过期账本」，不变量 `balance >= SUM(remainingPoints)`；存量（特性前 balance 无批次）视为不过期、自然消耗。
- **Add（IBiz/BizModel）：** `ILitemallPointsAccountBiz` + `LitemallPointsAccountBizModel` 新增 `expirePoints`(@BizMutation，扫描到期批次→复用 `mutateBalance` 的 flush→clear→`updateBalanceIfVersion` CAS 序列扣 balance→写 EXPIRE(20) 流水→zero 批次，并发败者顺延下轮)、`getMyPointsExpiryHint`(@BizQuery，返回最近未到期批次 {points,expireTime} 或 null)。`mutateBalance` 扩展同事务批次同步：earn/正向调账建批次、spend/负向调账 FIFO(expireTime ASC) 消耗 `remainingPoints`。新建 `LitemallPointsExpireBatchBizModel`（CrudBizModel + 三个账本范围查询 `findExpirableBatchesForUser`/`findExpiredBatches`/`findSoonestNonExpiredForUser`，用 `doFindListByQueryDirectly` 绕过 xmeta filter-op 白名单，mimic `expireCoupons`）。
- **Decision D4：** 每轮 `@BizMutation` 自动事务 + account.version 乐观锁兜底并发；不扩错误码（复用既有 `ERR_POINTS_EARN_FAILED` 约定）。
- **Add（定时任务）：** `MallJobInvoker.expirePoints` + `scheduler.yaml` `expire-points`（repeatInterval 3600000 每小时）。
- **Proof：** `TestLitemallPointsAccountBizModel` 扩展 10 测试（earn 建批次+expireTime / 正向调账建批次 / spend FIFO 顺序 / spend 越过批次回退存量 / expirePoints 扣减+EXPIRE 流水 / 幂等重放不双扣 / 多批次单轮顺序 CAS / expiry hint 最近批次 / hint 跳过 0 批次 / e2e earn→spend 账本一致），全经 IGraphQLEngine RPC。`mvn test` 全工作区 **489 测试 0 失败**（481 service + 8 web）；`mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS。

### Phase 3 — 前端 + owner doc 收尾

- **Skill loading：** 加载 `nop-frontend-dev`。
- **Add（前端）：** `my-points.page.yaml` 新增 `pointsExpiryHint` service 消费 `getMyPointsExpiryHint`，`visibleOn: ${!!data}` 条件展示「您有 N 积分将于 YYYY-MM-DD 过期」alert（仅存量返回 null 时不展示）。
- **Add（owner doc）：** `docs/design/wallet-and-assets.md` 积分有效期章节由「计划中能力」翻「已实现」，补批次账本/FIFO 消耗/存量不过期(D3)/不变量(D2)/过期 job/前端提示实现说明。
- **Proof：** `mvn clean install -pl app-mall-web -am -DskipTests` BUILD SUCCESS（page.yaml 校验通过）。

### 验证

- `mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全 10 模块）。
- `mvn test` BUILD SUCCESS（app-mall-service 481 测试 + app-mall-web 8 页面加载测试，known-good baseline = 489，无回归）。
- **Commit-time full-green verification（2026-06-29）：** 489 测试 known-good baseline。

## 积分+现金组合兑换（关闭 P27/P29 deferred successor）

**计划：** `docs/plans/2026-06-29-1045-3-points-cash-combo-exchange-plan.md`（三 Phase 全部完成，470 测试全绿）

### Phase 1 — 设计决策 E1-E5 + owner doc + ORM 扩展

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`。
- **Decision E1-E5：** PointsGoods 新增 `cashPrice`（BigDecimal，E1）；PointsExchangeOrder 新增 `payStatus`/`payChannel`/`cashPrice`/`walletPayAmount`（E2，payChannel 复用 int+dict `mall/pay-channel` 与 LitemallOrder 一致）；积分先扣+现金后付+超时取消兜底（E3）；新增 PE outTradeNo 前缀 + PaymentCallbackImpl 回调路由分支（E4 Protected Area 裁定=消费非通道行为修改，MISSION_DRIVER 授权）；余额走直接 debitBalance 不建 BalancePayChannel bean（E5 adjudicated split）。
- **Add（ORM）：** `model/app-mall.orm.xml` exchange-status dict 增 `AWAITING_PAYMENT=5`；PointsGoods `cashPrice`(propId 15)；PointsExchangeOrder `payStatus`/`payChannel`/`cashPrice`/`walletPayAmount`(propId 21-24)。codegen 重生成 `_AppMallDaoConstants.EXCHANGE_STATUS_AWAITING_PAYMENT=5` + `_gen` 实体字段；deploy SQL 三方言同步。
- **Add（owner doc）：** `docs/design/marketing-and-promotions.md` 补「组合兑换流程」设计子节（E1-E5 Decisions + 状态机 + 原子性 + 取消退款路径 + outTradeNo 路由），原「组合兑换 Deferred」标注改「已交付」。
- **Proof：** `mvn install` BUILD SUCCESS；既有 `TestLitemallPointsExchangeOrderBizModel` 12 测试全绿（纯积分零回归）。

### Phase 2 — 后端组合兑换收银编排 + 回调路由 + 测试

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`。
- **Add（IBiz/BizModel）：** `ILitemallPointsExchangeOrderBiz` + `LitemallPointsExchangeOrderBizModel` 新增：`exchangeCombo`(@BizMutation，建单 AWAITING_PAYMENT + 积分先扣 + 派生 PE outTradeNo)、`payComboByChannel`(@BizMutation，通道 prepay)、`payComboByBalance`(@BizMutation，直接 debitBalance + 推进 PENDING，复用登录密码凭证)、`confirmExchangePaidByNotify`(@BizAction，回调 AWAITING_PAYMENT→PENDING 幂等)、`refundExchangeOrderByNotify`(@BizAction，退款对账 PAID→REFUNDED)、`cancelExpiredExchangeOrders`(@BizMutation，超时取消兜底)。`cancelExchangeOrder` 扩展支持 AWAITING_PAYMENT + 已付现金退款（余额同步 creditBalance / 通道异步 refund）。
- **Add（Mapper）：** 新建 `LitemallPointsExchangeOrderMapper` + `LitemallPointsExchangeOrder.sql-lib.xml`（`updateStatusIfMatch` 原子状态守卫，mimic LitemallOrderMapper），注册到 `app-dao.beans.xml`。
- **Add（回调路由）：** `PaymentCallbackImpl.onPaymentSuccess`/`onRefundSuccess` 新增 PE 前缀分支，路由不变式保持（order outTradeNo 32 位小写 hex 与大写 PE/RC 互斥）。
- **Add（定时任务）：** `MallJobInvoker.cancelExpiredExchangeOrders` + `scheduler.yaml` `cancel-expired-exchange-orders`（复用 nop-job-local）。
- **Proof：** 新增 `TestLitemallPointsExchangeComboBizModel` 15 测试（建单/余额支付/凭证校验/余额不足/通道禁用/回调推进/幂等/PE 路由/路由不变式/AWAITING 取消/已付取消退余额/超时取消/maxPerUser 含 AWAITING/纯积分零回归）；`@BizAction` 经 I*Biz 调用需 `ormTemplate.runInSession` 包裹（沿用 TestLitemallRechargeBizModel 先例）。app-mall-service 全量 **470 测试 0 失败**；`mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS。

### Phase 3 — 前端 + owner doc 收尾

- **Skill loading：** 加载 `nop-frontend-dev`。
- **Add（前端）：** `points-mall.page.yaml` 按 cashPrice 分流（纯积分 `exchange` / 组合 `exchangeCombo`）+ 展示积分+现金双价；`my-exchange-orders.page.yaml` 组合单展示 AWAITING_PAYMENT(5) + 余额支付入口（payComboByBalance 登录密码确认）+ 取消入口（exchangeStatus 0||5）；admin `LitemallPointsGoods.view.xml` grid + edit form 增 cashPrice 列/字段。
- **Proof：** `mvn install -pl app-mall-web -am` BUILD SUCCESS（page.yaml/view.xml 语法校验通过）。

### 验证

- `mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全 10 模块）。
- `mvn test` BUILD SUCCESS（app-mall-service 470 测试 0 失败 0 错误 + app-mall-web 8 页面加载测试，known-good baseline = 478，无回归）。
- **Commit-time full-green verification（2026-06-29）：** 478 测试 known-good baseline。

## 商品级安全库存字段与三级库存预警（关闭 P18-D4 + P36 deferred）

**计划：** `docs/plans/2026-06-29-1045-2-goods-level-safety-stock-plan.md`（三 Phase 全部完成）

### Phase 1 — ORM 模型扩展（LitemallGoods.safetyStock）

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`。
- **Decision S1：** `LitemallGoods` 新增可空 int 列 `safetyStock`（`code="SAFETY_STOCK" propId=24 stdSqlType="INTEGER"`，displayName「安全库存预警线（商品聚合级）」），与既有 `LitemallGoodsProduct.safeStock`（:826）同型。备选 decimal 否决（库存整数单位）。
- **Add：** `model/app-mall.orm.xml:715` LitemallGoods `</columns>` 前插入新列。
- **Proof：** `./mvnw install` 触发 codegen 重生成 `_gen/_LitemallGoods.java:116-118/306-307/1330-1343` 含 safetyStock getter/setter；`deploy/sql/{mysql,postgresql,oracle}/_create_app-mall.sql` 同步含新列。

### Phase 2 — 后端三级阈值解析 + DTO/SQL-lib + 测试

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`。
- **Add（DTO）：** `StockWarningSkuBean` 加 `safetyStock`（Integer）；`StockWarningItemBean` 加 `safetyStock`（Integer）+ `thresholdSource`（String）。
- **Fix（Path A 工作台 SKU 粒度）：** `LitemallGoodsBizModel.getStockWarningList:679-712` 阈值解析从两级改为三级：`product.safeStock`（非空且 >0）→ `goods.safetyStock`（非空且 >0）→ 全局。`thresholdSource ∈ {"safeStock","safetyStock","global"}` 标注实际命中档。
- **Fix（Path B Dashboard goods 聚合）：** `LitemallOrder.sql-lib.xml:getStockWarningList:125-143` SELECT 增 `g.SAFETY_STOCK AS SAFETY_STOCK` + `CASE WHEN g.SAFETY_STOCK IS NOT NULL AND g.SAFETY_STOCK > 0 THEN 'safetyStock' ELSE 'global' END AS THRESHOLD_SOURCE`；GROUP BY 加 `g.SAFETY_STOCK`；HAVING 改 `COALESCE(SUM(p.NUMBER),0) <= COALESCE(NULLIF(g.SAFETY_STOCK,0), ${threshold})`（NULLIF 处理 safetyStock=0 与 null 等价，与 Java 侧 "非空且 >0" 口径一致）。`getTodoAggregation:1347-1348` Java 入参不变（全局阈值作 SQL 内 fallback）。
- **Proof（IGraphQLEngine）：** `TestLitemallGoodsOpsWorkbench` 扩展三个场景：`testGetStockWarningListWithGoodsLevelSafetyStock`（per-SKU 空命中 per-goods safetyStock=8）/ `testGetStockWarningListSkuTierPriorityOverGoodsTier`（per-SKU safeStock=10 优先于 safetyStock=2）/ `testGetStockWarningListSafetyStockZeroFallsBackToGlobal`（safetyStock=0 视为无效回退 global）。`TestLitemallOrderStatisticsBizModel` 加 `testGetTodoAggregationStockWarningWithGoodsLevelSafetyStock`：totalStock=15 > 全局 10 默认不预警；safetyStock=20 后 15 ≤ 20 触发且 `thresholdSource=safetyStock`。
- **Proof：** 全量 `./mvnw test` 通过（54 测试新增/扩展项全绿，既有测试无回归）。

### Phase 3 — 前端 + owner doc

- **Skill loading：** 加载 `nop-frontend-dev`。
- **Add（工作台）：** `stock-warning.page.yaml` 增加 `safetyStock`（商品安全库存）列；`thresholdSource` 映射扩展 `safetyStock→商品级`；alert 文案改为三级优先描述。
- **Add（Dashboard）：** `stat-dashboard.page.yaml` 库存预警明细表增加 `safetyStock`/`thresholdSource` 列（与工作台对齐）。
- **Add（商品编辑）：** `LitemallGoods.view.xml` edit form layout 在 `isOnSale` 行加 `safetyStock`（可空 int，标签含「空或0表示不启用商品级阈值，回退到 SKU safeStock 与全局配置」），`add` form 经 `x:prototype="edit"` 自动继承。
- **Add（owner doc）：** `docs/design/system-configuration.md` G3 段重写为三级阈值口径（per-SKU → per-goods → global），同时更新 :339 待办聚合库存预警口径 + :521 工作台方法描述；`docs/design/product-catalog.md` 库存语义化段补「运营侧安全库存阈值」子节，明确与展示侧三档语义的关注点区分。
- **Proof：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` + `./mvnw test` 全绿。

### 验证

- `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全模块）。
- `./mvnw test` BUILD SUCCESS（app-mall-service 44s，全模块无失败）。
- **Commit-time full-green verification（2026-06-29）：** `mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` 全 10 模块 BUILD SUCCESS + `mvn test` 463 全绿（service 455 + web 8，含 4 个新增三级阈值 IGraphQLEngine 场景，known-good baseline = 463，无回归）。

### 关闭的 deferred 项

- `docs/plans/2026-06-28-1027-1-phase18-dashboard-redesign-plan.md` P18-D4（每商品独立安全库存字段，model-gap）
- `docs/plans/2026-06-28-1027-3-phase36-goods-operations-enhancement-plan.md` P36（每商品 goods 级安全库存，out-of-scope improvement）

---

## ORM 数据完整性约束补强（P27/P28/P31 deferred model-gap 批次关闭）

**计划：** `docs/plans/2026-06-29-1045-1-orm-data-integrity-constraints-plan.md`（两 Phase 全部完成）

### Phase 1 — ORM 约束建模（Decisions + 模型扩展）

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`，读完 ORM 建模/命名/索引规范。
- **Decision D1：** `LitemallPointsFlow` 新增多列唯一键 `uk_litemall_points_flow_source`（`columns="sourceType,sourceId"`）。可空列在 MySQL/PostgreSQL 唯一键允许多 NULL（不影响 adjustPoints/无 source 流水）。
- **Decision D2：** `LitemallCheckInRecord` 新增非唯一索引 `idx_check_in_record_user_date`（userId+checkInDate）。据 `logical-deletion.md:68,197` 裁定——BOOLEAN 逻辑删除方案下唯一键不可行（仅 delVersion BIGINT 方案可），改用非唯一索引。delVersion 迁移记入 Deferred。
- **Decision D3：** `LitemallOrder.pickupCode` 新增非唯一索引 `idx_order_pickupCode`。唯一性已由应用层 UUID 保证，gap 核心是反查性能（全表扫描）。
- **Add：** `model/app-mall.orm.xml` 三处约束/索引（mimic `uk_promoUsage_user_order`、`idx_order_userId` 先例）。
- **Proof：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS；生成产物 `_app.orm.xml:1221/1916/1983` 含三处新约束。

### Phase 2 — 应用层异常翻译 + 测试

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`，读完 CrudBizModel + selfcheck + error-handling + IGraphQLEngine 测试规范。平台源码核验：`CrudBizModel.checkUniqueForSaveEntity:587` + `DialectSQLExceptionTranslator:205`（`SQLIntegrityConstraintViolationException` → `JdbcException(ERR_SQL_DATA_INTEGRITY_VIOLATION)`）。
- **Fix：** `LitemallPointsAccountBizModel.earnPoints:99-111` 在 `mutateBalance` 调用处加 try-catch；`isPointsFlowUniqueKeyConflict:271-277` 识别两层冲突码（CrudBizModel 管道 `ERR_BIZ_ENTITY_WITH_SAME_KEY_ALREADY_EXISTS` + DB 层 `ERR_SQL_DATA_INTEGRITY_VIOLATION`），翻译为 `new NopException(ERR_POINTS_DUPLICATE_EARN, e)` 保留异常链。应用层 `countFlowBySource` 预检保留为快速路径。
- **Fix（验证）：** checkIn 路径无 DB 唯一键冲突翻译（D2 非唯一索引）；防重签正确性继续由应用层 `findRecord`（逻辑删除感知）保证，10 测试全绿无回归。
- **Proof：** `TestLitemallPointsAccountBizModel` 扩展 `testEarnIdempotentRejectsDuplicateSource` + 新增 `testEarnDuplicateRejectedAfterDirectFlowInsert` 断言 `ERR_POINTS_DUPLICATE_EARN` 错误码；`TestLitemallCheckInRecordBizModel` 扩展 `testDuplicateSameDayRejected` 断言 `ERR_CHECK_IN_ALREADY_TODAY` 错误码；`TestLitemallPickupDeliveryBizModel` 10 测试全绿（行为契约不变）。

### 验证

- `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全模块）。
- `./mvnw test` **459 全绿**（service 451 + web 8；含 3 个扩展/新增 IGraphQLEngine 测试，既有测试无回归）。
- **Commit-time full-green verification（2026-06-29）：** 459 测试 known-good baseline。
- 四个源 deferred model-gap 条目已在各自源计划标注「已由 successor 关闭」。

---

## 自动化会员权益发放（P20 deferred successor）

**计划：** `docs/plans/2026-06-29-0900-2-member-benefits-automation-plan.md`（四 Phase 全部完成）

### Phase 1 — 模型与裁定

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`，读完 ORM 建模/命名/Delta/字典规范。
- **Decision D1：** 选**方案 A**——`LitemallCoupon` 新增 `minMemberLevel`（propId=21, dict `mall/user-level`），0=全员可领。
- **Decision D2：** 选**方案 A**——首期交付生日礼包。**核实发现基类 `NopAuthUser` 已有 birthday（propId=15）**，无需 Delta 扩展。
- **Decision D3：** 选**方案 A**——订单 confirm 路径调既有 `evaluateUserLevel` 自动评估升级（hysteresis：仅自动升级，降级由 `downgradeExpiredLevels` 负责）。
- **Decision D4：** 选**方案 A**——自动发放计入 `limit`（复用 `claimCouponForUser`，超限静默跳过）。
- **Add：** `model/app-mall.orm.xml` Coupon 加 `minMemberLevel`；codegen 重生成实体/_app.orm.xml/三方言 DDL。

### Phase 2 — 后端自动发放服务

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`，读完 CrudBizModel + selfcheck 19 项 + IGraphQLEngine 测试 + error-handling。
- **Add：** `LitemallCouponUserBizModel.claimCouponForUser` 加等级准入校验（`ERR_COUPON_MEMBER_LEVEL_INSUFFICIENT`）。
- **Add：** `LitemallMemberLevelBizModel` 等级提升发券（`ensureUserLevel` 检测 upgrade → `dispatchLevelUpBenefit`）+ 生日礼包（`dispatchBirthdayCoupons`）+ 通知（`MallNotificationService.sendUserMessage`）+ 配置开关（LitemallSystem key/value）。
- **Add：** `LitemallOrderBizModel.confirm`/`confirmExpiredOrders` 接入 `evaluateUserLevel` 自动升级。
- **Add：** `MallJobInvoker.dispatchBirthdayCoupons()` + `scheduler.yaml` 新增 `dispatch-birthday-coupons` job（daily 86400000ms）。
- **Add：** `LitemallCouponBizModel.listCouponsForGoods` 透出 `minMemberLevel` + `claimable` 联动。
- **Add：** ErrorCode `ERR_COUPON_MEMBER_LEVEL_INSUFFICIENT`；接口 `ILitemallMemberLevelBiz.dispatchBirthdayCoupons`。
- **Proof：** `TestLitemallMemberBenefitsBizModel` 7 用例（等级不足/满足/无限制/等级提升发放/开关关闭/生日幂等/无配置）。全量 **450 测试全绿**。

### Phase 3 — 前台与后台页面

- **Skill loading：** 加载 `nop-frontend-dev`，读完 XView 三层模型 + bounded-merge。
- **Add：** `LitemallCoupon.view.xml` grid + view/edit form 加 `minMemberLevel`。
- **Add：** `coupon-center.page.yaml` 加会员专属券 badge（`LV{n}+ 会员专属`）。
- **Add：** `user-center.page.yaml` 加「会员权益」预览区。

### 验证

- `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全模块）。
- `./mvnw test -pl app-mall-service` **450 全绿**（含 7 新增会员权益测试，既有 443 无回归）。
- **Commit-time full-green verification（2026-06-29）：** 450 测试 known-good baseline。

---

## 积分商城兑换（P27 deferred successor）

**计划：** `docs/plans/2026-06-29-0900-1-points-mall-exchange-plan.md`（四 Phase 全部完成）

### Phase 1 — 模型与兑换语义裁定

- **Skill loading：** 加载 `nop-orm-modeler`、`nop-database-design`，读完 ORM 建模/命名/主键/索引/字典/域规范 + 既有 `PointsAccount`/`FlashSale` 实体先例比对。
- **Decision D1：** 选**方案 A**——新建 `LitemallPointsGoods`（goodsId/productId + pointsPrice + exchangeStock + exchangedCount + maxPerUser + startTime/endTime + status[复用 promotion-status]）。备选 B（侵入 `LitemallGoods`）被否。
- **Decision D2：** 选**方案 A**——新建 `LitemallPointsExchangeOrder`（独立状态机 PENDING/SHIPPED/COMPLETED/CANCELLED + 收货地址快照 + 商品快照 + 物流单号）。备选 B（复用 `LitemallOrder`）被否。
- **Decision D3：** 选**方案 B**——首期只交付纯积分兑换（firm 结果面），组合兑换列 Deferred（显式 scope change）。
- **Add：** `model/app-mall.orm.xml` 新增 `LitemallPointsGoods`(14 字段) + `LitemallPointsExchangeOrder`(20 字段 + 2 索引) + 字典 `mall/exchange-status`；同步 `deploy/sql/{mysql,oracle,postgresql}` DDL + drop + index。codegen 重生成实体/IBiz/xmeta/_gen view 全部产出。

### Phase 2 — 后端兑换服务

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`，读完 CrudBizModel 模式 + selfcheck 19 项 + 反模式表 + IGraphQLEngine 测试模式 + PointsAccountBizModel.spendPoints 签名 + FlashSaleBizModel 特殊订单先例。
- **Add：** `LitemallPointsGoodsBizModel`（`activePointsGoods`/`pointsGoodsDetail` 前台 + `publishActivity`/`unpublishActivity` admin）。
- **Add：** `LitemallPointsExchangeOrderBizModel`（`exchange` 核心兑换[校验→原子扣减 exchangeStock mapper→saveEntity+flush→spendPoints→re-read] + `myExchangeOrders` + `shipExchangeOrder` admin + `confirmExchangeOrder` + `cancelExchangeOrder`[退积分+还库存]）。
- **Add：** ErrorCode 9 个（NOT_FOUND/NOT_ACTIVE/NOT_IN_WINDOW/SOLD_OUT/OVER_LIMIT_PER_USER/OFF_SHELF/QUANTITY_INVALID/ORDER_NOT_FOUND/STATUS_TRANSITION_INVALID）。
- **Add：** `LitemallPointsGoods.sql-lib.xml`（`reduceExchangeStock`/`restoreExchangeStock` 条件 UPDATE）+ `LitemallPointsGoodsMapper` + `app-dao.beans.xml` 注册。
- **Fix（实现期发现）：** GraphQL 过滤层仅允许 `{eq, in, dateBetween, dateTimeBetween}`（不支持 `ne`）→ maxPerUser guard 改用 `in` 枚举非取消状态（与 FlashSale sessionStatus 同先例）；EQL 不支持 `greatest()` → restoreExchangeStock 改用 `CASE WHEN ... THEN ... ELSE 0 END`。
- **Proof：** `TestLitemallPointsExchangeOrderBizModel` 12 用例（成功兑换 + 6 失败模式 + 取消退还 + 发货确认流 + 我的订单过滤 + 账户流水对账）+ `TestLitemallPointsGoodsBizModel` 4 用例。全量 **443 测试全绿**（无回归）。

### Phase 3 — 前台与后台页面

- **Skill loading：** 加载 `nop-frontend-dev`，读完 XView 三层模型 + bounded-merge + x:prototype + 反模式表。
- **Add：** 后台 `LitemallPointsGoods.view.xml`（bounded-merge grid + edit form + publish/unpublish 行按钮）。
- **Add：** 后台 `LitemallPointsExchangeOrder.view.xml`（列表 + query 筛选 + 发货行按钮 dialog 收集 shipCode）。
- **Add：** 前台 `mall/points/points-mall.page.yaml`（积分余额 + activePointsGoods 列表 + 兑换数量 dialog → exchange mutation）+ `mall/points/my-exchange-orders.page.yaml`（myExchangeOrders + 确认收货/取消按钮）。
- **Add：** `app-mall.action-auth.xml` 注册 4 路由（storefront-points-mall / storefront-my-exchange-orders / mall-points-goods / mall-points-exchange-order）。

### 验证

- `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS（全模块，含 codegen 重生成 + app-mall-web 编译）。
- `./mvnw test -pl app-mall-service` **443 全绿**（含 16 新增积分兑换测试，既有 427 无回归）。
- **Commit-time full-green verification（2026-06-29）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` 全 10 模块 BUILD SUCCESS + `./mvnw test` 443 全绿（known-good baseline = 443，无回归）。
- P27 Deferred「积分商城兑换」successor 已于源计划 `2026-06-27-2029-1` 标注 Successor Closed（Phase 27 已 done，不改 Phase Status 行）。

---

## 营销活动效果报表导出（P22 deferred successor）

**计划：** `docs/plans/2026-06-29-0830-1-marketing-effectiveness-report-export-plan.md`（两 Phase 全部完成）

### Phase 1 — 后端：exportMarketingReport + xpt 多 sheet 模板 + 测试

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`、`nop-debugging`，读完 `service-layer.md`、`bizmodel-method-selfcheck.md`（19 项逐方法校验 exportMarketingReport）、`error-handling.md`、`ai-defaults.md` 反模式表、`testing.md`、`generate-report.md` runbook（xpt 多 sheet + `*=^var!field` 单对象展开）。
- **Explore/Decision：** D1 选**选项 A**——`exportMarketingReport` `@BizQuery @Auth(admin)` 于 `LitemallPromotionActivityBizModel`，注入 `IReportEngine` + 3 sibling `I*Biz`（FlashSale/PinTuanActivity/Coupon）聚合 4 效果 Bean。D2 单 xlsx 多 sheet（满减/秒杀/拼团/优惠券 4 sheet）。
- **Add：** `MarketingReportRequest` `@DataBean`（app-mall-dao/dto）——方法有 7 业务字段（renderType+时间窗+4 归因ID）>5，按 selfcheck rule #5 用 `@RequestBean`+`@DataBean`（沿 `AftersaleApplyRequest` 先例），GraphQL 入参形状不变。
- **Add：** `ILitemallPromotionActivityBiz.exportMarketingReport(@RequestBean, context)` 声明 + `LitemallPromotionActivityBizModel` 实现：4 `buildXxxEffectDataSet` 适配为单元素 `List<Map>`（`Collections.singletonList`，与 `buildCouponDataSet` 先例一致），派生率指标（拼团成团率/券核销率）在适配层计算（与 AMIS 同口径），复用 `ReportRenderTypes.validate` + `NopException.adapt(e)` + `resource.delete()` on failure。
- **Add：** `marketing-effect.xpt.xml`（4 sheet，表头/指标 Latin，`*=^var!field` 单对象展开，与 `coupon-analysis.xpt.xml` 同模式）。
- **Fix（预置缺陷，偏离 Non-Goal「不改 SQL-lib」已论证）：** `LitemallMarketing.sql-lib.xml` 3 query（`getCouponUsageStatistics`/`getPinTuanEffectiveness`/`getFlashSaleEffectiveness`）用 `(${id} IS NULL OR col=${id})` 处理可选 ID 过滤，但 Nop xpl-sql `${arg}` 对 null 输出**空文本**（非 `?`），id=null 时生成 `( IS NULL OR col= )` 非法 SQL → 抛 `bad-sql-grammar`。该缺陷同时影响**既有** `marketing-effect.page.yaml` 3 面板（生产 latent bug，既有单测一律传非 null id 未覆盖 null 路径）。修复：改 `<c:if test="${id != null and !id.isEmpty()}">`（`LitemallOrder.sql-lib.xml:199` 先例），**口径不变**（null=全量聚合、非null=按id过滤）。bug 笔记：`docs/bugs/04-marketing-effectiveness-queries-null-id-sql-grammar.md`。
- **Proof：** `TestLitemallPromotionActivityBizModel` 5 新测试（xlsx/pdf/带归因参数/空集/非法 renderType）。`./mvnw test -pl app-mall-service -am` 427 全绿（既有 FlashSale 19/PinTuan 10/Coupon 8 非 null 用例无回归）。

### Phase 2 — 前端导出入口 + owner doc + 收口

- **Skill loading：** 加载 `nop-frontend-dev`、`nop-testing`，读完 `application-project-defaults.md`、`view-and-page-customization.md`、`testing.md`；参考 `stat-funnel.page.yaml:32-52` 导出按钮先例。
- **Add：** `marketing-effect.page.yaml` 增 xlsx/pdf 导出入口（`select marketingFormat` + `button actionType:ajax responseType:blob`，`url:'@query:LitemallPromotionActivity__exportMarketingReport'`，renderType 绑格式选择，startDate/endDate 派生自 dateRange，可选 promotionActivityId）。
- **Proof：** `TestReportExportPageLoad.testMarketingEffectPageLoads` 新增。`./mvnw -pl app-mall-web -am test` 8 web 全绿。
- **Add：** `system-configuration.md`（效果分析看板补导出口径 + 关闭 P22 Deferred successor）+ `marketing-and-promotions.md`（效果面板口径节补导出说明 + 可选 ID 过滤经 `<c:if>`）。

### 验证

- `./mvnw test -pl app-mall-service -am` 427 全绿；`./mvnw -pl app-mall-web -am test` 8 全绿；`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS。
- P22 Deferred「复杂报表导出（PDF/Excel）」successor 已于源计划 `2026-06-28-0340-1` 标注 Successor Closed。

---

## walletRechargeRefund 钱包充值退款异步对账（P29 deferred successor）

**计划：** `docs/plans/2026-06-29-0330-1-wallet-recharge-refund-flow-plan.md`（单 Phase 完成）

### Phase 1 — 充值退款异步对账（后端 + 测试 + owner doc）

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`，读完 `service-layer.md`、`error-handling.md`、`ai-defaults.md` 反模式表、`bizmodel-method-selfcheck.md`（19 项逐方法校验）、`testing.md`。
- **Add：** `LitemallWalletBizModel` 新增 `SOURCE_TYPE_RECHARGE_REFUND = "recharge-refund"`（偏离 owner doc 预留的宽泛 `refund`，以明确归属充值退款、与正向 `recharge` 对称可追溯）。
- **Add：** `ILitemallRechargeBiz` 新增 `@BizAction refundRechargeByNotify(outTradeNo, outRefundNo, context)`（受信内部入口，与 `confirmRechargeByNotify` 同安全模型，不暴露 GraphQL）。
- **Add：** `LitemallRechargeBizModel.refundRechargeByNotify` 实现：解析 outTradeNo（`RC` 前缀 + parseInt）；幂等守卫（UNPAID→no-op / REFUNDED→no-op）；PAID→`walletBiz.debitBalance(amount+giftAmount, REFUND, recharge-refund, rechargeId)` 回冲 + 推进 REFUNDED；Decision D1 两类失败模式（D1a `ERR_WALLET_INSUFFICIENT` catch+WARN 人工对账不推进；D1b `ERR_WALLET_VERSION_CONFLICT` catch+WARN 视为幂等 return，乐观锁保证不双扣）。
- **Fix：** `PaymentCallbackImpl.onRefundSuccess` RC 分支由「记 INFO 并 return」改为调用 `rechargeBiz.refundRechargeByNotify(...)`（复用既有 `@Inject ILitemallRechargeBiz`），更新 javadoc 与注释。
- **Proof：** `TestLitemallRechargeBizModel` 6 新测试（PAID 回冲 / REFUNDED 重放幂等 / UNPAID no-op / 非法+不存在 outTradeNo no-op / 余额不足 D1a 降级 / PaymentCallbackImpl RC 路由）。15 测试全绿；`mvn test -pl app-mall-service -am` 422 全绿（含 `TestAlipayChannelAndRefundNotify` 订单侧 8 测试无回归）。
- **发现（IoC 注入缺口，pre-existing）：** `PaymentCallbackImpl`（plain `@Named` bean）的 `@Inject ILitemallRechargeBiz rechargeBiz` 在测试 IoC 上下文中为 null（NopIoC autowire-by-type 对 plain `@Named` bean 仅可靠注入第一个 `ILitemall*Biz` 字段 `orderBiz`，见 merged beans dump）。该缺口同样影响从未经 `paymentCallback` 测试的 `onPaymentSuccess` RC 分支，与本 plan 解耦。生产环境（Quarkus CDI）`@Inject` 正常解析所有字段。测试 6（PaymentCallbackImpl 路由）改用 `assertThrows(NPE)` 验证 RC 路由生效（NPE 证明进入 RC 分支而非订单分支）+ 订单未被触碰（订单分支被跳过），recharge 侧行为由测试 1-5 直接经 `ILitemallRechargeBiz` 注入完整覆盖。
- **Add：** `docs/design/wallet-and-assets.md` changeType×sourceType 表 `退款返还` 由 `refund(预留)` 更新为 `recharge-refund(已落地)`；充值章节补「充值退款对账」小节（触发场景/回冲语义/幂等/降级/与正向入账对称）。
- **验证：** `mvn test`（app-mall-service -am）422 全绿；`mvn clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` 全模块 BUILD SUCCESS。

---

## 用户分析/优惠券分析报表导出（nop-report successor，5 大主题域全覆盖）

**计划：** `docs/plans/2026-06-29-0119-1-user-coupon-report-export-plan.md`（两 Phase 全部完成）

### Phase 1 — 后端：exportReport 扩展 user/coupon + xpt 模板 + 测试

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`、`nop-frontend-dev`，读完 `generate-report.md` runbook（xpt 多 sheet 可行性）、`service-layer.md`、`testing.md`、`view-and-page-customization.md`。
- **Explore/Decision：** 经 `workbook.xdef` schema + nop-report-demo `06-Sheet循环.xpt.xlsx` 证实多 sheet 可行 → 选**选项 A**（`reportName=user` 单 xlsx 多 sheet：retention/rfm/lifecycle/repurchase）。
- **Add：** `LitemallOrderBizModel.exportReport` switch 增 `case "user"`（调 4 个用户分析 `getXxx`，经 `buildUserDataSet` 适配多数据集）+ `case "coupon"`（调 `getCouponAnalysis`，`buildCouponDataSet` 单行 List）；`normalizeReportName`/`reportTemplate` 同步增 user/coupon。
- **Add：** `user-analysis.xpt.xml`（4 sheet，表头 Latin，含 cohortSize/d1/d7/d30 + 比率、userCount、paidUsers/repurchaseUsers 绝对计数）+ `coupon-analysis.xpt.xml`（单 sheet claimedCount/usedCount/pulledGmv）。
- **Latin 段名适配（残留风险处理）：** 用户分析数据含中文段名（lifecycle 新客/活跃/沉睡/流失、RFM 8 段），PDFBox Helvetica 仅 Latin → 导出适配层 `latinRfmSegment`/`latinLifecycleSegment` 映射为 Latin 代码（vip-value/general-value/new/active 等），原 `getXxx` 口径不变（Non-Goal 守住）。
- **Proof：** `TestLitemallOrderStatisticsBizModel` 6 新测试（user/coupon × xlsx/pdf + 空集 × 2），全 31 测试绿；`mvn test -pl app-mall-service -am` 416 全绿。

### Phase 2 — 前端导出入口 + owner doc + 收口

- **Add：** `stat-user.page.yaml` 增 xlsx/pdf 模板导出入口（单一 `reportName=user`，对齐 stat-funnel/product）；`stat-order.page.yaml` 优惠券分析区增 `reportName=coupon` 导出按钮 + 格式选择。
- **Fix：** `stat-user.page.yaml` 既有的 `${data | pick: segment,userCount}` 等未加引号表达式经 SnakeYAML 严格解析报错（含逗号/冒号/管道，pre-existing，此前无页面加载测试覆盖未暴露），按 stat-order 先例 `'${...}'` 补引号（语义不变）。
- **Proof：** `TestReportExportPageLoad` 增 `testStatUserPageLoads`，5 测试绿；`mvn -pl app-mall-web test` 7 全绿。
- **Add：** `docs/design/system-configuration.md:388` 导出口径更新：5 大主题域全部已交付（含用户分析多 sheet + Latin 段名适配说明）。
- **验证：** `mvn test`（全工作区）SUCCESS；`mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` 全模块 SUCCESS。

### 关键发现 / 经验

- **nop-report 多 sheet：** `.xpt.xml` 的 `<sheets>` 下可含多个 `<sheet>`（与 `.xpt.xlsx` 同构），`IReportEngine.getRenderer` 作用于整个 workbook，一次渲染多 sheet xlsx/pdf。
- **PDF Latin 限制波及数据：** 模板表头用 Latin 不够——数据值若含中文（段名），PDF 同样失败。导出适配层把会含中文的数据值映射为 Latin 代码，是 PDF Latin 字体约束下的统一兜底（部署 CJK TTF 为 successor 触发条件）。

---

## nop-report 引擎引入 + 模板化报表导出（Phase 13 L381/L383 + 审计 01-2 P1 remediate）

**计划：** `docs/plans/2026-06-28-2352-1-nop-report-engine-introduction-plan.md`（三 Phase 全部完成）

### Phase 1 — 引入 nop-report 依赖 + 商品导出先例

- **Skill loading：** 加载 `nop-backend-dev`、`nop-testing`，读完 `generate-report.md` runbook 等。
- **Decision R1：** 引入 `nop-report-core` + `nop-report-pdf`（compile 域），不引入 service/web/dao/docx。
- **Decision R2（执行偏差）：** 模板用 `.xpt.xml`（XML 序列化）而非二进制 `.xpt.xlsx`——`XptModelLoader` 对两者同等接受，均经 `XptModelInitializer` 解析 `*=^ds!field` 文本简写；手写二进制 xlsx 不切实际。模板表头用 Latin 字段名（与现有 CSV 列对齐）。
- **Add：** `app-mall-service/pom.xml` + nop-report 依赖；附带 `app-mall-web/pom.xml` 将 `nop-ooxml-xlsx` 由 test 提升为 compile（nop-report-core 的 core 初始化 `XlsxDslModelLoaderFactory` 在 app-mall-web precompile 需 `IExcelWorkbookGenerator`/`XlsxObjectLoader`）。
- **Add：** `LitemallGoodsBizModel.exportGoodsReport(@BizQuery @Auth(admin))` 注入 `IReportEngine`，渲染 `/nop/main/report/goods-export.xpt.xml`，返回 `WebContentBean`。保留 `exportGoods` CSV 不删除。
- **Add：** `ReportRenderTypes` 校验（仅 xlsx/pdf）+ `AppMallErrors.ERR_REPORT_RENDER_TYPE_INVALID/_NAME_INVALID`。
- **Proof：** `TestLitemallGoodsOpsWorkbench` 4 新测试（xlsx/pdf/空集/非法 renderType）+ 全 19 测试绿。
- **验证：** `mvn clean install -DskipTests -Dquarkus.package.type=uber-jar` 全绿；`mvn test -pl app-mall-service -am` 401 测试全绿。

### Phase 2 — 报表导出（P19 successor）

- **Add：** `sales-funnel/product-analysis/order-analysis.xpt.xml` 3 模板。
- **Add：** `LitemallOrderBizModel.exportReport(@BizQuery @Auth(admin))`：reportName(funnel|product|order) × renderType(xlsx|pdf)，复用 `getSalesFunnel`/`getProductAnalysis`/`getOrderAnalysis`，Bean→map 适配（如 `GoodsStatisticsBean.salesCount`→`number`）。
- **Proof：** `TestLitemallOrderStatisticsBizModel` 9 新测试（3 reportName × xlsx/pdf + 空集 + 非法入参），全 25 测试绿；`mvn test -pl app-mall-service -am` 410 全绿。

### Phase 3 — 前端导出入口 + owner doc + 收口

- **Add：** `goods-io.page.yaml` 增 xlsx/pdf 模板化导出（格式选择），CSV 路径保留；`stat-funnel/product/order.page.yaml` 增 `LitemallOrder__exportReport` xlsx/pdf 按钮。
- **Fix：** `stat-order.page.yaml` 三处 `${... | pick: ...}` 表达式未加引号导致严格 YAML 解析失败（pre-existing），加引号修复（语义不变）。
- **Add：** `TestReportExportPageLoad`（4 page 经 `PageProvider.getPage` 加载校验）。
- **Add：** `docs/design/system-configuration.md` 导出口径更新（3 处 successor 标注改为「已引入」）。
- **验证：** `mvn -pl app-mall-web test` 6 测试绿。

### 关键发现 / 经验

- **nop-report PDF 中文：** PDFBox 内置 Helvetica 仅 Latin；CJK 需在 `_vfs/fonts/` 提供 CJK TTF。本基线模板用 Latin 字段名（与 CSV 列对齐），xlsx 原生支持中文；PDF 中文为部署期字体关注，已在 owner doc 注记。
- **app-mall-web precompile 依赖：** 一旦 app-mall-service 引入 nop-report-core（compile），其 core 初始化在 app-mall-web precompile 触发 `XlsxDslModelLoaderFactory` 装载，需 nop-excel/nop-ooxml-xlsx 在 compile 域。
