# 2026-06-19 Dev Log

## E2E 测试加固计划 Phase 3+4 完成（admin RPC 冒烟 + 浏览器 UI 交互，全量 107 passed）

执行计划 `docs/plans/2026-06-18-2000-e2e-test-hardening-plan.md` Phase 3+4，e2e 套件由 44 → **107 passed / 0 failed**。

### Phase 3 — admin 后台 RPC 冒烟

- Explore（2026-06-19）：curl 验证 nop/123 凭证在 `%dev` profile 下对全部 31 个 admin 实体的 findPage RBAC 权限——全部返回 `status:0`，无权限限制。记录：31 个实体全部可访问，无需区分清单。
- Add：新建 `e2e/tests/admin-pages.spec.ts`，含 62 个测试：(a) 31 admin 实体 findPage RPC 冒烟（断言 `status:0` + `items` 数组），(b) 31 admin 页面 main.page.yaml 渲染冒烟（`POST /r/PageProvider__getPage` 断言 `type:page` + `body` 非空）。注意：admin 页面参数用 `bizObjName` 而非 `pageId`。
- Add：更新 `docs/references/e2e-testing-guide.md` 测试文件表，补全 storefront-happy-path 与 admin-pages 条目。
- 注意：main.page.yaml 渲染冒烟的 `PageProvider__getPage` 入参格式为 `{pagePath: 'main/', bizObjName: 'LitemallGoods'}`，非 `pageId` 参数。

### Phase 4 — 浏览器 UI 交互

- Explore（2026-06-19）：Playwright 浏览器渲染探测确认 "NOP Chaos Console" SPA 渲染（`page.goto('/')` → 200 + SPA shell），`page.goto('/#/storefront-home')` → AMIS .cxd-Page 可定位，`page.goto('/#/LitemallGoods')` → admin AMIS 页可渲染。结论：SPA 前端内置于 jar（`nop-web-site`），可浏览器渲染。
- Decision：走"可渲染"分支，范围选 admin UI（admin main.page.yaml 页面结构稳定，API token 注入方式可靠）。Storefront UI 交互流（支付/购物车）留作后续。
- Add：新建 `e2e/tests/admin-ui.spec.ts`，含 1 条真实 page 交互流：API 登录取 token → `page.evaluate` 注入 localStorage → `page.goto('/#/LitemallGoods')` SPA 导航 → waitForTimeout + 断言 body 非空 + `.cxd-Grid` AMIS 组件存在 + `#root` 已附加。

### 验证

- `cd e2e && npx playwright test` = **107 passed / 0 failed**（原 44 + Phase 3 62 + Phase 4 1），19.0s
- 构建 jar 前 kill 残留 java 进程，无端口/文件锁冲突

### 涉及文件清单

- `e2e/tests/admin-pages.spec.ts`（new, 62 tests）
- `e2e/tests/admin-ui.spec.ts`（new, 1 test）
- `docs/references/e2e-testing-guide.md`（modified, 测试文件表更新）
- `docs/plans/2026-06-18-2000-e2e-test-hardening-plan.md`（modified, Phase 3/4 完成状态 + Closure Gates 勾选）
- `docs/logs/2026/06-19.md`（本日志）

## 测试加固计划 Phase 2-4 完成（e2e 业务流程串联，全量 44 passed）

执行计划 `docs/plans/2026-06-19-2100-unit-gap-and-e2e-flow-hardening-plan.md` Phase 2-4，e2e 套件由 39 → **44 passed / 0 failed**。

### Phase 2 — 订单履约后半段（ship→confirm）

- Explore RBAC：启动 e2e 应用 + curl 验证，nop/123 凭证可调 admin 操作（ship 返回参数错误非权限拒绝）。orderId 为 Integer 类型。
- 新建 `e2e/tests/order-fulfillment.spec.ts`：submit→pay(201)→ship(301)→confirm(401)。
- 构建 jar 时发现并 kill 残留 Java 进程（PID 11540，之前 e2e 未关闭的 quarkus 应用，持有 jar 锁致 FastJar 构建失败）。

### Phase 3 — 售后与退款

- 新建 `e2e/tests/aftersale-refund.spec.ts`：(1) apply→batchApprove→refund，未发货退款后订单→203；(2) cancel CREATED 订单 101→102。
- Protected Area：只测不改业务代码。

### Phase 4 — 营销（优惠券 + 拼团）

- 新建 `e2e/tests/marketing.spec.ts`：(1) coupon save→claimCoupon→submit(couponUserId)，couponPrice>0；(2) groupon rules save→publishRules→openGroupon，status=1。
- 拼团参团 joinGroupon 移入 Deferred（需第二用户身份，e2e 仅 nop 单用户）。

### 验证

- 全量 e2e `npx playwright test` = **44 passed / 0 failed**（原 39 + order-fulfillment 1 + aftersale-refund 2 + marketing 2）。
- 注：webServer 日志的 `MallJobInvoker` NPE 是 e2e 环境定时任务 IoC 注入问题（orderBiz 等为 null），非本计划引入，不影响测试通过。

## 测试加固计划 Phase 1 完成（单元测试缺口补齐）+ 发现 clean baseline 预存在回归

执行计划 `docs/plans/2026-06-19-2100-unit-gap-and-e2e-flow-hardening-plan.md` Phase 1。

### 新增单元测试（3 个，全过）

- 新建 `TestLitemallOrderGoodsBizModel`：补 `expireCommentWindow` 的 `IGraphQLEngine` 业务逻辑层测试（与已有 `TestLitemallSchedulerJobInvoker` 的 mock 调度委托 wiring 测试互补）。过期/未过期两路径。
- `TestLitemallGrouponBizModel` 追加 `testPublishRulesGoodsNotOnSale`：商品下架时 `publishRules` 应失败（`ERR_GROUPON_RULES_GOODS_NOT_ON_SALE`）。
- 实施修正：`OrderGoods.productId` 非空需补建 `GoodsProduct`；`picUrl` NOT NULL 且 image domain 把空串转 null 须用真实 URL；`updateEntity` 须包 `ormTemplate.runInSession`。
- Explore 结论：库存并发超卖已覆盖于 `TestConcurrencyGuards.testReduceStockAtomicGuard`（SKU=1，两线程并发，断言恰好 1 成功），不补。

### 重要发现：clean baseline 预存在回归（非本计划引入）

- `./mvnw test -pl app-mall-service -am` 全量实跑，发现除已知 auth 10 失败外，`TestLitemallAftersaleBizModel`(5) + `TestLitemallCommentBizModel`(6) 也失败。
- `git stash` 验证 HEAD `f98d1ee` clean baseline：同样失败 → confirmed 预存在回归，与本计划无关。
- 根因：image domain picUrl 回归。`OrmFileComponent.copyFrom` 在 `submit` 时从 cart 复制 picUrl 到 orderGoods，因 `nop_file_record` 缺记录导致 null（违反 NOT NULL）。与 e2e 计划 06-19 的 image domain 约束发现同源。
- `docs/testing/known-good-baselines.md` 此前"核心 36 全过"记录已过时（平台 `DataInitInitializer` 改动后失效），已更新。
- 处置：21 个预存在失败（auth 10 + aftersale 5 + comment 6）路由到独立修复计划（见计划 Deferred But Adjudicated）。e2e storefront happy-path 不受影响（39 passed，种子含 `nop_file_record.csv`）。

### 验证

- 新增 3 测试单独跑：全过。
- 本计划零新失败（clean baseline 验证）。

## E2E 测试加固计划 Phase 1+2 完成（init-data 基建 + happy-path 修复，全量 39/39 通过）

执行计划 `docs/plans/2026-06-18-2000-e2e-test-hardening-plan.md` Phase 1+2，e2e 套件由 38 passed + 1 failed（happy-path 空库）转为 **39 passed / 0 failed**。

### Phase 1 — init-data 基建

- 种子数据最终位置：**项目根 `_vfs/_init-data/`**（7 张 CSV：6 张核心表 + `nop_file_record.csv`），非原计划的 `app-mall-web/src/main/resources/_vfs/_init-data/`
- 关键认知（用户提示）：Nop VFS 默认 `nop.core.resource.dir-override-vfs=./_vfs`（`CoreConfigs.java:213-214`、`ResourceHelper.getOverrideVFsDir:1112-1124`），运行目录 `_vfs/` 自动合并进 VFS 命名空间，**无需打 jar**，CSV 修改后重启即生效
- 已通过 git mv 把 6 张 CSV 从 `app-mall-web/src/main/resources/_vfs/_init-data/` 迁到项目根 `_vfs/_init-data/`
- `e2e/playwright.config.ts:26` webServer.command 追加 `-Dnop.orm.init-database-data=true`
- 实测 Proof：应用启动日志含 `load-csv-data` 全部 7 张表，`curl LitemallGoods__findPage` 返回 5 条，`LitemallBrand__findPage` 返回 3 条

### Phase 1 实施中触发的平台 bug 修复（nop-entropy）

`DataInitInitializer` 在 IoC 启动早期通过 `ioc:after` 链式触发（`BeanContainerImpl.getBean0:411-415` 遍历 `getNextBeans()`），`@PostConstruct init()` 调用时 `OrmTemplateImpl.sessionFactory` 字段尚未注入完成（IoC 循环依赖暴露半成品），`ormTemplate.runInSession(...)` 触发 `OrmSessionRegistry.get(null)` NPE。

修 `nop-entropy`：
- `nop-persistence/nop-orm/.../DataInitInitializer.java`：移除 `@PostConstruct` 注解（`init()` 方法保留）
- `nop-persistence/nop-orm/.../beans/orm-defaults.beans.xml`：`DataInitInitializer` bean 添加 `ioc:delay-method="init"`，`init()` 推迟到 IoC 完全启动后执行（`BeanContainerImpl.runDelayMethod:591-601`），循环依赖消失
- `mvn install -pl nop-persistence/nop-orm -am` 后重打 `app-mall-app` jar 验证

详见 `nop-entropy/ai-dev/logs/2026/06-19.md` 的对应条目（平台变更日志归 `nop-entropy/ai-dev/logs/`）。

### Phase 1 实施中发现的 image domain 种子约束

- `domain="image"` 字段在 `OrmFileComponent.copyFrom`（如 `LitemallCartBizModel.addGoods:84-88` cart 从 goods 复制 picUrl）路径下，要求源字段值是 `/f/download/{fileId}` 格式且 `nop_file_record` 表存在对应 `fileId` 记录
- `DaoResourceFileStore.attachFile:342-350` 还校验 `BIZ_OBJ_NAME` 匹配
- 处理：goods 的 PIC_URL/GALLERY 改为 `/f/download/goods-{id}` 格式 + 新增 `nop_file_record.csv` 种子（6 条，`BIZ_OBJ_NAME=LitemallGoods`）；goods_product.URL 改为空（非 mandatory），addGoods 走 else 分支用 goods.picUrl
- brand/category 的 PIC_URL 保持字面 URL（仅展示用，不会被 copyFrom），不影响加载

### Phase 2 — happy-path 修复

修 `e2e/tests/storefront-happy-path.spec.ts` 两处测试代码 bug：
- `LitemallGoodsProduct__findList` 的 filter `{eq: ['goodsId', goodsId]}` 非法 → Nop 标准 TreeBean 格式 `{$type: 'eq', name, value}`（错误码 `nop.err.core.filter.op-is-null`）
- `findList` 返回 `data` 是数组（非 `{items: [...]}` 包装），`data?.items?.[0]` 永远 undefined → 改为 `data?.[0]`

### 验证

- `cd e2e && npx playwright test` → **39 passed / 0 failed**（含 happy-path）
- 应用启动 14s 内成功，无 NPE，种子数据全部入库
- 本地未跑 mvn 全量 test（仅 e2e 全量），核心 BizModel 单元测试（35 个测试类，~98% 自定义方法覆盖）状态保持

### Phase 1 残留 follow-up

- `addTime`/`updateTime` 被框架 auto-fill 覆盖为当前时间（非 2018 原值）：归因为实体 `createTimeProp="addTime"` 配置使 ORM 在 `saveEntity` 时强制刷新；非阻塞 happy-path（测试不依赖时间戳），但种子数据语义失真。Phase 1 末 Proof 项已标 `[ ]` 记录该偏差

### 涉及文件清单（本项目）

- `e2e/playwright.config.ts`（modified）
- `e2e/tests/storefront-happy-path.spec.ts`（modified，filter 格式 + findList 返回结构）
- `_vfs/_init-data/litemall_brand.csv`（renamed from `app-mall-web/src/main/resources/_vfs/_init-data/`，内容未改）
- `_vfs/_init-data/litemall_category.csv`（renamed）
- `_vfs/_init-data/litemall_goods.csv`（renamed + PIC_URL/GALLERY 改为 `/f/download/...`）
- `_vfs/_init-data/litemall_goods_attribute.csv`（renamed）
- `_vfs/_init-data/litemall_goods_product.csv`（renamed + URL 改为空）
- `_vfs/_init-data/litemall_goods_specification.csv`（renamed）
- `_vfs/_init-data/nop_file_record.csv`（new）
- `docs/plans/2026-06-18-2000-e2e-test-hardening-plan.md`（checklist 勾选 + 新增 4 条 Decision/Fix）
- `docs/logs/2026/06-19.md`（本日志）

### 涉及文件清单（nop-entropy，独立 commit）

- `nop-persistence/nop-orm/src/main/java/io/nop/orm/initialize/DataInitInitializer.java`（移除 `@PostConstruct`）
- `nop-persistence/nop-orm/src/main/resources/_vfs/nop/orm/beans/orm-defaults.beans.xml`（`DataInitInitializer` 添加 `ioc:delay-method="init"`）
- `docs-for-ai/02-core-guides/orm-model-design.md`（补充 workDir/_vfs 不打 jar 机制 + image domain 种子约束）
- `ai-dev/logs/2026/06-19.md`（追加 DataInitInitializer delay-method 修复条目）
