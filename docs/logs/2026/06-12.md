# Development Log - 2026-06-12

### 2026-06-12

- Continued `docs/plans/2026-06-09-phase1-user-registration-login-plan.md`
- **Phase 1A fix:** Fixed `gender` value from 0 to 1 in `LoginApiExBizModel.signUp()` — platform dict `auth/gender` only accepts 1 (male) and 2 (female), not 0.
- **Phase 1A fix:** Added `loginType: 1` to login test requests in `TestLoginApiSignUp` — platform `LoginServiceImpl.isValidLoginMethod()` requires `loginType=LOGIN_TYPE_USERNAME_PASSWORD(1)`, default 0 is rejected as invalid.
- **Phase 1A result:** All 7 signUp tests pass (signUp success, duplicate username, empty username/password/mobile, login after signUp, disabled user cannot login).
- **Pre-existing failures:** Goods (1), Cart (7), Order (4), Aftersale (2) — unrelated to Phase 1 changes (missing NOP_FILE_RECORD table etc.).
- **Phase 1B:** Implemented `getMyProfile` and `updateMyProfile` in `NopAuthUserExBizModel` (extends `NopAuthUserBizModel`), declared in `INopAuthUserBiz` interface. Used `@Optional` annotation for update params.
- **Phase 1B:** Verified `changeSelfPassword` (platform inherited method) works — uses `IUserContext` + `UserContextImpl` for test setup.
- **Phase 1B result:** All 4 profile tests pass (getMyProfile, updateMyProfile, changeSelfPassword success, changeSelfPassword wrong old password).
- **Total:** 11 tests pass (7 signUp + 4 profile).
- **Next:** Phase 1C — backend user management page customization + permission config.

### 2026-06-12 (afternoon) — Plan 06-12 Execution

- Executed `docs/plans/2026-06-12-next-execution-slice-plan.md` (Phase 1C/1D + Phase 2 + Phase 3 + Phase 6)
- **Phase 1C:** Customized NopAuthUser Delta view (`_delta/default/nop/auth/pages/NopAuthUser/NopAuthUser.view.xml`) — grid (8 cols: userName/nickName/phone/gender/userType/status/createTime/updateTime), edit form (8 fields), view form (11 fields). Hidden enterprise fields via bounded-merge.
- **Phase 2A:** Added `getCategoryTree()` (public) and `getCategoryList()` to `ILitemallCategoryBiz` + `LitemallCategoryBizModel`. Delete protection with `ERR_CATEGORY_HAS_CHILDREN` and `ERR_CATEGORY_HAS_PRODUCTS`.
- **Phase 2B:** Brand view uses default CrudBizModel, no custom backend needed.
- **Phase 2C:** Added `onSale()`, `offSale()`, `frontList()` (public), `frontDetail()` (public) to `ILitemallGoodsBiz` + `LitemallGoodsBizModel`. Error codes: `ERR_GOODS_NO_SKU`, `ERR_GOODS_NOT_ON_SALE`, `ERR_GOODS_NOT_FOUND`.
- **Phase 3A:** Full address CRUD with user isolation in `ILitemallAddressBiz` + `LitemallAddressBizModel`. Default address management, 20-address limit. Error codes: `ERR_ADDRESS_LIMIT_EXCEEDED`, `ERR_ADDRESS_NOT_FOUND`, `ERR_ADDRESS_NOT_OWNER`.
- **Phase 3B:** Added `getRegionTree()` (public) to `ILitemallRegionBiz` + `LitemallRegionBizModel`.
- **Phase 6:** Added `search()` (public) and `adminSearch()` to `ILitemallGoodsBiz` + `LitemallGoodsBizModel`. Added `getHotKeywords()` (public) and `getDefaultKeywords()` (public) to `ILitemallKeywordBiz` + `LitemallKeywordBizModel`.
- **Verification:** `./mvnw.cmd compile -DskipTests` — BUILD SUCCESS. `./mvnw.cmd test` — 28 tests, 14 errors (all pre-existing NOP_FILE_RECORD), 0 new failures.
- **Roadmap:** Phase 1/2/3/6 updated to `done`. LitemallSearchHistory moved to Phase 7.
- **Plan status:** completed

### 2026-06-12 (evening) — Phase 7/8/10 Execution

- Executed `docs/plans/2026-06-12-phase-7-10-interactive-coupon-content-plan.md`
- **Phase 7A (收藏):** Added `addCollect`, `removeCollect`, `isCollect`, `listByType` to `ILitemallCollectBiz` + `LitemallCollectBizModel`. Error codes: `ERR_COLLECT_ALREADY_EXISTS`, `ERR_COLLECT_NOT_FOUND`. Customized `LitemallCollect.view.xml` grid with query form.
- **Phase 7B (足迹):** Added `recordFootprint` (same goods same day dedup), `listFootprints`, `clearFootprints` to `ILitemallFootprintBiz` + `LitemallFootprintBizModel`.
- **Phase 7C (评论):** Added `submitComment` (order status/ownership/uniqueness validation), `commentList` (public), `myComments`, `adminReply` to `ILitemallCommentBiz` + `LitemallCommentBizModel`. Error codes: `ERR_COMMENT_ORDER_NOT_RECEIVED`, `ERR_COMMENT_ALREADY_EXISTS`, `ERR_COMMENT_EXPIRED`, `ERR_COMMENT_NOT_OWNER`, `ERR_COMMENT_ORDER_GOODS_NOT_FOUND`. Customized `LitemallComment.view.xml` with admin reply button.
- **Phase 7D (搜索历史):** Added `recordSearch` (same keyword same day dedup), `listSearchHistory`, `clearSearchHistory` to `ILitemallSearchHistoryBiz` + `LitemallSearchHistoryBizModel`.
- **Phase 8A (优惠券规则):** Added `publishCoupon`, `unpublishCoupon`, `listAvailableCoupons` (public), `listMyCoupons` to `ILitemallCouponBiz` + `LitemallCouponBizModel`. Customized `LitemallCoupon.view.xml` with publish/unpublish buttons.
- **Phase 8B (优惠券核销):** Added `claimCoupon`, `redeemCoupon`, `selectCouponForOrder`, `useCoupon`, `returnCoupon` to `ILitemallCouponUserBiz` + `LitemallCouponUserBizModel`. Error codes: `ERR_COUPON_NOT_FOUND`, `ERR_COUPON_NOT_AVAILABLE`, `ERR_COUPON_LIMIT_EXCEEDED`, `ERR_COUPON_CODE_INVALID`, `ERR_COUPON_USER_NOT_FOUND`, `ERR_COUPON_NOT_USABLE`, `ERR_COUPON_MIN_NOT_MET`, `ERR_COUPON_GOODS_NOT_MATCH`.
- **Phase 8C (优惠券与订单集成):** Modified `ILitemallOrderBiz.submit()` to accept `@Optional couponUserId`. Modified `LitemallOrderBizModel.submit()` to call `selectCouponForOrder` + `useCoupon`. Modified `cancel()` to query CouponUser by orderId and call `returnCoupon`. Fixed actualPrice double-subtract bug.
- **Phase 10A (专题):** Added `frontList` (public), `frontDetail` (public) to `ILitemallTopicBiz` + `LitemallTopicBizModel`. Customized `LitemallTopic.view.xml`.
- **Phase 10B (广告):** Added `listActiveAds` (public, time window filter) to `ILitemallAdBiz` + `LitemallAdBizModel`. Customized `LitemallAd.view.xml`.
- **Phase 10C (FAQ/反馈):** Added `listIssues` (public) to `ILitemallIssueBiz` + `LitemallIssueBizModel`. Added `submitFeedback` to `ILitemallFeedbackBiz` + `LitemallFeedbackBizModel`. Customized `LitemallIssue.view.xml` and `LitemallFeedback.view.xml`.
- **Verification:** `./mvnw.cmd clean compile -DskipTests` — BUILD SUCCESS.
- **Roadmap:** Phase 7/8/10 updated to `done`.
- **Plan status:** completed
