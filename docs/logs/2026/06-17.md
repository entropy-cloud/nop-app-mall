# Development Log - 2026-06-17

### 2026-06-18（Plan A + Plan B 实施完成 — Phase 11/13 补全 + 测试加固/运营优化）

- **Plan A（Phase 11/13 补全）**：
  - Phase 1（nop-job 调度装配）：`app-mall-app/pom.xml` 引入 `nop-job-local:2.0.0-SNAPSHOT`；`application.yaml` 配置 `nop.job.scheduler.config-path`；`_vfs/nop/job/conf/scheduler.yaml` 注册 5 个 job；`MallJobInvoker` 5 方法绑定；`app-service.beans.xml` IoC 注册
  - Phase 2（看板）：选 Decision B（AMIS chart + API），新增 `stat-dashboard.page.yaml` + 开放 `stat-manage` 菜单
  - Phase 3（验证归真）：roadmap Phase 11/13 从 partial → done
- **Plan B（测试加固 + 运营优化）**：
  - Phase 1（测试加固）：新增 `TestConcurrencyGuards`（库存+券并发 2/2 过）、`storefront-happy-path.spec.ts`；修复 `getUserStatistics` SQL 列名 `DELETED`→`DEL_FLAG`；`claimCoupon` 增 `ConcurrentHashMap` 锁消除并发竞态
  - Phase 2（运营优化）：`frontListByFlags` 首页 isHot/isNew 过滤；`getAvailableRulesById` 团购详情服务端查询；`MallNotificationService` 补 `IEmailSender` 分支
  - Phase 3（外部依赖关闭）：微信沙箱联调 + DDL 模板修复正式记录关闭
- **view.xml xdef 合规性修复**（既有回归）：
  - 5 处 `visibleOn="..."` 属性改为 `<visibleOn>...</visibleOn>` 子元素（action.xdef 规定 visibleOn 是子元素非属性）
  - `LitemallOrder.view.xml` ship-button dialog 从内联 `<body>` 改为 `<simple name="ship" form="ship">` + `<dialog page="ship"/>`（page 引用 pages 中的页面定义，非直接引用 form）
- 验证：`./mvnw clean install -DskipTests` BUILD SUCCESS（10 模块）；`./mvnw test` 120 run / 10 fail（全为预存在 auth）；`npx playwright test` 38 passed / 1 failed（happy-path 空库 Deferred）；调度器 e2e 启动日志确认 `nop.new-bean:MallJobInvoker` + 5 job 配置加载
- **Plan A + Plan B 状态：completed**

### 2026-06-17（Plan 3 治理可信度修复 — 全部 Phase 完成，标记 completed）

- Phase 1 闭合审计回补：独立 subagent ses_12ad816bfffetSKyTboTCx9Daw 对 `2026-06-13-next-phase-plan.md` 做真实 closure audit → Verdict REVISE（实现可信，闭合门 2 虚假勾选 + nop-job 未引入未记录）；按方案 B 修正：闭合门 [x]→[~]、Deferred 补 nop-job 条目、Closure 节加 Post-Closure Note
- Phase 5 测试基线：known-good-baselines 回写 Plan 2 完成后基线；TestIBizNewEntity 从纯 assertNotNull 升级为业务属性断言（orderStatus/orderSn/userId/checked 默认值）
- Phase Final + Closure Gates + Closure section 全部填写
- 验证：TestMallNotificationService 3/3 + TestIBizNewEntity 3/3 全过
- **Plan 3 状态：completed**（所有 in-scope 治理项闭环；e2e happy-path 留 Deferred）

### 2026-06-17（Plan 3 治理可信度修复实施）

- Phase 2（技能真值源统一）：删除 `.opencode/skills/nop-nodejs-backend`（NestJS+Prisma 与 Java/Nop 不匹配）；`docs/skills/README.md` 顶部增加双源声明（.opencode/skills 为权威实现技能列表，docs/skills 为 audit prompt）+ 11 个实现技能注册表；2 个计划文件 16 处技能 gate 占位符补真实文档路径
- Phase 3（context 文档刷新）：`project-context.md` E2E 命令改 `cd e2e && npx playwright test`；`codebase-map.md` 全表 Last Verified 刷至 2026-06-17 + 补 e2e/wx/delta/meta 入口；`module-boundaries.md` 测试位置改 `app-mall-service/src/test` + 补 e2e Playwright 条目 + 删除"No E2E framework configured"
- Phase 4（roadmap 归真）：Phase 11/13 从 `done` 回退为 `partial`（nop-job 调度/nop-report 看板未引入）；Current Baseline 重写反映真实基线（25 页面前台/微信支付/31 测试类/38 e2e）；Entity Coverage 补 LitemallResetCode + 改"32 活跃实体"；删除 extAction1 僵尸引用和"核心缺口"矛盾段
- Phase 1（闭合审计回补）：新增 `TestMallNotificationService`（3 测试：null sender 跳过/正常发送捕获/异常吞掉，纯 JUnit 无需 IoC 容器，0.354s 执行）
- Phase 5（测试基线）：known-good-baselines.md 补 Plan 2 完成后基线行（111 跑/101 过/clean package SUCCESS）；TestIBizNewEntity 从纯 assertNotNull 升级为业务属性断言（orderStatus/orderSn/userId/checked 默认值校验）
- 验证：`./mvnw -pl app-mall-service -Dtest=TestMallNotificationService test` → 3/3 过

### 2026-06-17（Plan 2 closure audit 修订 + 标记 completed）

- 独立子代理 closure audit（ses_12af9bf87ffei4ChVZv5JzUUQu）Round 1 Verdict REVISE：7 blockers
  - B1: claimCoupon/claimCouponForUser 残留 `coupon.setTotal(total-1)` 递减代码（plan 声称已删但实际未删 → 下发不足 bug）
  - B2: Phase 2/3 line items [ ] 但 Exit Criteria [x]
  - B3: Phase 4 重复块（同项 [x] + [ ] 并存）
  - B4: visibleOn 仍在 tpl HTML 字符串内（AMIS 不解析为指令）
  - B5/B6/B7: Phase Final + Closure Gates + Closure section 全空
- 修订: 删除两处 total 递减残留; 删除 Phase 4 重复块; visibleOn 提升为组件属性; 勾选 Phase 2/3 line items; 填写 Phase Final + Closure Gates + Closure; marketing doc 团购状态措辞对齐
- 验证: 28 核心测试全过（B1 修复无回归）
- **Plan 2 状态: completed**（closure audit 修订后闭合）

### 2026-06-17（Plan 2 Phase 5 残余修复 + Phase Final owner docs 对齐）

- 08-6 售后退款按钮 batch 矛盾：移除 `LitemallAftersale.view.xml` refund-button 的 `batch="true"`（refund 方法签名是单条 `refund(@Name("id") String id)`，batch=true 会传 `$ids` 导致参数不匹配）
- owner doc 价格公式对齐：`order-and-cart.md` 价格语义段补计算公式（`goodsPrice = Σ product.price × number`、`orderPrice = goodsPrice + freightPrice - couponPrice`、`actualPrice = orderPrice - integralPrice - grouponPrice`），与 ORM `:1000` 注释和 `LitemallOrderBizModel.submit()` 实现三方一致
- 验证：`./mvnw -pl app-mall-web -DskipTests compile` → BUILD SUCCESS
- **Plan 2 当前状态**：Phase 1/2/3/4 Exit Criteria 全达标；Phase 5 仅 e2e 冒烟留后续；Phase Final owner docs 已对齐，独立 closure audit 待执行

### 2026-06-17（Plan 2 ORM 批准后实施：Phase 1 并发治理 + Phase 3 注释 + Phase 4 字典）

- 人工批准 ORM 变更后，执行 `docs/plans/2026-06-16-1800-p1-domain-logic-hardening-plan.md` 剩余 ORM 相关子项
- Phase 3（ORM 注释）：`model/app-mall.orm.xml:1000` ACTUAL_PRICE comment 从 `order_price - integral_price` 改为 `order_price - integral_price - groupon_price`，与代码中 `actualPrice = orderPrice - integralPrice - grouponPrice` 一致
- Phase 4（团购状态字典）：新增 `mall/groupon-status` 字典（0/1/2/3 = 开团未支付/开团中/开团失败/开团成功）；`model/app-mall.orm.xml:789` LitemallGroupon STATUS 列从无字典引用改为 `ext:dict="mall/groupon-status"`；displayName 和 comment 同步更新
- Phase 1（并发治理 Decision B：原子条件 UPDATE，不涉模型迁移）：
  - 新增 `LitemallCouponUserMapper.updateStatusIfUnused`（sql-lib EQL `where id=? and status=0`）+ `LitemallOrderGoodsMapper.updateCommentFlagIfUnused`（`where id=? and comment=0`）
  - `LitemallCouponUserBizModel.useCoupon` 改为原子条件 UPDATE，影响行数=0 抛异常（消除 09-08 并发重复核销）
  - `LitemallCommentBizModel.submitComment` 的 orderGoods.comment 改为原子条件 UPDATE（消除 11-10 评论并发重复提交）
  - CouponUser 总量校验改 `total!=null` 且 count<total（修复 11-3 total 翻转 bug）
- IoC 注册：新增 mapper 必须在 `app-dao.beans.xml`（保留层）手动注册 `<bean>` + `SqlLibProxyFactoryBean`（生成层 `_dao.beans.xml` 只自动处理 ORM 模型中有 `tagSet="mapper"` 的实体）
- 验证：`./mvnw clean package -DskipTests` → 全 10 模块 BUILD SUCCESS；`./mvnw -pl app-mall-service test` → 111 跑 / 101 过 / 10 失败（10 个为预存在 auth 失败：TestLoginApiSignUp ×4 + TestNopAuthUserProfile ×4 + TestPasswordReset ×2，错误"非法的字典项:0"，Plan 1 已记录不变）
- Skill：`nop-backend-dev`+`nop-orm-modeler`+`nop-testing`；每方法 selfcheck 19 项通过

### 2026-06-17（Plan 2 电商域逻辑加固计划持续实施，完成越权/价格校验/前台对齐第一批）

- 执行 `docs/plans/2026-06-16-1800-p1-domain-logic-hardening-plan.md` 的非 ORM 阻断子项，当前计划状态改为 `in progress`
- 后端域逻辑：
  - `LitemallOrderBizModel`：`submit()` 增地址归属校验；下单商品快照改用当前 `product.price`；优惠券范围校验改传 `goodsId + categoryId`；团购下单校验 `rules.goodsId` 必须出现在本次勾选商品；`cancel/prepay/pay/confirm` 增本人订单守卫；`ship` 增 `@Auth(roles="admin")`
  - `LitemallCouponUserBizModel`：限量券不再递减 `coupon.total`，改按已领张数与 `total` 比较；受限券在 `goodsIds` 为空时直接拒绝，避免类目/指定商品券越界使用
  - `LitemallGrouponBizModel`：`openGroupon/joinGroupon` 校验订单必须包含团购规则绑定商品；`joinGroupon` 满员时把同团记录置 `status=3`；团购超时退款守卫收紧为 `orderStatus==PAY && aftersaleStatus==INIT`，并统一 `outRefundNo=refund_{orderSn}`
- 前端/后台页面：
  - storefront：结算页优惠券下拉改 `labelTpl`；订单列表/详情/结果页金额统一展示 `actualPrice`；订单状态补齐 `103/202/203/204`；团购活动详情补 `status=3=开团成功`
  - aftersale：申请页补 `type=1 已收货无需退货`、`type=2 退货退款`；列表页类型映射同步，并修正订单详情跳转参数名
  - admin：订单页补发货按钮；商品页补上架/下架按钮；团购规则页补发布/下架按钮；团购活动页补查看行按钮；营销菜单、反馈/FAQ 菜单恢复可达
- owner doc：`marketing-and-promotions.md` 已同步团购成功状态当前实现语义（0/1/2/3）
- 测试与验证：
  - `./mvnw -pl app-mall-service -Dtest=TestLitemallOrderBizModel,TestLitemallCouponUserBizModel,TestLitemallGrouponBizModel,TestLitemallCommentBizModel,TestLitemallCollectBizModel,TestLitemallFootprintBizModel test` → 37/37 通过，BUILD SUCCESS
  - `./mvnw -pl app-mall-web -DskipTests compile` → BUILD SUCCESS
  - 团购测试为适配新“订单必须含目标商品”守卫，补造真实 `LitemallOrder`/`LitemallOrderGoods` 测试数据，并补 `NopFileRecord` 夹具满足 `picUrl` 文件组件约束

### 2026-06-17（设计文档与代码一致性修复计划实施完成）

- 执行 `docs/plans/2026-06-17-1700-design-code-consistency-fix-plan.md`，修复审计报告 4 项发现，全量 111 测试零回归
- Phase 1（文档对齐）：
  - F-doc-code-1 `order-and-cart.md` 售后范围/流转规则——售后资格从"只有已收货"改为"已支付未发货(201)或已收货(401/402)+售后INIT"，对齐 `LitemallAftersaleBizModel:194-196` + aftersale-type 字典 GOODS_MISS
  - F-name-1 `order-and-cart.md` 迁移规则+text-art——消除"已申请退款"订单状态措辞，归到售后状态机 REQUEST
  - F-doc-code-2 `marketing-and-promotions.md` 团购——"开团成功"澄清为参团人数计算值，非持久 status 枚举（0/1/2）
  - ripple `flow-overview.md` 售后 mermaid 触发边改"订单已支付或完成收货"
- Phase 2（代码修复）：
  - F-doc-code-3 `LitemallOrderBizModel.deleteOrder:478-483` 删除白名单追加 GROUPON_EXPIRED(204)，使 204 与 203 已退款一致可删。human 确认见 `docs/discussions/2026-06-17-204-deletable-confirmation.md`
  - 新增 `testDeleteGrouponExpiredOrder`：正向(204 可删 + deleted=true + orderStatus 保持 204) + 反向(PAY=201/SHIP=301 被拒)。踩坑：直接 DAO `updateEntity` 需活跃 OrmSession，改用 `ormTemplate.runInSession(...)`（参考 TestLitemallCommentBizModel:250）
- 验证：`./mvnw.cmd test -pl app-mall-service -Dtest=TestLitemallOrderBizModel` → 8/8 通过；全量 `./mvnw.cmd test -pl app-mall-service` → 111/111 通过，零回归，BUILD SUCCESS
- Skill：Phase 2 nop-backend-dev+nop-testing；selfcheck 19 项通过（仅改守卫条件，未改签名/注解/错误码）
- 计划审计：Round 1 needs-revision（Blocker B1 行为变更缺可追溯确认）→ 新增 discussion 记录 + M1/M2 采纳 → Round 2 passed

### 2026-06-17（设计文档一致性审计 + 修复本次引入回归）

- 审计报告：`docs/analysis/2026-06-17-design-doc-consistency-audit.md`。方法：状态码字典 ↔ `_AppMallDaoConstants` ↔ 设计表逐值比对 + BizModel 每个 setOrderStatus 写入点与资格守卫逐条比对
- 发现：2 处文档内在不一致（均为本次 flow-overview.md 引入）+ 3 处文档-代码不一致 + 1 处命名混淆 + 6 类已对齐项
- 已修本次回归：flow-overview.md 订单状态机 mermaid 去掉 `已支付→退款中→已退款`（与 202 保留位抉择冲突），改为 `已支付→已退款(售后退款成功,未发货)`；L3 售后资格规则拆分（原"评价与售后都以收货完成为边界"过宽，售后实际含未发货退款）
- 关键发现（待处理）：F-doc-code-1 售后申请资格——`order-and-cart.md` 说"只有已完成收货才能申请售后"，但 `LitemallAftersaleBizModel:194-196` 允许 PAY(201)‖CONFIRM(401)‖AUTO_CONFIRM(402)，aftersale-type 字典 GOODS_MISS=未收货退款 印证代码为真相，文档过度收窄
- 其他待处理：团购"开团成功"误列为持久态（实为计算值）；团购超时(204)不在删除白名单（`LitemallOrderBizModel:478-482`），文档当终态可删；迁移叙述"已申请退款"命名混淆

### 2026-06-17（docs/design 引入 FSD 手法：全局流程总览 + 跨域引用表）

- 背景：对比 `C:/projects/cardlite-product/DMS/docs/fsd`（同源 AGE 模板，但额外拆了 fsd/ 业务规格层）。结论：DMS 整体文档模式不更先进，但其"规格/结构拆两层 + 双向反引 + FSD 写作手法"对状态机密集系统更优；商城 4/5 域为 CRUD，不宜全量复制 fsd/ 目录，改为局部吸收
- 新增 `docs/design/flow-overview.md`：L1 购买主链路页面流 + L2 订单主状态机/售后状态机（mermaid）+ 状态-域-文档-页面映射矩阵 + L3 跨域公共规则（资格前置/价格库存实时性/快照语义/恢复语义/团购透传/支付模式分流）。纯业务基线视图，不掺实现说明
- 给 5 个域文档补"跨域引用表"结构化小节（替换/增强原散文式 cross-ref）：`order-and-cart.md`（枢纽域，9 个交接点）、`product-catalog.md`、`marketing-and-promotions.md`、`user-and-address.md`、`system-configuration.md`
- 更新 `docs/design/README.md`：flow-overview.md 纳入起始文档与详细文档表
- 关键 Decision：保留单层 design/ 结构（不过度设计），只注入 FSD 的"全局总览图 + 矩阵 + 跨域引用表"三手法
- 清理债处理：`order-and-cart.md` 迁移规则段原有带日期实现说明块（含代码路径名 `LitemallAftersaleBizModel.apply/refund`、"202 当前无写入点"实施状态），改写为纯设计抉择说明（退款为何由 aftersaleStatus 表达 + 两条迁移约定 + 202 保留位）。实现细节已存于 06-16 日志，不重复
- 验证：纯文档变更，无代码/模型/契约改动，无需构建验证
- Skill：无匹配（文档结构优化，非 Nop 实现）

### 2026-06-17（Plan 1 Phase 1 + Phase 4 实施完成：支付回调链路 + ORM cascade 商品删除保护）

- 执行 Plan 1 Phase 1（支付确认链路，Protected: ask-first，已获 human approval），闭环 P0-1（回调不推进订单）+ P0-2（pay() 无凭证可推进）
- 改动：新增 `app-mall-api/.../IPaymentCallback.java`（桥接接口，wx 不依赖 dao/service）+ `app-mall-service/.../PaymentCallbackImpl.java`（实现，`app-service.beans.xml` 注册 `<bean ioc:type="@bean:id" ioc:default="true"/>`）；`ILitemallOrderBiz` + `LitemallOrderBizModel.confirmPaidByNotify`（幂等：已 PAY 跳过；系统上下文 `ServiceContextImpl()` 因回调无用户会话）；`WxPayNotifyResource` @Inject paymentCallback 验签后调用；`WxPayServiceImpl.parseNotifyBody` 仅 tradeState==SUCCESS 返回 outTradeNo；`PayService.isEnabled()` 抽象（WxPayServiceImpl=WXPAY_ENABLED、MockPayServiceImpl=false）；`pay()` 守卫 actualPrice>0 && isEnabled()→`ERR_ORDER_USE_REAL_PAYMENT`
- 关键 Decision：选项 B（confirmPaidByNotify 内部接口由回调驱动）优于选项 A（pay() 内 queryPayment）——回调是微信侧可信触发源，前端不应决定支付事实。`pay()` 接受边界收窄为零金额任意模式 / 示例模式任意金额
- IoC 踩坑：跨模块 `<ref>` 在 `nop.err.ioc.unknown-bean-ref` 上失败；`@Named` 不足以接口类型注入；必须 `<bean id=... ioc:type="@bean:id" ioc:default="true"/>` 显式注册实现
- 验证：新增 `testConfirmPaidByNotify`（回调→订单推进 + 幂等）；核心套件全过
- 执行 Plan 1 Phase 4（数据完整性，Protected: ORM model + human approval，已获），闭环 P0-7（goods→orderGoods cascadeDelete 软删级联破坏订单商品历史）+ 11-6（无删除保护）
- 改动：移除 `model/app-mall.orm.xml` goods→orderGoods 的 cascadeDelete=true（保留 order→orderGoods）；`mvn install -pl app-mall-dao -am` 重新生成，核验 `_app.orm.xml` goods→orderGoods tagSet=`pub,updatable,insertable`（无 cascadeDelete）；`LitemallGoodsBizModel.defaultPrepareDelete`（注入 `ILitemallOrderGoodsBiz`，按 goodsId+deleted=false 计数，>0 抛 `ERR_GOODS_HAS_ORDER_HISTORY`，参照 CategoryBizModel:82-100）；新增 ErrorCode
- Decision：驳回"仅 defaultPrepareDelete 不改模型"——goods 用 useLogicalDelete，软删级联仍连带软删 orderGoods 破坏历史可见性/统计；模型层移除是根因修复，order→orderGoods 保留保证订单删除仍清理明细
- 验证：新增 `testDeleteGoodsWithOrderHistoryRejected`（IGraphQLEngine 调 `LitemallGoods__delete`，有 orderGoods 引用→status=-1）；**核心套件 36 测试全过（+1），零回归**；package BUILD SUCCESS
- 测试数据踩坑：`LitemallOrderGoods.picUrl` 是 OrmFileComponent（非普通 String）——空串被当 null 触发 NOT NULL 违约，任意串被当 fileId 触发 UnknownEntityException；必须先建 `NopFileRecord` 再以其 fileId 作 picUrl。对比 `LitemallGoods.picUrl` 是普通 String 列（空串可存）
- Skill：`nop-backend-dev`+`nop-testing`+`nop-debugging`+`nop-orm-modeler`+`nop-database-design`；逐方法 selfcheck
- **Plan 1 状态**：Phase 1/2/3/4/5 全 completed；Phase Final 进行中（闭合审计 + 全量 `./mvnw test` 待跑 + known-good-baselines 待回写）

### 2026-06-17（Plan 1 闭合审计 Round 1 → 修复 Blocker B1 → Round 2 PASS）

- 独立子代理闭合审计（ses_12d0e0a67ffe...）Verdict REVISE：发现 1 Blocker + 4 Minors
- **Blocker B1（P0-5 库存回滚变异顺序 bug）**：`LitemallAftersaleBizModel.refund()` 在 `shouldRestock` 检查前已把 orderStatus 变异为 203，致未发货 PAY + type≠GOODS_REQUIRED 订单 `orderStatus==PAY` 为 false → 跳过库存回滚——正是 P0-5 要修复的场景。**这是审计最关键价值**：原 testRefundSuccess 只断言 aftersaleStatus，未断言库存/订单状态，故 bug 静默通过。
- 修复：捕获 `wasUnshipped`（变异前状态）用于 `shouldRestock` 检查；强化 `testRefundSuccess` 断言 `product.number` 回滚 +2 与 `orderStatus==203`
- Minors：M1 plan 路径（PaymentCallbackImpl 在 `service/pay/`）；M2 owner doc 方法名（`onPaymentSuccess` 非 `confirmPaidByNotify`，无 amount 参数）；M4 `WxPayNotifyResource` NopException 映射 401→500（confirmPaidByNotify 幂等，WeChat 重试安全）；M3 测试覆盖盲区（已补 stock+203，coupon 归还断言留后续）
- 验证：核心套件 45 测试全过（含强化 testRefundSuccess），零回归；`./mvnw clean package -DskipTests` 全 10 模块 SUCCESS
- 教训（记入 lessons 候选）：**状态变异与条件检查的顺序是并发/状态机修复的高危区**——在"检查状态→变异状态→再检查同一状态"模式下，第二次检查读到的是变异后值。修复后必须用变异前快照（`wasUnshipped`）。测试必须断言"副作用"（库存/券/通知）而非仅状态码，否则逻辑 bug 静默通过。
- **Plan 1 状态：completed**（closure audit passed Round 2）。Plan 2（域逻辑加固）/Plan 3（治理）已审计通过但未实施。
