# Development Log - 2026-06-17

### 2026-06-17（设计文档与代码一致性修复计划实施完成）

- 执行 `docs/plans/2026-06-17-1700-design-code-consistency-fix-plan.md`，修复审计报告 4 项发现，全量 111 测试零回归
- Phase 1（文档对齐）：
  - F-doc-code-1 `order-and-cart.md` 售后范围/流转规则——售后资格从"只有已收货"改为"已支付未发货(201)或已收货(401/402)+售后INIT"，对齐 `LitemallAftersaleBizModel:194-196` + aftersale-type 字典 GOODS_MISS
  - F-name-1 `order-and-cart.md` 迁移规则+text-art——消除"已申请退款"订单状态措辞，归到售后状态机 REQUEST
  - F-doc-code-2 `marketing-and-promotions.md` 团购——"开团成功"澄清为参团人数计算值，非持久 status 枚举（0/1/2）
  - ripple `flow-overview.md` 售后 mermaid 触发边改"订单已支付或完成收货"
- Phase 2（代码修复）：
  - F-doc-code-3 `LitemallOrderBizModel.deleteOrder:478-483` 删除白名单追加 GROUPON_EXPIRED(204)，使 204 与 203 已退款一致可删。human 确认见 `docs/discussions/2026-06-17-204-deletable-confirmation.md`
  - 新增 `testDeleteGrouponExpiredOrder`：正向(204 可删 + deleted=true + orderStatus 保持 204) + 反向(PAY=201/SHIP=301 被拒)。踩坑：直接 DAO `updateEntity` 需活跃 OrmSession，改用 `ormTemplate.runInSession(...)`（参考 TestLitemallCommentBizModel:250）
- 验证：`./mvnw.cmd test -pl app-mall-service -Dtest=TestLitemallOrderBizModel` → 8/8 通过；全量 `./mvnw.cmd test -pl app-mall-service` → 111/111 通过，零回归，BUILD SUCCESS
- Skill：Phase 2 nop-backend-dev+nop-testing；selfcheck 19 项通过（仅改守卫条件，未改签名/注解/错误码）
- 计划审计：Round 1 needs-revision（Blocker B1 行为变更缺可追溯确认）→ 新增 discussion 记录 + M1/M2 采纳 → Round 2 passed

### 2026-06-17（设计文档一致性审计 + 修复本次引入回归）

- 审计报告：`docs/analysis/2026-06-17-design-doc-consistency-audit.md`。方法：状态码字典 ↔ `_AppMallDaoConstants` ↔ 设计表逐值比对 + BizModel 每个 setOrderStatus 写入点与资格守卫逐条比对
- 发现：2 处文档内在不一致（均为本次 flow-overview.md 引入）+ 3 处文档-代码不一致 + 1 处命名混淆 + 6 类已对齐项
- 已修本次回归：flow-overview.md 订单状态机 mermaid 去掉 `已支付→退款中→已退款`（与 202 保留位抉择冲突），改为 `已支付→已退款(售后退款成功,未发货)`；L3 售后资格规则拆分（原"评价与售后都以收货完成为边界"过宽，售后实际含未发货退款）
- 关键发现（待处理）：F-doc-code-1 售后申请资格——`order-and-cart.md` 说"只有已完成收货才能申请售后"，但 `LitemallAftersaleBizModel:194-196` 允许 PAY(201)‖CONFIRM(401)‖AUTO_CONFIRM(402)，aftersale-type 字典 GOODS_MISS=未收货退款 印证代码为真相，文档过度收窄
- 其他待处理：团购"开团成功"误列为持久态（实为计算值）；团购超时(204)不在删除白名单（`LitemallOrderBizModel:478-482`），文档当终态可删；迁移叙述"已申请退款"命名混淆

### 2026-06-17（docs/design 引入 FSD 手法：全局流程总览 + 跨域引用表）

- 背景：对比 `C:/projects/cardlite-product/DMS/docs/fsd`（同源 AGE 模板，但额外拆了 fsd/ 业务规格层）。结论：DMS 整体文档模式不更先进，但其"规格/结构拆两层 + 双向反引 + FSD 写作手法"对状态机密集系统更优；商城 4/5 域为 CRUD，不宜全量复制 fsd/ 目录，改为局部吸收
- 新增 `docs/design/flow-overview.md`：L1 购买主链路页面流 + L2 订单主状态机/售后状态机（mermaid）+ 状态-域-文档-页面映射矩阵 + L3 跨域公共规则（资格前置/价格库存实时性/快照语义/恢复语义/团购透传/支付模式分流）。纯业务基线视图，不掺实现说明
- 给 5 个域文档补"跨域引用表"结构化小节（替换/增强原散文式 cross-ref）：`order-and-cart.md`（枢纽域，9 个交接点）、`product-catalog.md`、`marketing-and-promotions.md`、`user-and-address.md`、`system-configuration.md`
- 更新 `docs/design/README.md`：flow-overview.md 纳入起始文档与详细文档表
- 关键 Decision：保留单层 design/ 结构（不过度设计），只注入 FSD 的"全局总览图 + 矩阵 + 跨域引用表"三手法
- 清理债处理：`order-and-cart.md` 迁移规则段原有带日期实现说明块（含代码路径名 `LitemallAftersaleBizModel.apply/refund`、"202 当前无写入点"实施状态），改写为纯设计抉择说明（退款为何由 aftersaleStatus 表达 + 两条迁移约定 + 202 保留位）。实现细节已存于 06-16 日志，不重复
- 验证：纯文档变更，无代码/模型/契约改动，无需构建验证
- Skill：无匹配（文档结构优化，非 Nop 实现）

### 2026-06-17（Plan 1 Phase 1 + Phase 4 实施完成：支付回调链路 + ORM cascade 商品删除保护）

- 执行 Plan 1 Phase 1（支付确认链路，Protected: ask-first，已获 human approval），闭环 P0-1（回调不推进订单）+ P0-2（pay() 无凭证可推进）
- 改动：新增 `app-mall-api/.../IPaymentCallback.java`（桥接接口，wx 不依赖 dao/service）+ `app-mall-service/.../PaymentCallbackImpl.java`（实现，`app-service.beans.xml` 注册 `<bean ioc:type="@bean:id" ioc:default="true"/>`）；`ILitemallOrderBiz` + `LitemallOrderBizModel.confirmPaidByNotify`（幂等：已 PAY 跳过；系统上下文 `ServiceContextImpl()` 因回调无用户会话）；`WxPayNotifyResource` @Inject paymentCallback 验签后调用；`WxPayServiceImpl.parseNotifyBody` 仅 tradeState==SUCCESS 返回 outTradeNo；`PayService.isEnabled()` 抽象（WxPayServiceImpl=WXPAY_ENABLED、MockPayServiceImpl=false）；`pay()` 守卫 actualPrice>0 && isEnabled()→`ERR_ORDER_USE_REAL_PAYMENT`
- 关键 Decision：选项 B（confirmPaidByNotify 内部接口由回调驱动）优于选项 A（pay() 内 queryPayment）——回调是微信侧可信触发源，前端不应决定支付事实。`pay()` 接受边界收窄为零金额任意模式 / 示例模式任意金额
- IoC 踩坑：跨模块 `<ref>` 在 `nop.err.ioc.unknown-bean-ref` 上失败；`@Named` 不足以接口类型注入；必须 `<bean id=... ioc:type="@bean:id" ioc:default="true"/>` 显式注册实现
- 验证：新增 `testConfirmPaidByNotify`（回调→订单推进 + 幂等）；核心套件全过
- 执行 Plan 1 Phase 4（数据完整性，Protected: ORM model + human approval，已获），闭环 P0-7（goods→orderGoods cascadeDelete 软删级联破坏订单商品历史）+ 11-6（无删除保护）
- 改动：移除 `model/app-mall.orm.xml` goods→orderGoods 的 cascadeDelete=true（保留 order→orderGoods）；`mvn install -pl app-mall-dao -am` 重新生成，核验 `_app.orm.xml` goods→orderGoods tagSet=`pub,updatable,insertable`（无 cascadeDelete）；`LitemallGoodsBizModel.defaultPrepareDelete`（注入 `ILitemallOrderGoodsBiz`，按 goodsId+deleted=false 计数，>0 抛 `ERR_GOODS_HAS_ORDER_HISTORY`，参照 CategoryBizModel:82-100）；新增 ErrorCode
- Decision：驳回"仅 defaultPrepareDelete 不改模型"——goods 用 useLogicalDelete，软删级联仍连带软删 orderGoods 破坏历史可见性/统计；模型层移除是根因修复，order→orderGoods 保留保证订单删除仍清理明细
- 验证：新增 `testDeleteGoodsWithOrderHistoryRejected`（IGraphQLEngine 调 `LitemallGoods__delete`，有 orderGoods 引用→status=-1）；**核心套件 36 测试全过（+1），零回归**；package BUILD SUCCESS
- 测试数据踩坑：`LitemallOrderGoods.picUrl` 是 OrmFileComponent（非普通 String）——空串被当 null 触发 NOT NULL 违约，任意串被当 fileId 触发 UnknownEntityException；必须先建 `NopFileRecord` 再以其 fileId 作 picUrl。对比 `LitemallGoods.picUrl` 是普通 String 列（空串可存）
- Skill：`nop-backend-dev`+`nop-testing`+`nop-debugging`+`nop-orm-modeler`+`nop-database-design`；逐方法 selfcheck
- **Plan 1 状态**：Phase 1/2/3/4/5 全 completed；Phase Final 进行中（闭合审计 + 全量 `./mvnw test` 待跑 + known-good-baselines 待回写）

### 2026-06-17（Plan 1 闭合审计 Round 1 → 修复 Blocker B1 → Round 2 PASS）

- 独立子代理闭合审计（ses_12d0e0a67ffe...）Verdict REVISE：发现 1 Blocker + 4 Minors
- **Blocker B1（P0-5 库存回滚变异顺序 bug）**：`LitemallAftersaleBizModel.refund()` 在 `shouldRestock` 检查前已把 orderStatus 变异为 203，致未发货 PAY + type≠GOODS_REQUIRED 订单 `orderStatus==PAY` 为 false → 跳过库存回滚——正是 P0-5 要修复的场景。**这是审计最关键价值**：原 testRefundSuccess 只断言 aftersaleStatus，未断言库存/订单状态，故 bug 静默通过。
- 修复：捕获 `wasUnshipped`（变异前状态）用于 `shouldRestock` 检查；强化 `testRefundSuccess` 断言 `product.number` 回滚 +2 与 `orderStatus==203`
- Minors：M1 plan 路径（PaymentCallbackImpl 在 `service/pay/`）；M2 owner doc 方法名（`onPaymentSuccess` 非 `confirmPaidByNotify`，无 amount 参数）；M4 `WxPayNotifyResource` NopException 映射 401→500（confirmPaidByNotify 幂等，WeChat 重试安全）；M3 测试覆盖盲区（已补 stock+203，coupon 归还断言留后续）
- 验证：核心套件 45 测试全过（含强化 testRefundSuccess），零回归；`./mvnw clean package -DskipTests` 全 10 模块 SUCCESS
- 教训（记入 lessons 候选）：**状态变异与条件检查的顺序是并发/状态机修复的高危区**——在"检查状态→变异状态→再检查同一状态"模式下，第二次检查读到的是变异后值。修复后必须用变异前快照（`wasUnshipped`）。测试必须断言"副作用"（库存/券/通知）而非仅状态码，否则逻辑 bug 静默通过。
- **Plan 1 状态：completed**（closure audit passed Round 2）。Plan 2（域逻辑加固）/Plan 3（治理）已审计通过但未实施。
