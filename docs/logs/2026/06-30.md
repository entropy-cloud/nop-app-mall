# Development Log - 2026-06-30

### 2026-06-30 (M3 商品详情 & 购物车 交付)

- **mobile-m3 商品详情 & 购物车交付（`docs/plans/2026-06-30-1343-3-...`）**——跨仓库（nop-chaos-flux `apps/mall-mobile/`，`@nop-chaos/mall-mobile`）。M3 全部三 phase 验证通过，纯消费既有后端 API，**无后端改动**。
  - **Phase 1 商品详情页：** `src/pages/goods/detail.tsx`（`PageShell` + `Gallery` 轮播 + 价格/划线价 + 标题/简介 + `getStockSemantic` 库存语义 + SKU 选择 `SkuDrawer` 底部抽屉 + 评价摘要消费公开 `getCommentSummary`（总数/好评率/5 星分布）+ `dangerouslySetInnerHTML` 详情富文本 + 底部固定操作栏：收藏/加入购物车/立即购买）；`src/components/gallery.tsx`（手势左右滑动 + dots + 序号，render-time 派生 `current` 避免 setState-in-effect）；`src/components/sku-drawer.tsx`（规格矩阵 pills → product 解析、未选齐禁用、缺货组合置灰、数量步进、`requireLogin` 分支）；`src/pages/goods/sku-matrix.ts`（纯函数：`buildSkuMatrix`/`resolveProduct`/`isSelectionComplete`/`optionHasStock`，axis 名按 `goods.specifications` 关系回退 `规格N`）；`src/api/{catalog-api.ts` 增 `fetchGoodsDetail`/`fetchStockSemantic`/`splitGallery`、`product-api.ts` `fetchProductsByGoods`（`/r/LitemallGoodsProduct__findList` filter `{eq:["goodsId",id]}`，格式据 `app-mall-web` goods-detail.page.yaml 既有范本）、`comment-api.ts` `fetchCommentSummary`（type=0 商品）}。
  - **Phase 2 收藏/加购/足迹接线：** `src/pages/goods/use-goods-actions.ts`（`useGoodsActions(goodsId)` hook：`toggleCollect`/`addToCart`/`buyNow`/`recordFootprint`，`ensureAuth` 经 M1 `requireAuth` 半游客拦截——未登录跳 `login?returnTo`；加购成功 `incCartBadge(1)` 刷角标；足迹未登录静默跳过、已登录 onMount 记录、失败可重试；`collected` 状态据 `isCollect` 异步回显，用 React 文档版「adjust state when prop changes」render-time guarded setState（`prevResetKey`）替代 setState-in-effect）；`src/api/{collect-api.ts,footprint-api.ts,cart-api.ts}`（typed client，REST `/r/` 裸 args）。
  - **Phase 3 购物车页：** `src/pages/cart.tsx`（替换占位；未登录提示登录态、`findPage` filter `{eq:["userId",uid]}` 拉全量（设计要求显示勾选态，checkedList 仅返回已勾选不足以承载 check/uncheck UI）；`SwipeCell` 行包裹左滑删除；单选/全选乐观更新 + 失败回滚；数量 `QuantityStepper` 防抖 500ms + 不变不发（D2 §3.3）；数量=1 减号变删除图标（D2 §3.2，`onMinusAtMin`）；清空确认对话框；结算入口禁用并提示「M4 上线」；角标与列表长度联动）；`src/components/swipe-cell.tsx`（自建，按 `flux-renderers-mobile/SwipeCellRenderer` 手势语义——`touch-action:pan-y`、阈值 40、closeOnOutside pointerdown、close-after-action；ref 写仅在事件处理、render 只读 state，避 `react-hooks/refs`）；`src/components/quantity-stepper.tsx`。
  - **Decision D1（SKU 交互）：** 底部 drawer 弹层（否决内联展开：多规格占屏过大）。**Decision D2（数量/删除）：** 步进器 + swipe-cell 左滑删除 + 数量=1 减号变删除（否决行内删除按钮：占横向空间）；防抖 + 不变不发。
  - **lint 适配（react-hooks recommended-latest + react-compiler，全 `error`）：** render-phase setState/reset 全改 render-time guarded setState（gallery、use-goods-actions）；`react-hooks/refs` 禁 render 阶段 ref 读 → swipe-cell 用 `isDragging` state + `dragOffsetRef`（仅事件处理写）；label-has-associated-control 加 `htmlFor`/`id`、无文本 checkbox 改 `aria-label`；`no-array-index-key` 改 `key={src}`/`key={axis.id}`（axis 加 stable `id` 字段）；`react-compiler` 禁 disable react-hooks 规则 → footprint effect 改 `recordFootprint` 直接入 deps（callback 已 `useCallback` 稳定）。
  - **验证（全绿）：** 前端 `typecheck` EXIT=0、`lint` EXIT=0、`test` **150 passed**（21 files，新增 goods-detail 5 + sku-matrix 13 + use-goods-actions 8 + cart 11）、`build` EXIT=0（dist ~234KB index + 190KB react-vendor + 31KB css）。无后端改动（纯消费 `LitemallGoods`/`LitemallGoodsProduct`/`LitemallComment`/`LitemallCollect`/`LitemallFootprint`/`LitemallCart` 既有 GraphQL）。

### 2026-06-30 (M2 首页 & 分类导航 交付 + 修复)

- **mobile-m2 首页 & 分类导航交付（`docs/plans/2026-06-30-1343-2-...`）**——跨仓库（nop-chaos-flux `apps/mall-mobile/` + nop-app-mall 后端）。M2 全部四 phase 验证/修复通过。
  - **Phase 1 首页：** `src/pages/home/index.tsx`（Banner 轮播 + 快捷入口网格 + 新品/热销推荐楼层 + 专题精选）、`src/components/{banner,search-bar,goods-card,state-views,pull-to-refresh,page-shell}.tsx`、`src/api/catalog-api.ts`（`fetchHomeBanners` **前端 `position===1` 过滤**、`fetchGoodsByFlags` isNew/isHot、`fetchTopicPage`）、`src/hooks/use-async.ts`。下拉刷新（`PullToRefresh` ref → `refreshAll`）；骨架/空/错/重试态（`FloorState` + `Skeleton`/`EmptyState`/`ErrorRetry`）。
  - **Phase 2 分类页：** `src/pages/category/index.tsx`（左侧 rail 一级分类 + 二级 pills + 商品网格 `InfiniteScroll` 触底分页）、`src/hooks/use-paged-list.ts`（分页 append/replace + hasMore + seq 防过期）。
  - **Phase 3 搜索：** `src/pages/search/index.tsx`（防抖 400ms + 热门关键词 + 本地历史）、`src/pages/search/search-history.ts`（localStorage 去重/上限 10 条/置顶/清空/容错）。
  - **Phase 4 品牌后端 + 前端：** 后端 `app-mall-service/.../entity/LitemallBrandBizModel.java` 新增 `frontList`/`frontDetail`（`@BizQuery @Auth(publicAccess=true)`，仿 `LitemallTopicBizModel` peer）+ `app-mall-dao/.../biz/ILitemallBrandBiz.java` 声明方法；`app-mall-service/.../entity/TestLitemallBrandBizModel.java`（IGraphQLEngine：frontList 分页/frontDetail 存在/不存在/已删除/匿名访问）。前端 `src/pages/brand/{list,detail}.tsx` + `src/api/brand-api.ts`。
  - **修复（执行期发现并修）：**
    - 后端编译错误：`LitemallBrandBizModel` 缺 `import PageBean`；`app-mall-biz/` 为误建的游离模块（不在 parent pom、非依赖、与 dao 同 FQN `app.mall.biz.ILitemallBrandBiz` 冲突致 `@Override` 失败）→ 删除 `app-mall-biz/`，方法声明移入 `app-mall-dao` 对齐 Topic peer。
    - 后端测试：`LitemallBrand.picUrl` 非空，`TestLitemallBrandBizModel.setUp` 补 `picUrl`。
    - 前端测试基础设施：happy-dom（vitest 4.1.2）`window.localStorage` 为裸对象（无 Storage 方法）→ 新增 `src/test-setup-dom.ts`（仅在缺失时装 in-memory Storage）；RTL auto-cleanup 因 vitest `globals:false` 未注册致 DOM 跨测试累积（首页 topic×2、品牌 Acme×2、分类 8 卡片）→ setup 注册 `afterEach(cleanup)`；`vitest.config.ts` 用 `mergeConfig` 追加 setupFile。
    - 前端测试 bug：`infinite-scroll.test.tsx` 传 `loading={false}` 却断言加载文案 → 改 `true`；`home.test.tsx` Banner 为单可见 slide 轮播，断言改为校验 `data-count`/dots；`category.test.tsx` 用 `find`（首个）改 `filter().at(-1)`（末次）。
    - **lint（react-hooks recommended-latest + react-compiler，全 `error`）：** `useAsync`/`usePagedList` 由 forwarded-`deps` 数组改为单 `key` 参数（`useEffect(fn,[key])` 字面量，react-compiler 禁止 disable react-hooks 规则）；render-phase ref 更新移入 effect；`forwardRef`→React 19 ref-as-prop（`PullToRefresh`）；`doRefresh` 包 `useCallback`；分类页 `activeTopId` 改 render-time 派生（去 setState-in-effect）；搜索历史改 render-time guarded setState（`react-hooks/refs` 禁 render 阶段 ref 访问）。
  - **验证（全绿）：** 前端 `typecheck` EXIT=0、`lint` EXIT=0、`test` **114 passed**（17 files）、`build` EXIT=0；后端 `./mvnw -pl app-mall-service test -Dtest=TestLitemallBrandBizModel` **Tests run: 6, Failures: 0, Errors: 0**。roadmap M2 `todo`→`done`。

### 2026-06-30

- **mobile-m1 移动端脚手架 & 基础设施交付（`docs/plans/2026-06-30-1343-1-...`）**——跨仓库（nop-chaos-flux monorepo `apps/mall-mobile/`，`@nop-chaos/mall-mobile`）落地 M1 全部三 phase，纯消费既有 `LoginApi`，无后端改动。
  - **Phase 1 脚手架 + 渲染器/env 接线：** `apps/mall-mobile/`（`package.json` 仿 `apps/playground`，仅必需 `workspace:*` + `react`/`react-dom`/`lucide-react`/`zustand`；脚本 dev/build/typecheck/test/lint）、`tsconfig.json`（extends `tsconfig.base.json`）、`vite.config.ts`（React 插件 + `@tailwindcss/vite` + babel react-compiler preset + dev proxy `/api` 与 `/r` → `MALL_BACKEND_ORIGIN` 默认 `http://localhost:8080` + workspace alias + `manualChunks` react-vendor/ui）、`vitest.config.ts`（共享 `createSharedVitestConfig happy-dom`）、`index.html`（移动 viewport + safe-area）、`src/schema-renderer.ts`（`createDefaultRegistry` + 注册 basic/form/data/content/layout/**mobile** 渲染器 + `createSchemaRenderer()`）、`src/env.ts`（`createFetcher` 注入 `Authorization` + 解析 Nop `{status:0,data}` 信封；`createNotify`/`createConfirm`；`createEnv` 工厂）、`src/main.tsx` + `src/styles.css`（Tab 壳 + safe-area + 44px 触摸基线样式）。
  - **Phase 2 路由 + Tab + 全局状态：** `src/route-model.ts`（`RouteSpec = tab{home/category/cart/profile} | auth{login/register/forgot,returnTo} | page{page,title,params}`；`parseRoute`/`buildRoute` hash 序列化，正确切分 path 与 query）、`src/use-route.ts`（hashchange 监听 + `navigate(spec,{replace})` + `back()`；页面栈即浏览器 history，push/pop 复用 history.back）、`src/layouts/tab-shell.tsx`（底部 4 Tab + 购物车角标 slot，lucide 图标，44×44px 触摸区）、`src/store/index.ts`（Zustand + `persist`：`accessToken`/`refreshToken`/`userInfo` partialize 持久化、`cartBadge` session-only；`createMallStore(getStorage)` 工厂 + `createMemoryStorage` 便于测试注入；`resolvePersistentStorage` 探测 localStorage 不可用时降级内存）、4 Tab 占位骨架页（home/category/cart/profile，M2/M3/M6 填充）、`src/router-views.tsx`、`src/App.tsx`。
  - **Phase 3 认证 + Token 拦截续期 + 半游客：** `src/api/login-api.ts`（typed client：`login`/`signUp`/`sendResetCode`/`resetPassword`/`logout` 走 `getAppEnv().fetcher`，POST `/r/LoginApi__<op>` 裸 args；`GraphQLError` 携带 status/msg；login 写 store，signUp 不自动登录客户端再调 login——对齐 live 契约 `TestLoginApiSignUp`）、`src/auth/refresh.ts`（`refreshAccessToken` **单飞**：in-flight promise 复用，并发 401 共享一次 `LoginApi__refreshToken`；失败 clearAuth；走原生 fetch 避免与 flux fetcher 递归）、`src/env.ts` fetcher 增强（401 → `refreshAccessToken` → 以新 token **重放原请求一次**；refresh 返回 null → `onUnauthorized` → 跳登录）、`src/env-instance.ts`（singleton `getAppEnv()` 接线 store `getToken` + `refreshAccessToken` + `hashNavigate` + `redirectToLogin(returnTo)`）、`src/guards/require-auth.ts`（半游客：`PROTECTED_ACTIONS` 集合 = 加购/结算/个人中心/订单/优惠券/取消订单/收藏/足迹/售后/评价；`requireAuth(action,navigate,isLoggedIn)` 未登录跳 `login?returnTo=<原意图>`）、`src/pages/auth/{login,register,forgot}.tsx`（受控表单 + 校验 + 60s 验证码冷却 + loading/error 反馈，按钮 ≥44px）。
  - **Decision D1（app 落点）：** monorepo 内 `apps/mall-mobile/`（flux 包均 `private:true`，跨仓库消费摩擦大；playground 范本即在 monorepo 内；与 `nop-entropy` 兄弟仓库依赖模式一致）。**Decision D2（路由）：** 复用 playground route-model + use-route + flux `navigate` env 钩子，否决 react-router（避免双源）。**Decision D3（续期）：** 被动 401 续期 + 单飞重放，否决定时主动刷新（Nop 无显式 refresh_token 双令牌契约）。
  - **验证（全绿）：** `pnpm --filter @nop-chaos/mall-mobile typecheck` EXIT=0；`build` EXIT=0（dist 产物 ~168KB index + 190KB react-vendor）；`test` **58 passed**（env fetcher 9 + Nop 信封 helper + 401 续期重放/失败回退 2 + route-model roundtrip 12 + store auth/cart/persist 10 + login-api login/signUp/resetCode/resetPassword/logout 成功+失败态 10 + refresh 单飞/失败 5 + guard returnTo 5 + schema-renderer 注册烟测 3）；`lint` EXIT=0。跨模块下游未触共享包改动（仅新增 app）。
  - 文档同步：`docs/architecture/system-baseline.md` Mobile Mall H5 章节由 `planned` 改 `M1 delivered`，补「Engineering landing (D1)」+「Auth consumption layer (D3)」+ apps/mall-mobile 引用；`docs/backlog/mobile-frontend-roadmap.md` M1 `todo`→`done`。**残留风险/successor**：三方登录（后端未提供契约）、生产反代/CORS（本计划仅保证 dev proxy）、signUp 不自动登录（客户端两步，已对齐契约）。
  - 注：happy-dom 测试环境 `localStorage.setItem` 非函数，故 store `resolvePersistentStorage` 探测后降级 `createMemoryStorage()`，测试经 `createMallStore(()=>memoryStorage)` 注入存储验证持久化语义（生产浏览器 localStorage 正常）。

- 修正 `missions/mall-mobile.json` 配置：`moduleDir` 由 `"."`（Maven 根，与 pnpm commands 不匹配、导致 CHECK 假绿）改为 `"app-mall-mobile"`；description 补「代码位于 app-mall-mobile/」+「M1 脚手架落地前 CHECK 无 package.json 可跑，视为未起步」。commands（pnpm test/build/lint/typecheck）保持不变——对目标态（Vite/React 前端）正确，现与 moduleDir 一致。为满足驱动器存在性校验（`config.js:75`→`mission-check.mjs:43` 强制 moduleDir 须存在于磁盘），创建占位目录 `app-mall-mobile/README.md`（说明用途、状态、nop-chaos-flux 引用路径；无 package.json，避免干扰 M1 `pnpm create vite` 脚手架）。前端位置决策：仓库内 `app-mall-mobile/`（与 `app-mall-*` Maven 模块并列的独立 pnpm/Vite 工程）。`mission-check` 校验 exit=0，`--list-missions` 正常。`docs/context/codebase-map.md` 新增 Mobile frontend (planned) 行。

- 架构文档校正（用户指令固化）：移动端前端技术栈确认为 **nop-chaos-flux**（JSON-to-React 低代码框架，React 19 + Zustand），移动端开发必须参考并直接复用 nop-chaos-flux 的 mobile 机制，不自建移动端 UI 栈。修正架构 owner 文档与 backlog 的历史冲突——`docs/architecture/system-baseline.md` Frontend Stack 拆分为 Web/Admin（AMIS）+ Mobile H5（nop-chaos-flux）两面、State Management 补 Zustand、补 nop-chaos-flux 仓库与 flux-guide 引用路径；`docs/architecture/project-vision.md` 约束行补移动端栈、Non-Goals「Not a mobile application (web-only)」改为「非原生 App（移动端为 nop-chaos-flux H5）」；`docs/backlog/mobile-frontend-roadmap.md` 新增「nop-chaos-flux Reference」章节。**仓库已定位**：`~/app/nop-chaos-flux-wt/nop-chaos-flux-master/`（含 `flux-guide/` 开发指南、`packages/flux-renderers-mobile/` 移动端组件包 pull-refresh/infinite-scroll/swipe-cell/countdown/notice-bar）。M1-M9 均 todo，M1 启动脚手架前须先通读 `flux-guide/README.md` + `flux-guide/mobile/README.md`。

- 交付 deferred successor「剩余业务事件站内信通知（拼团成功 + 评价奖励）」。计划：`docs/plans/2026-06-30-0229-1-remaining-event-notifications-plan.md`（源 `2026-06-29-1921-3` Deferred「其他业务事件站内信补全」，触发条件 P25 拼团/P33 评价/P35 消息中心 done 已满足）。
- **Phase 1 后端实现：**
  - `app-mall-service/.../entity/LitemallPinTuanActivityBizModel.java`：`markPinTuanSuccess`（ACTIVE→SUCCESS 翻转后）新增成团站内信——主事务内读事件开关 `mall_message_event_enabled_pintuan-success` + `dao().flushSession()`（保证本次 join 刚 saveEntity 的成员落库可见）+ 按 groupId 查全部成员组装 `List<String>` 载荷 + 组装 content（嵌商品名，经新增 `@Inject ILitemallGoodsBiz goodsBiz` 解析），`txn().afterCommit` 内零 DB 查询逐人 `sendUserMessage(uid, MSG_TYPE_ORDER, "拼团成功", content)`。新增常量 `EVENT_KEY_PINTUAN_SUCCESS`/`PINTUAN_SUCCESS_TITLE` + helper `collectGroupMemberUserIds`/`buildPinTuanSuccessContent`。幂等靠 ACTIVE→SUCCESS 状态守卫（D2）。
  - `app-mall-service/.../entity/LitemallCommentBizModel.java`：新增 `@Inject MallNotificationService notificationService` + 常量 `EVENT_KEY_COMMENT_REWARD`/`COMMENT_REWARD_TITLE` + helper `notifyCommentReward`。两路径接线：`submitComment` 预审关 reward>0 即时发放后 + `batchAuditComments` approve 分支 earnPoints 后，经 `notifyCommentReward`（主事务读开关解析 notifyUid→null=关，afterCommit 投递 SYSTEM「评价奖励到账」含积分数+评价编号）。仅 reward>0 投递（D3）；幂等靠 PENDING→APPROVED/提交守卫，非聚合——同用户同日不同评价各收一条（D4 方案 B）。
- **测试：** `TestLitemallPinTuanActivityBizModel` +3（成团全员各收 1 条 / 未成团 0 条 / 开关关闭 0 条）；`TestLitemallCommentBizModel` +2（submit 预审关 reward>0 收 1 条 / reward=0 收 0 条）；`TestLitemallCommentOpsWorkbench` +4（approve reward>0 收 1 条 / approve reward=0 收 0 条 / reject 0 条 / 同用户两条不同评价各收共 2 条锁定非聚合）。调试发现两个根因：(1) joinPinTuan 同事务 saveEntity 的成员对 findList 不可见→加 `dao().flushSession()`；(2) `litemall_points_account.user_id` 为 INTEGER-backed 列，测试须用数字 userId（真实用户均数字 id）。
- 文档同步：`docs/design/system-configuration.md`（事件→站内信清单 +2 行：拼团成功 ORDER / 评价奖励 SYSTEM + 分类说明）、`docs/design/marketing-and-promotions.md`（拼团「成团判定」补成团通知语义 + 评价得积分补奖励站内信通知语义）。
- 验证（全绿）：`./mvnw -pl app-mall-service test` **Tests run: 551, Failures: 0, Errors: 0, Skipped: 0**（重点 PinTuan/Comment 既有测试零回归 + 9 新增用例）；`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS。


- **Phase 1 后端分群定向投放 + 成员解析抽取：**
  - `app-mall-dao/.../ILitemallOrderBiz.java`：新增 public 方法 `collectRfmLifecycleMatches(segmentValue, byRfm, context)` 声明（内部 helper，无 `@BizQuery/@BizMutation`，同 `releasePromotionUsage`/`refundComboAware` 先例——跨实体经 `ILitemallOrderBiz` 注入调用，rule #6 接口契约）。
  - `app-mall-service/.../entity/LitemallOrderBizModel.java`：`@Override` 实现 `collectRfmLifecycleMatches`（public，**非 private**）——即原 `getSegmentMembers` 主体（装载全量 `getUserPaymentSummaryAllTime` → 三分位阈值 → 逐用户分类 → 命中段过滤 → 排序），返回全量 `List<SegmentMemberBean>`（不分页）。`getSegmentMembers` 重构为调用它后分页——既有行为/返回零回归（移除不再使用的 `emptyMemberPage`，`paginateMembers` 已正确处理空列表）。RFM/生命周期分群仅含 `lastPayTime != null` 用户（既有守卫不变）。
  - `app-mall-dao/.../ILitemallUserMessageBiz.java`：新增 `sendSegmentMessage(segmentType, segmentValue, title, content)` `@BizMutation @Auth(roles="admin")` 声明，返回送达人数。
  - `app-mall-service/.../entity/LitemallUserMessageBizModel.java`：实现 `sendSegmentMessage`——校验 title/content 非空 → 按 `segmentType` 解析全量 userId：`tag`→`userTagBiz.findList(eq(tag))` 去重；`rfm`/`lifecycle`→`orderBiz.collectRfmLifecycleMatches(segmentValue, byRfm)`（经注入 `ILitemallOrderBiz`）去重；非法 type 抛 `ERR_USER_PORTRAIT_INVALID_SEGMENT_TYPE` → 逐成员 `newEntity`+`setMsgType(MSG_TYPE_MARKETING)`+`saveEntity` → 返回送达数（空分群返回 0，D3）。新增 `@Inject ILitemallOrderBiz orderBiz` + `@Inject ILitemallUserTagBiz userTagBiz`（非 private）。跨实体走 `I*Biz`（非 daoProvider 绕过）。
  - `app-mall-service/.../AppMallErrors.java`：新增 `ERR_MESSAGE_SEGMENT_TITLE_OR_CONTENT_EMPTY`。
  - NopIoC 循环依赖：`OrderBizModel→MallNotificationService→UserMessageBizModel→OrderBizModel` 链经 early-singleton reference 解析（与既有 4 个 Order↔BizModel 循环同机制，`nop.ioc.bean-depends-graph.allow-cycle=true` 默认开启），容器正常启动。
- **Phase 1 测试：** `TestLitemallUserMessageBizModel` 新增 6 个 IGraphQLEngine 测试（tag 投放送达数=1 / rfm 投放 / lifecycle 投放 / 空分群返回 0 / 非法 segmentType 拒绝 / 空 title 拒绝；rfm/lifecycle 经 `getUserPortrait` 取实际段值断言送达数≥1）；`getSegmentMembers` 既有 `TestLitemallUserPortraitBizModel` 测试零回归（重构后全绿）。
- 文档同步：`docs/design/system-configuration.md`（消息中心章节补「分群定向推送」能力行 + 业务规则 + D6/D7；MARKETING 分类行更新）、`docs/design/marketing-and-promotions.md`（新增「分群定向营销推送」章节，含触达口径 / D1-D3 / 残留风险与 successor）。
- **Phase 2 前端分群定向推送页：**
  - `app-mall-web/.../mall/message-ops/segment-push.page.yaml`（新增运行时页，对齐 `broadcast.page.yaml` + `segment.page.yaml`）：segmentType select（标签/RFM/生命周期）→ segmentValue 动态控件（`tagValue` input-text / `rfmValue` select 8 中文段 / `lifecycleValue` select 4 中文段，`visibleOn` 按 segmentType 切换，避免同名字段冲突）→ title/content → 「推送」按钮调 `@mutation:LitemallUserMessage__sendSegmentMessage`（api.data 按 segmentType 三元映射 segmentValue）→ feedback 送达人数。
  - `app-mall-web/.../_vfs/app/mall/auth/app-mall.action-auth.xml`：`mall-manage` 下挂 `mall-message-segment-push`（orderNo 211，与「系统公告下发」并列）。
- 验证（全绿）：`./mvnw test -pl app-mall-service -am` **Tests run: 542, Failures: 0, Errors: 0, Skipped: 0**（重点 `getSegmentMembers` 零回归 + `sendSegmentMessage` 6 新增用例）；`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw -pl app-mall-web -am -DskipTests compile` BUILD SUCCESS；跨模块下游校验 `./mvnw test`（全工作区）**Tests run: 550（542 service + 8 web），Failures: 0, Errors: 0**——无下游破坏。`docs/testing/known-good-baselines.md` 追加本 successor 行。

- 交付 deferred successor「跨通道组合支付（余额抵扣 + 第三方通道补差，含安全退款）」。计划：`docs/plans/2026-06-30-0044-1-combo-payment-balance-plus-channel-plan.md`（源 P30 多支付通道 Deferred「跨通道组合支付（余额 + 第三方混合）」）。
- **Phase 1 后端组合支付核心 + 重入守卫 + CREATED 回冲：**
  - `app-mall-dao/.../ILitemallOrderBiz.java`：新增 `payWithCombo(orderId, useBalanceAmount, confirmCredential)` `@BizMutation` 声明。
  - `app-mall-service/.../entity/LitemallOrderBizModel.java`：实现 `payWithCombo`（Decision D1 先原子扣减余额部分 `debitBalance(debitAmount, PAY, sourceType=pay, orderSn)` → 写 `walletPayAmount`；remainder==0 退化全额余额 `payChannel=BALANCE`+`markOrderPaidCore`；remainder>0 `payService.createPayment(totalFee=remainder)`+设 payId，订单保持 101）。组合重入守卫 `rejectComboReentry`（walletPayAmount>0 抛 `ERR_ORDER_COMBO_PENDING`）落地于 pay/payByBalance/prepay/payWithCombo。CREATED 取消/超时余额回冲 `creditBackComboBalance`（cancel/cancelExpiredOrders CAS 守卫后 `creditBalance(walletPayAmount, REFUND, sourceType=order-refund, orderSn)`，复用既有 `SOURCE_TYPE_ORDER_REFUND`）。
  - `app-mall-service/.../AppMallErrors.java`：新增 `ERR_ORDER_COMBO_PENDING` / `ERR_ORDER_COMBO_AMOUNT_INVALID`。
- **Phase 2 后端组合感知退款拆分（资金安全）：**
  - `ILitemallOrderBiz.java`：新增 `refundComboAware(order, refundAmount, context)` 返回 `boolean`（app-mall-dao 不可见 PayRefundResponseBean，故用 boolean 保留各调用方域特定错误语义）。
  - `LitemallOrderBizModel.java`：实现 `refundComboAware`（Decision D3 比例分摊 walletPortion=refundAmount×walletPayAmount/actualPrice scale=2 HALF_UP，channelPortion=refundAmount−walletPortion 余数归通道；组合订单 creditBalance 钱包部分 + channelPortion>0 payService.refund 通道部分；普通订单零回归既有全额通道退款）。
  - 全站 5 退款站点经 `refundComboAware` 路由：`LitemallAftersaleBizModel.refund()`/`confirmReturnReceived()`（保留 `ERR_AFTERSALE_REFUND_FAILED`）、`cancelExpiredPickupOrders()` 非 BALANCE 分支（BALANCE 分支保留既有 creditBalance）、`LitemallGrouponBizModel.refundGrouponOrder()`、`LitemallPinTuanActivityBizModel.refundMemberOrder()`（团购/拼团保留 try-catch refundFailures）。移除 aftersale/groupon/pintuan 不再使用的 `PayService` 注入与 PayRefund*Bean 导入。
- **Phase 3 前端收银台组合支付 UI：**
  - `app-mall-web/.../mall/pay/pay.page.yaml`：余额通道与微信通道之间新增组合分支（visibleOn BALANCE 可用且 actualPrice>0）——展示可用余额 + 抵扣输入（input-number 默认 min(余额,actualPrice)，max 不得超余额/实付）+ 实时剩余 + 密码确认弹窗（复用 P30 登录密码样式）+ `@mutation:LitemallOrder__payWithCombo` + feedback（paid→成功跳订单详情 / !paid+codeUrl→微信补差 QR + 查看订单）。既有零金额/余额全额/微信全额分支零回归。
- **测试：** `TestComboPaymentBizModel` 10 例（成功组合/退化全额/余额不足/非法金额/凭证错误/组合重入/payByBalance 重入/prepay 重入/cancel 回冲/cancelExpiredOrders 回冲）；`TestComboRefundSplitBizModel` 5 例（整单拆分/部分分摊/普通零回归/全额余额 channelPortion=0/通道失败钱包仍回冲）。既有支付/售后/团购/拼团/自提测试零回归。
- 验证（全绿）：`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` EXIT=0；`./mvnw -pl app-mall-service test` **Tests run: 536, Failures: 0, Errors: 0, Skipped: 0**；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。
- 文档同步：`docs/design/order-and-cart.md`（新增「组合支付流程」+「组合订单退款拆分」章节，含 D1/D2/D3 rationale 与残留风险）、`docs/design/wallet-and-assets.md`（余额支付章节补组合抵扣 + CREATED 回冲 + 组合退款钱包部分回冲 + sourceType=order-refund taxonomy）。

- 关闭 deferred「验证码过期记录的定期清理」（源 `docs/plans/2026-06-13-reset-code-storage-plan.md:162`，触发条件 nop-job 已满足）。计划：`docs/plans/2026-06-29-2330-3-reset-code-periodic-cleanup-plan.md`。
- 实现：新增 `ILitemallResetCodeBiz.cleanupExpiredResetCodes` + `LitemallResetCodeBizModel.cleanupExpiredResetCodes` `@BizMutation`——读 `mall_reset_code_retention_days`（缺省 7 天）→ `CoreMetrics.currentDateTime().minusDays(retention)` cutoff → `doFindListByQueryDirectly`（`lt(addTime, cutoff)` + `eq(deleted, false)`，limit 500）→ 逐条 `deleteEntity`（逻辑删除，与 `sendResetCode`/`resetPassword` 惰性清理同语义）。
  - `app-mall-dao/.../ILitemallResetCodeBiz.java`、`app-mall-service/.../entity/LitemallResetCodeBizModel.java`
  - `MallJobInvoker.cleanupExpiredResetCodes()`（新增 `@Inject ILitemallResetCodeBiz resetCodeBiz`）
  - `scheduler.yaml` 注册 `cleanup-expired-reset-codes` 每日 job（86400000ms）
- 测试：`TestLitemallResetCodeBizModel` 5 个 IGraphQLEngine 测试（过期删除 / 保留期内保留 / 已删除不重复处理 / config 覆盖 / 单轮 500 上限）；`TestLitemallSchedulerJobInvoker` 补 `cleanupExpiredResetCodes` mock 断言。
  - 关键点：`LitemallResetCode.addTime` 为 createTimeProp，插入时自动置 now()。测试种子需 insert→flush→setAddTime(past)→updateEntity 在同一 `ormTemplate.runInSession` 内回拨时间（createTime 仅在 insert 自动设，update 保留回拨值）。
- 验证（全绿）：`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` EXIT=0；`./mvnw test -pl app-mall-service -am` **Tests run: 521, Failures: 0, Errors: 0, Skipped: 0**。
- 文档同步：`docs/design/user-and-address.md`（新增「密码重置与验证码」章节，惰性+定期清理互补）、`docs/design/system-configuration.md`（扩展运营任务清单 + 调度频率）、`docs/backlog/implementation-roadmap.md`（Phase 1 deferred 闭环标注）、源 plan deferred 条目标注「已由 successor 关闭」。
