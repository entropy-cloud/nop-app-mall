# Development Log - 2026-06-30

### 2026-06-30

- 交付 deferred successor「跨通道组合支付（余额抵扣 + 第三方通道补差，含安全退款）」。计划：`docs/plans/2026-06-30-0044-1-combo-payment-balance-plus-channel-plan.md`（源 P30 多支付通道 Deferred「跨通道组合支付（余额 + 第三方混合）」）。
- **Phase 1 后端组合支付核心 + 重入守卫 + CREATED 回冲：**
  - `app-mall-dao/.../ILitemallOrderBiz.java`：新增 `payWithCombo(orderId, useBalanceAmount, confirmCredential)` `@BizMutation` 声明。
  - `app-mall-service/.../entity/LitemallOrderBizModel.java`：实现 `payWithCombo`（Decision D1 先原子扣减余额部分 `debitBalance(debitAmount, PAY, sourceType=pay, orderSn)` → 写 `walletPayAmount`；remainder==0 退化全额余额 `payChannel=BALANCE`+`markOrderPaidCore`；remainder>0 `payService.createPayment(totalFee=remainder)`+设 payId，订单保持 101）。组合重入守卫 `rejectComboReentry`（walletPayAmount>0 抛 `ERR_ORDER_COMBO_PENDING`）落地于 pay/payByBalance/prepay/payWithCombo。CREATED 取消/超时余额回冲 `creditBackComboBalance`（cancel/cancelExpiredOrders CAS 守卫后 `creditBalance(walletPayAmount, REFUND, sourceType=order-refund, orderSn)`，复用既有 `SOURCE_TYPE_ORDER_REFUND`）。
  - `app-mall-service/.../AppMallErrors.java`：新增 `ERR_ORDER_COMBO_PENDING` / `ERR_ORDER_COMBO_AMOUNT_INVALID`。
- **Phase 2 后端组合感知退款拆分（资金安全）：**
  - `ILitemallOrderBiz.java`：新增 `refundComboAware(order, refundAmount, context)` 返回 `boolean`（app-mall-dao 不可见 PayRefundResponseBean，故用 boolean 保留各调用方域特定错误语义）。
  - `LitemallOrderBizModel.java`：实现 `refundComboAware`（Decision D3 比例分摊 walletPortion=refundAmount×walletPayAmount/actualPrice scale=2 HALF_UP，channelPortion=refundAmount−walletPortion 余数归通道；组合订单 creditBalance 钱包部分 + channelPortion>0 payService.refund 通道部分；普通订单零回归既有全额通道退款）。
  - 全站 5 退款站点经 `refundComboAware` 路由：`LitemallAftersaleBizModel.refund()`/`confirmReturnReceived()`（保留 `ERR_AFTERSALE_REFUND_FAILED`）、`cancelExpiredPickupOrders()` 非 BALANCE 分支（BALANCE 分支保留既有 creditBalance）、`LitemallGrouponBizModel.refundGrouponOrder()`、`LitemallPinTuanActivityBizModel.refundMemberOrder()`（团购/拼团保留 try-catch refundFailures）。移除 aftersale/groupon/pintuan 不再使用的 `PayService` 注入与 PayRefund*Bean 导入。
- **Phase 3 前端收银台组合支付 UI：**
  - `app-mall-web/.../mall/pay/pay.page.yaml`：余额通道与微信通道之间新增组合分支（visibleOn BALANCE 可用且 actualPrice>0）——展示可用余额 + 抵扣输入（input-number 默认 min(余额,actualPrice)，max 不得超余额/实付）+ 实时剩余 + 密码确认弹窗（复用 P30 登录密码样式）+ `@mutation:LitemallOrder__payWithCombo` + feedback（paid→成功跳订单详情 / !paid+codeUrl→微信补差 QR + 查看订单）。既有零金额/余额全额/微信全额分支零回归。
- **测试：** `TestComboPaymentBizModel` 10 例（成功组合/退化全额/余额不足/非法金额/凭证错误/组合重入/payByBalance 重入/prepay 重入/cancel 回冲/cancelExpiredOrders 回冲）；`TestComboRefundSplitBizModel` 5 例（整单拆分/部分分摊/普通零回归/全额余额 channelPortion=0/通道失败钱包仍回冲）。既有支付/售后/团购/拼团/自提测试零回归。
- 验证（全绿）：`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` EXIT=0；`./mvnw -pl app-mall-service test` **Tests run: 536, Failures: 0, Errors: 0, Skipped: 0**；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。
- 文档同步：`docs/design/order-and-cart.md`（新增「组合支付流程」+「组合订单退款拆分」章节，含 D1/D2/D3 rationale 与残留风险）、`docs/design/wallet-and-assets.md`（余额支付章节补组合抵扣 + CREATED 回冲 + 组合退款钱包部分回冲 + sourceType=order-refund taxonomy）。

- 关闭 deferred「验证码过期记录的定期清理」（源 `docs/plans/2026-06-13-reset-code-storage-plan.md:162`，触发条件 nop-job 已满足）。计划：`docs/plans/2026-06-29-2330-3-reset-code-periodic-cleanup-plan.md`。
- 实现：新增 `ILitemallResetCodeBiz.cleanupExpiredResetCodes` + `LitemallResetCodeBizModel.cleanupExpiredResetCodes` `@BizMutation`——读 `mall_reset_code_retention_days`（缺省 7 天）→ `CoreMetrics.currentDateTime().minusDays(retention)` cutoff → `doFindListByQueryDirectly`（`lt(addTime, cutoff)` + `eq(deleted, false)`，limit 500）→ 逐条 `deleteEntity`（逻辑删除，与 `sendResetCode`/`resetPassword` 惰性清理同语义）。
  - `app-mall-dao/.../ILitemallResetCodeBiz.java`、`app-mall-service/.../entity/LitemallResetCodeBizModel.java`
  - `MallJobInvoker.cleanupExpiredResetCodes()`（新增 `@Inject ILitemallResetCodeBiz resetCodeBiz`）
  - `scheduler.yaml` 注册 `cleanup-expired-reset-codes` 每日 job（86400000ms）
- 测试：`TestLitemallResetCodeBizModel` 5 个 IGraphQLEngine 测试（过期删除 / 保留期内保留 / 已删除不重复处理 / config 覆盖 / 单轮 500 上限）；`TestLitemallSchedulerJobInvoker` 补 `cleanupExpiredResetCodes` mock 断言。
  - 关键点：`LitemallResetCode.addTime` 为 createTimeProp，插入时自动置 now()。测试种子需 insert→flush→setAddTime(past)→updateEntity 在同一 `ormTemplate.runInSession` 内回拨时间（createTime 仅在 insert 自动设，update 保留回拨值）。
- 验证（全绿）：`./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` EXIT=0；`./mvnw test -pl app-mall-service -am` **Tests run: 521, Failures: 0, Errors: 0, Skipped: 0**。
- 文档同步：`docs/design/user-and-address.md`（新增「密码重置与验证码」章节，惰性+定期清理互补）、`docs/design/system-configuration.md`（扩展运营任务清单 + 调度频率）、`docs/backlog/implementation-roadmap.md`（Phase 1 deferred 闭环标注）、源 plan deferred 条目标注「已由 successor 关闭」。
