# 2026-06-15

## 计划执行：plan-closure-and-residual-cleanup-plan（三计划关闭 + 真实遗留项收尾）

执行 `docs/plans/2026-06-15-1324-plan-closure-and-residual-cleanup-plan.md`，补齐三个状态过期计划的真实缺口并独立 closure audit 关闭它们。

### Phase 1 — 关闭 `2026-06-13-adversarial-audit-remediation-plan`

补齐 2 项真实缺口 + 1 项 closure audit 发现的残留：

- **AR-18 gender 默认值**：`app-mall-delta/.../LoginApiExBizModel.java:125` 将注册时 `"gender", 1,`（男）改为 `"gender", 0,`（未知）
- **AR-10 claimCouponForUser 测试**：`app-mall-service/.../TestLitemallCouponUserBizModel.java` 新增 3 个测试方法（success/not-found/duplicate-rejected），通过 `ILitemallCouponUserBiz` 接口直接调用（因方法为 `@BizAction`，不走 GraphQL）
- **AR-23 extAction3 残留**：closure audit 发现 `app-mall-delta/.../NopAuthUser.xbiz` 仍含 `extAction3` 测试函数（其余子项已落地）。已移除整个 `<actions>` 块
- Closure audit：round 1 REVISE（1 major + 1 minor），round 2 PASS。两个 plan 文件均改写为 `completed` + 完整 closure evidence

### Phase 2 — 关闭 `2026-06-13-orm-index-and-entity-cleanup-plan`

补齐 Phase 3 DDL 索引传播（Phase 1/2 已落地）：

- **Decision**：选项 B（定向追加），避免全量 codegen 引入意外 diff。选项 A 作为 alternative 记录
- **DDL 索引追加**：向 `deploy/sql/{mysql,postgresql,oracle}/_create_app-mall.sql` 各追加 31 条 `CREATE INDEX` 语句，与 `model/app-mall.orm.xml` 的 31 个 `<index>` 定义一一对应。MySQL/Oracle 使用大写列名（USER_ID），PostgreSQL 使用小写（user_id）
- **Owner doc**：`docs/architecture/module-boundaries.md` 新增 "Deploy DDL vs ORM Model Sync Status" 段，记录索引同步状态、drift watch 触发条件、被删除的三个实体表
- Closure audit：REVISE（4 majors 均为 closure-recording gap：plan 状态未翻、owner doc 未更、dev log 未更、验证证据未记录），全部已补齐

### Phase 3 — 关闭 `2026-06-09-phase1-user-registration-login-plan`

Phase 0/1A/1B 此前已 `done`；Phase 1C/1D 标 `in-progress`/`planned` 但经 live-repo 核验所有子项均已落地：

- **Phase 1C 裁定**：NopAuthUser Delta view（`app-mall-delta/.../NopAuthUser.view.xml`）已 bounded-merge 显式列出 grid 列，password/salt/openId/deptId/workNo/position/tenantId 通过 `x:override="remove"` 移除；`signUp` 标 `@Auth(publicAccess=true)`，profile 方法默认要求登录。`./mvnw.cmd compile -DskipTests -pl app-mall-web -am` BUILD SUCCESS
- **Phase 1D 裁定**：全流程由 3 个测试类 15 个测试覆盖（`TestLoginApiSignUp` 注册/重复/空字段/注册后登录/禁用拦截、`TestNopAuthUserProfile` 查看/更新/改密、`TestPasswordReset` 重置流程）；owner docs 概念一致；roadmap Phase 1 已 `done`（本 closure 补正规化历史过程债）
- **历史过程债补正**：roadmap Phase 1 此前在未走 closure audit 情况下就标为 `done`，本次 closure audit 补齐证据链。无需追溯状态变更

### 整体验证（全绿）

- `./mvnw.cmd compile -DskipTests`：BUILD SUCCESS（10 模块，13.1s）
- `./mvnw.cmd test -pl app-mall-service -Dtest='TestLoginApiSignUp,TestNopAuthUserProfile,TestPasswordReset'`：Tests run: 15, Failures: 0, Errors: 0
- 三个原计划全部经独立 closure audit 验证为 `completed`

---

## 计划执行：storefront-extension-pages-plan（13 个前台扩展页面）

完成 `docs/plans/2026-06-15-1239-storefront-extension-pages-plan.md` 全部 Phase 1-6 + Phase Final。

### 交付内容

新增 13 个前台扩展 `.page.yaml` 页面（纯 AMIS YAML，无后端代码变更）：

- 搜索发现：`mall/search/search.page.yaml`
- 内容营销：`mall/topic/topic-list.page.yaml`、`mall/topic/topic-detail.page.yaml`
- 促销：`mall/coupon/coupon-center.page.yaml`、`mall/groupon/groupon-list.page.yaml`、`mall/groupon/groupon-rules-detail.page.yaml`、`mall/groupon/groupon-activity-detail.page.yaml`
- 用户互动：`mall/user/collect.page.yaml`、`mall/user/footprint.page.yaml`
- 售后：`mall/user/aftersale-list.page.yaml`、`mall/user/aftersale-apply.page.yaml`
- 客服支持：`mall/user/feedback.page.yaml`、`mall/help/faq.page.yaml`

配套改动：

- `app-mall.action-auth.xml`：在 `storefront` TOPM 下新增 13 个 SUBM 路由注册条目（orderNo 812-824）
- `home.page.yaml`：搜索框跳转改为 `/storefront-search`；导航栏新增领券/团购/专题入口；底部新增 FAQ/反馈入口
- `user-center.page.yaml`：修正"我的优惠券"占位链接（原指向 `/storefront-category`）；新增收藏/足迹/售后/团购/反馈菜单项；"继续购物"链接修正为 `/storefront-home`
- `checkout.page.yaml`：submit data 块新增 `grouponRulesId`/`grouponId` 字段，从 URL 参数透传给 `LitemallOrder__submit`（后端 `@Optional` 已支持，团购开团/参团经加购→结算流程触发）
- `e2e/tests/storefront-pages.spec.ts`：`STOREFRONT_PAGES` 数组扩展 13 个新页面

### 编码期发现并处理的 model-gap

**`LitemallGroupon` 无 `grouponRules` 关系**：计划原文假设团购活动详情页可用 `${groupon.grouponRules.goodsId}` 取规则关联商品。实际 ORM 中 `LitemallGroupon` 仅有 `order` 关系，无 rules 关系（`app-mall.orm.xml` L798-805）。处理：团购活动详情页改为两步——先 `grouponDetail(id)` 取得 `rulesId`，再 `LitemallGrouponRules__findList` filter by id 取得规则及 `goodsId`。属非阻塞 model-gap（运行时可通过 `I*Biz` + 客户端过滤替代，业务正确性不受影响），已记入 plan 的 `Deferred But Adjudicated` 区。触发条件：下次修改 `LitemallGroupon` 模型时补 `to-one rules` 关系。

### 验证（全绿）

- `./mvnw.cmd compile -DskipTests -pl app-mall-web -am`：BUILD SUCCESS（18.7s）
- `./mvnw.cmd clean package -DskipTests -pl app-mall-app -am`：BUILD SUCCESS（54.9s）
- e2e 套件：`37 passed (31.3s)`（原 24 + 新增 13 个 `PageProvider__getPage` 页面渲染冒烟用例）
- 编译期清理了一次 java 进程占用 `quarkus-run.jar` 导致的 fast-jar 构建失败

### 关键实现约定（与现有 storefront 计划一致）

- 公开页面调用 `@Auth(publicAccess=true)` 方法；认证页面（collect/footprint/aftersale/feedback 等）依赖登录态，未登录由 GraphQL 401 自然拒绝
- `recordSearch(keyword, from)` 的 `from` 固定传 `"search"`
- 团购流程：前端不直接调用 `openGroupon`/`joinGroupon`，改为加购 → 结算透传 URL 参数 → 后端 `submit` 内部触发
- 收藏/足迹列表项需嵌套 `frontDetail` 查询补全商品图名价（实体本身只存 `valueId`/`goodsId`）
- 售后 `userList` 返回 `List` 非 `PageBean`，前端按全量渲染
- owner docs（`docs/design/app-overview.md` 界面列表）已按业务域归并补充
