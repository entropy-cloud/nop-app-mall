# Development Log - 2026-06-27

### 2026-06-27 — Phase 34 首页运营打标（Homepage Operation Tagging）全量交付

- 完成 enhanced-roadmap Phase 34（首页运营打标）全部 3 个 Phase，计划 `docs/plans/2026-06-27-2029-3-phase34-homepage-operation-tagging-plan.md`。本批次（2026-06-27-2029）第 3 顺位收尾，独立轻量运营增强，不阻塞也不依赖其他 phase。**不触及** `LitemallOrderBizModel.submit()` 价格构成（标记位与促销活动互不干扰）。
- **业务设计（Phase 1，docs-only）：** `docs/design/product-catalog.md` 首页运营打标章节订正命名漂移——新增「字段映射」表（设计语义→模型字段 isNew/isHot/isRecommend + propId 13/14/22），三标记位命名对齐模型实际；补推荐位业务规则（默认 false / isOnSale 约束 / 为你推荐第三楼层 / frontListByFlags 三标记过滤）。两个 Decision：①三字段复用裁决（isNew/isHot/isRecommend 均已建模，否决按 roadmap 重命名为 newFlag/hotFlag/recommendFlag，避免无谓回归）；②isRecommend 接线范围（唤醒 dormant 字段，与既有标记位语义对称）。
- **后端（Phase 2）：** `ILitemallGoodsBiz.frontListByFlags` 接口先声明 `@Optional @Name("isRecommend") Boolean isRecommend`（接口先声明遵循项目约定，避免 7 参接口未实现编译失败），`LitemallGoodsBizModel` impl @Override 同步入参 + isRecommend 过滤（参照既有 isNew/isHot 实现）；`frontList` 包装器 positional 调用同步新签名 `frontListByFlags(null,null,null,...)`（机械改动，无行为变化）。**API 向后兼容**（@Optional 省略即不过滤，既有调用方零回归）。无模型改动、无 ErrorCode 新增、无事务变化。selfcheck 通过。
- **前端（Phase 2）：** `home.page.yaml` 新增「为你推荐」第三楼层（调 `@query:LitemallGoods__frontListByFlags` 传 isRecommend=true，参照新品/人气楼层结构 pageSize=8）；后台 `LitemallGoods.view.xml`（保留层）全程暴露 isRecommend——grid `bounded-merge` cols 加 `<col id="isRecommend"/>`（白名单，与 isNew/isHot 并列）、edit form layout 加 `isRecommend[是否推荐]`、query form layout 加三标记位过滤（`id goodsSn keywords isNew isHot isRecommend`）。未改 `_gen`，复用既有 AMIS 三层定制模式。
- **验证（Phase 3）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **176 测试全绿**（含 `TestLitemallGoodsExtendedBizModel` 新增 `testFrontListByFlagsRecommend` 1 例：isRecommend=true 命中 / 省略 isRecommend 向后兼容 / isRecommend+isHot 组合过滤排除仅 isRecommend 项）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。roadmap stale 两处订正（`:435` 交付范围 + `:584` 扩展字段表，均改述为「三字段均已存在，P34 仅接线启用」）。
- 已知约束：批量打标/取消运营（属 P36 商品运营增强）、首页楼层 DIY 运营化排序（roadmap 明确单独规划）在 Non-Goals 移出。

### 2026-06-27 — Phase 23 限时折扣（Time-Limited Discount）全量交付

- 完成 enhanced-roadmap Phase 23（限时折扣）全部 4 个 Phase，计划 `docs/plans/2026-06-27-2029-2-phase23-time-discount-plan.md`。本批次（2026-06-27-2029）第 2 顺位，裁决 P15 满减遗留的「限时折扣与满减 promotionPrice 槽位共存」问题。
- **业务设计（Phase 1，docs-only）：** `docs/design/marketing-and-promotions.md` 新增「限时折扣 / Time-Limited Discount」完整章节（价格层位裁决、discountType 计算、×vipPrice 取低、×满减共存、stockLimit 强制、多重折扣互斥、状态语义、倒计时/剩余库存展示）；`docs/design/order-and-cart.md` 订正 promotion price 段（限时折扣移至商品单价层，promotionPrice 槽位仅满减）**且 goods price 公式由 `min(retail,vip)` 扩为 `min(retail,vip,timeDiscountPrice)`** + 计算顺序补限时折扣单价层 + 跨域引用表加限时折扣构件；`docs/design/product-catalog.md` 新增「详情页限时折扣展示」规则（促销横幅/倒计时/剩余库存/与满减并存）。六个 Decision 落定：①价格层位=商品单价层（goodsPrice 汇总，不复用 promotionPrice，消解 P15 遗留共存点）；②discountType 对齐 P15 小数支付率编码（0.9=9折）；③×vipPrice 取三者最低；④×满减天然共存（折扣后 goodsPrice 判档）；⑤stockLimit>0 原子扣减；⑥多重折扣取最优价 + 最新 startTime（无 priority 字段，不新增模型字段）。
- **后端（Phase 2）：** `LitemallTimeDiscountBizModel.selectTimeDiscountForProduct`（`@BizQuery`，返回结构化 Map：promoPrice/originalPrice/discountAmount/discountType/endTime/stockLimit/remainingStock；查 ACTIVE+时间窗内折扣，productId 精确匹配优先 null 兜底全 SKU，computePromoPrice 对齐 P15 打折=retail×支付率/直降=retail-额，校验 promoPrice>0 且 <retail，多折扣取最低价 tiebreak 最新 startTime；纯计算，扣减在 submit）；`LitemallOrderBizModel.submit` 接线限时折扣（goodsPrice 汇总时 min(retail,vip,timeDiscountPrice)，限时折扣不进 promotionPrice）；新增 `LitemallTimeDiscountMapper.reduceTimeDiscountStock`（`@SqlLibMapper` 条件 UPDATE `stockLimit >= num` 原子扣减，售罄返回 0）+ 注册到 `app-dao.beans.xml`；submit 对 stockLimit>0 折扣扣减，售罄抛 `ERR_TIME_DISCOUNT_SOLD_OUT`（事务回滚保证一致）。`AppMallErrors` 新增 time-discount 域 4 个 ErrorCode。Protected Area（ORM `PROMOTION_PRICE` comment 订正为「满减优惠金额」移除「限时折扣」）已更新，comment 不驱动 codegen 无需重生成。selfcheck 通过。
- **前端（Phase 3）：** `goods-detail.page.yaml` 新增 `timeDiscount` service 调 `selectTimeDiscountForProduct`，命中展示促销横幅（划线原价 + 促销价 + 直降金额 + 倒计时 endTime + 剩余库存），无折扣 visibleOn 隐藏，与满减横幅并存；`search.page.yaml` 列表卡片加 `timeDiscount` service（每卡查该商品折扣，命中展示「限时价 ¥X」+ 限时折扣标签，零售价始终展示兜底）；后台 `LitemallTimeDiscount.view.xml` 定制（grid bounded-merge + edit form 含折扣字段占位提示 productId null=全SKU/stockLimit 0=不限/discountValue 折扣率说明 + dict 自动渲染 discountType/status 下拉 + drawer 编辑 + query 按 goodsId/status 过滤）。
- **验证（Phase 4）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **175 测试全绿**（含新增 `TestLitemallTimeDiscountBizModel` 11 例：打折/直降/草稿/超时窗/未来窗/多折扣取低/productId 匹配与兜底/超折扣无效/stockLimit 与倒计时 + `TestLitemallOrderBizModel` 扩展至 22 例：限时折扣单价/直降/×满减共存/stockLimit 售罄拒绝与扣减 6 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS（含 view.xml/page.yaml 资源经 precompile 解析通过）。
- 已知约束：秒杀（P24，场次/限购/不走购物车）、拼团（P25）、营销活动管理后台（P22）在 Non-Goals（独立 phase）；stockLimit=0 表示不限，扣减后 depleted-to-0 与 configured-unlimited 在存储值上不可区分，受限「不新增字段」约束接受此残留风险（运营配置余量 + 活动 endTime 约束暴露面）。

### 2026-06-27 — Phase 27 积分体系（Points System）全量交付

- 完成 enhanced-roadmap Phase 27（积分体系）全部 4 个 Phase，计划 `docs/plans/2026-06-27-2029-1-phase27-points-system-plan.md`。本批次（2026-06-27-2029）第 1 顺位，解除 P28（签到得积分）阻塞。
- **业务设计（Phase 1，docs-only）：** `docs/design/wallet-and-assets.md` 积分账户章节扩展（并发安全原子操作、changeType×sourceType 分类法、有效期策略标为计划中、获取/抵扣/返还语义）；`docs/design/marketing-and-promotions.md` 新增「积分体系」章节（获取规则/抵扣规则与上限/取消退款返还/商城兑换交接）；`docs/design/order-and-cart.md` integralPrice 段补抵扣规则与计算顺序 + 部分退款积分返还规则。七个 Decision 落定：获取主触发源=购物赠送（confirm 时按 earn_per_yuan×actualPrice，否决支付即赠送防刷分）；抵扣=N/ratio 受 orderPrice×max_ratio 上限；原子操作=乐观锁 version 字段；返还=订单级返还+赠送不追回（同 P16 券对称）；有效期=不实现（model-gap）；规则存放=NopSysVariable 不新增实体；changeType×sourceType 分类法（复用字典三值 EARN/SPEND/EXPIRE，细分来源落 sourceType，使「不改模型」自洽）。
- **后端（Phase 2）：** `LitemallPointsAccountBizModel` earn/spend（`@BizMutation`，乐观锁 `LitemallPointsAccountMapper.updateBalanceIfVersion` 条件 UPDATE + 流水 balanceAfter 快照，spend 校验余额非负，账户不存在自动创建；参照 useCoupon/cancelExpiredOrders 的 flushSession+clearEntitySessionCache 模式使 EQL UPDATE 走真实 SQL）、getMyPoints（`@BizQuery`）、adjustPoints（`@BizMutation @Auth(roles=admin)`）；`LitemallPointsFlowBizModel.getMyPointsFlows`（`@BizQuery`，changeType/sourceType 筛选）；`LitemallOrderBizModel.submit` 加 `usePoints`（`@Optional`，向后兼容）→ computeIntegralPrice（actualPrice 减项层，受 cap 约束，超额抛 ERR_POINTS_DEDUCT_EXCEED_LIMIT）+ spendPoints；`confirm`/`confirmExpiredOrders` 触发购物赠送（幂等 sourceType+sourceId）；`cancel`/`cancelExpiredOrders`/`aftersale.refund`(whole-order)/`groupon.refundGrouponOrder` 返还抵扣积分（item 级部分退款不返还，同券对称）。`AppMallErrors` 新增 points 域 5 个 ErrorCode。selfcheck 通过。
- **前端（Phase 3）：** `mall/points/my-points.page.yaml`（余额大数字 + 全部/获取/消耗三 tab 流水，changeType/sourceType mapping；注册路由 `/storefront-my-points` orderNo 826 + user-center 入口）；`checkout.page.yaml` 加 `pointsService`+`usePoints` input-number（visibleOn 余额>0，max=余额，描述含比例/上限），submit 传 usePoints；`LitemallPointsAccount.view.xml`（grid bounded-merge + 手工调账 listAction dialog/form 调 adjustPoints）+ `LitemallPointsFlow.view.xml`（grid 含 sourceType/sourceId/balanceAfter 等审计列，drawer 编辑）。
- **验证（Phase 4）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **159 测试全绿**（含新增 `TestLitemallPointsAccountBizModel` 7 例 + `TestLitemallOrderBizModel` 扩展至 17 例：积分抵扣/超上限拒绝/确认收货赠送/取消返还 4 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。
- 已知约束：积分商城兑换/签到评价分享得积分/积分有效期均为 Non-Goals（model-gap 或属 P28/P33）；购物赠送幂等为应用层查重（无 DB 唯一键，Deferred model-gap）；earn/spend 作为复用基座被 P28/P33 后续接入。

### 2026-06-27 — Phase 16 订单项级售后增强（Order-Item-Level Aftersale）全量交付

- 完成 enhanced-roadmap Phase 16（订单项级售后增强）全部 5 个 Phase，计划 `docs/plans/2026-06-27-1742-3-phase16-order-item-aftersale-plan.md`。本批次（2026-06-27-1742）第 3 顺位收尾，P15/P26 前置已 done。
- **业务设计（Phase 1，docs-only）：** `docs/design/order-and-cart.md` 售后章节升级为 OrderGoods 项级（粒度/类型-状态映射表/多项并发互斥/退款额上限/部分退款三副作用/aftersaleStatus 派生/时间线/通知去重）；`docs/design/system-configuration.md` 新增「字典维护 - 售后原因字典」段。八个 Decision 落定：①落地=单表+既有 orderItemId；②并发=单项单进行中终态后可再申；③item 上限=number×price（P15 已确认 promotion/coupon 订单级未分摊到行，actualPayAmount 未写入）；④aftersaleStatus=派生（订正审计 round-3 误判：该列在 LitemallOrder 非 OrderGoods）；⑤副作用=全部退款才进 203/仅退该项还库/部分不恢复券；⑥类型按状态映射；⑦orderItemId=null 整单兼容；⑧通知订单级去重。
- **模型（Phase 2，Protected Area ask-first）：** `model/app-mall.orm.xml` 新增 `mall/aftersale-reason` 字典（valueType=string，6 选项）+ `LitemallAftersale.reason` 列 `ext:dict` 绑定。按 Phase 1 Decision 不新增列/关系（aftersaleStatus 派生、orderItemId 既有）。人工确认链=独立 3 轮审计通过 + roadmap 授权 + 用户 mission 显式指令 + P15/P26 同模式先例（d95d77f/918cfff）。codegen 重生成 `_AppMallDaoConstants`（AFTERSALE_REASON_* 常量）/`_app.orm.xml`/`aftersale-reason.dict.yaml`，dao 编译通过。
- **后端（Phase 3）：** `LitemallAftersaleBizModel` 改造为 item 级——apply 校验 orderItemId 归属/类型-状态映射/多项并发/退款额上限/reason 字典；refund 部分退款副作用（全部退款才进 203、仅退该项还库、整单才恢复券）+ 写 processTime/processNote 时间线；order.aftersaleStatus 转 aggregate 视图（recomputeOrderAftersaleStatus，`orm().flushSession()` 解决 flush 时序）；batchApprove/batchReject/cancel 同步聚合视图。`AftersaleApplyRequest` 加 orderItemId（公开契约变更，缺省整单兼容）。`AppMallErrors` 新增 4 个 aftersale 域 ErrorCode。selfcheck 通过（#18 CoreMetrics.currentDateTime）。
- **前端（Phase 4）：** `order-detail.page.yaml` 按 OrderGoods 行加「申请售后」入口（visibleOn 201/401/402；dialog form 提交 apply，含 orderId/orderItemId + type/reason 字典 select/amount/comment）；后台 `LitemallAftersale.view.xml` grid bounded-merge 突出 orderItemId/item 金额/时间线列 + view 表单重排为时间线语义。
- **验证（Phase 5）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **148 测试全绿**（`TestLitemallAftersaleBizModel` 扩展至 12 例：item 申请/退款/互斥/类型映射/字典校验/不属于订单/部分退款保持 PAY+单项还库/全部退款转 203 + 整单兼容/超额/取消聚合）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。
- 已知约束：满减/券折扣未分摊到行，故 item 退款额上限（number×price）可能略高于理论实付分摊，由运营审核把关；退货入库/物流回寄在 Non-Goals（owner doc 已界定）；item 退款通知保持订单级去重（多次退款仅一条通知，明细在订单详情核对）。

### 2026-06-27 — Phase 26 会员等级体系（Member Level System）全量交付

- 完成 enhanced-roadmap Phase 26（会员等级体系）全部 5 个 Phase，计划 `docs/plans/2026-06-27-1742-2-phase26-member-level-system-plan.md`。解除 P27（积分体系）阻塞。
- **业务设计（Phase 1）：** `docs/design/user-and-address.md` 新增「会员等级体系」章节（等级模型、升级指标=累计消费、降级机制=周期内未达标逐级降、vipPrice 价格影响、权益配置预留、个人中心展示）；`docs/design/order-and-cart.md` goods price 定义补 vipPrice。三个 Decision：升级指标=累计消费（不加 growthValue）；vipPrice=SKU 单价层 + OrderGoods 快照备选 B（单一 vipPrice 字段）；降级=机制 A 周期内未达标降一级。
- **模型（Phase 2）：** `model/app-mall.orm.xml` 新增 `LitemallMemberLevel` 实体（level/name/upgradeThreshold/downgradeThreshold/benefits(JSON)/sortOrder + level 唯一键）；`LitemallGoodsProduct` 加 `vipPrice`；`LitemallOrderGoods` 加 `vipPrice` 快照。codegen 重生成（entity/IBiz/BizModel 脚手架/API bean/xmeta/view 骨架/deploy/sql 三方言 DDL）全模块编译通过。
- **后端（Phase 3）：** `LitemallMemberLevelBizModel` 实现等级评估（`evaluateUserLevel`/`evaluateMyLevel` `@BizMutation`，累计消费=已支付/已收货订单 actualPrice 求和）、进度查询（`getMyLevelProgress` `@BizQuery`）、降级（`downgradeExpiredLevels` `@BizMutation`，周期 `mall_member_eval_period_days` 默认 365，保级阈值=当前等级 downgradeThreshold，未达标降一级普通为底）；`LitemallOrderBizModel.submit` 接 vipPrice（会员 userLevel>=1 按 `min(retail,vip)` 计 goodsPrice，快照 vipPrice 到 OrderGoods）。NopAuthUser 跨实体访问因 `INopAuthUserBiz` 在 test-scoped 的 app-mall-delta，按 AGENTS.md 用 `IOrmTemplate`（单实体 get）+ `CrudBizModel.daoProvider()`（批量）fallback，userLevel 经 `IOrmEntity.orm_propValueByName` 访问。`AppMallErrors` 新增 member-level 域 4 个 ErrorCode。xmeta 限制（userLevel 仅 eq/in、时间字段无 ge）已在 Java 侧规避。
- **前端（Phase 4）：** `mall/user/user-center.page.yaml` 新增 memberLevel service 调 `getMyLevelProgress` 展示当前等级+距下一级金额+累计消费；后台 `LitemallMemberLevel.view.xml` 定制（grid/edit/query/CRUD drawer）；`LitemallGoodsProduct.view.xml` ref-edit grid 新增 vipPrice 列。
- **验证（Phase 5）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar -T 1C` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **141 测试全绿**（含新增 `TestLitemallMemberLevelBizModel` 6 例 + `TestLitemallOrderBizModel` vipPrice 2 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。
- 已知约束：权益发放（专属券/生日礼包/专享客服）为 Non-Goals（依赖 P8+P32），仅在 benefits JSON 预留；降级周期重置因 nop-job 未引入走手动 BizMutation，未来由定时任务复用同一方法。

### 2026-06-27 — Phase 15 满减送（Full-Discount Promotion）全量交付

- 完成 enhanced-roadmap Phase 15（满减送）全部 4 个 Phase，计划 `docs/plans/2026-06-27-1742-1-phase15-full-discount-promotion-plan.md`。
- **业务设计（Phase 1）：** `docs/design/marketing-and-promotions.md` 新增「满减送 / Full-Discount Promotion」完整业务设计（多档自动最优、商品范围匹配、叠加策略、活动状态语义、取消/退款语义）；`docs/design/order-and-cart.md` 价格构成补 `promotionPrice` 构件与计算顺序约定（先会员价 goodsPrice → 再判满减门槛）；删除「满减不在范围内」过时陈述。价格公式：`orderPrice = goods + freight - coupon - promotionPrice`（promotion 与 coupon 同处 orderPrice 减项层，actualPrice 公式不变）。
- **后端（Phase 2）：** `LitemallPromotionActivityBizModel.selectPromotionForOrder`（`@BizQuery`，纯计算：查 ACTIVE+时间窗内活动、校验 goodsScope/goodsScopeValue、按 priority 选活动+最优档位、discountType 减金额/打折）；`LitemallOrderBizModel.submit` 接线 promotionPrice + 叠加守卫（配置 `mall_promotion_coupon_stacking`，默认允许）；`AppMallErrors` 新增 promotion 域 4 个 ErrorCode（STACKING_NOT_ALLOWED 用于 submit）。Protected Area（ORM `ORDER_PRICE` comment）已更新；ORM `comment` 为元数据不影响生成实体，无需重生成。xmeta 时间字段仅允许 eq/in/dateBetween/dateTimeBetween，满减时间窗判定在 Java 侧完成。
- **前端（Phase 3）：** `checkout.page.yaml` 动态满减预览（`promotionService` + visibleOn>0 + 应付减 promotion，替换硬编码 -¥0.00）；`goods-detail.page.yaml` 满减摘要；后台 `LitemallPromotionActivity.view.xml`（grid bounded-merge + edit form tiers `input-table` 子表 + dict 下拉 + drawer 编辑 + query 过滤）。
- **验证（Phase 4）：** `./mvnw clean install -DskipTests -Dquarkus.package.type=uber-jar` BUILD SUCCESS；`./mvnw test -pl app-mall-service -am` **133 测试全绿**（含新增 `TestLitemallPromotionActivityBizModel` 7 例 + `TestLitemallOrderBizModel` 满减/叠加 3 例）；`./mvnw -pl app-mall-web -DskipTests compile` BUILD SUCCESS。
- 已知约束：`maxPerUser` 强一致计数属 model-gap（无 promotion-usage 实体），本计划保留字段不强执行（见计划 Deferred 项）；结算页满减预览按 goodsPrice 匹配 ALL 范围，下单以服务端权威计算为准。

### 2026-06-27 — 修复 mission-driver CHECK 暴露的测试失败（promotionPrice / picUrl / 过期截止时钟）

- mission-driver `CHECK` 步骤（build agent，GLM-5.2）卡在 `mvn test`，超 23 分钟未决。根因是 commit `57a7784` 之前 build 一直损坏（codegen 引用已删除的 `LitemallUser`），掩盖了一批真实测试失败。build agent 修好 build 后暴露出测试失败，但在「提取 opaque `ApiResponse` 错误」上反复编译跑测试，效率低。
- 杀掉 build agent 后定位修复，共修两类问题，全部测试绿（`Tests run: 123, Failures: 0, Errors: 0`，`mvn test` BUILD SUCCESS）：
  - **`promotionPrice`/`pinTuanPrice` 非空**：`57a7784` 给 `LitemallOrder` 加了 `mandatory=true` 的两个新价格字段，但 `submit` 与若干测试 fixture 未初始化。build agent 已修 `submit` 与 `TestLitemallGroupon{,Expire}` fixture（本日志记录现状，非本次改动）。
  - **`OrderGoods.picUrl` 非空**（本次修复）：`domain="image"` 列存的是文件引用 `/f/download/{fileId}`（见 `DaoResourceFileStore.getFileLink`），不是普通 URL。测试 fixture 把 cart/orderGoods 的 picUrl 设成 `http://test.com/xxx.png`，导致 `OrmFileComponent.copyFrom`（`source.getFileId()` 经 `decodeFileId` 解析失败返回空）静默不赋值，save 时撞 `mandatory`。已将 4 个测试文件的 picUrl 改为对应的 `/f/download/{fileId}`：`TestLitemallOrderBizModel`、`TestLitemallAftersaleBizModel`、`TestLitemallCommentBizModel`（2 处）、`TestLitemallGrouponBizModel`。
  - **过期截止时钟不一致**（本次修复，产品级 bug）：`cancelExpiredOrders`/`confirmExpiredOrders` 用 `LocalDateTime.now()`（系统时钟）算 cutoff，但实体的 `addTime`/`shipTime` 由 ORM `createTime` 域经 `CoreMetrics.currentTimeMillis()`（autotest 下为 `TestClock`）写入，两套时钟可能错位导致 `addTime > cutoff` → 批量过期查不到单（`count=0`）。`TestClock` 与系统时钟实测偏差约 4s。已改用 `CoreMetrics.currentDateTime()` 与 ORM 同源，符合 AGENTS.md「用 `CoreMetrics` 而非 `System`/`LocalDateTime.now`」的规则。同时 cutoff 过滤由 `lt` 改为 `le`（创建时刻恰等于超时阈值也算过期，符合 job/retry 模块的包含式 cutoff 约定）。
- 改动文件（仅逻辑文件）：`app-mall-service/.../LitemallOrderBizModel.java`（+`CoreMetrics` import，2 处 cutoff）、4 个测试 fixture。git status 中大量 `_gen/*`、`deploy/sql/*`、`_templates/*` 改动为 codegen 模板变化的重生成噪音，非本次逻辑改动。

### 2026-06-27

- 从远程master分支同步代码（SHA: 35d90f2）
- 使用`git ls-remote`无缓存检查，确保获取最新远程状态
- 本地master分支已更新到远程最新版本
- 创建AGE模板符号链接：`../attractor-guided-engineering-template` → `/Users/abc/app/attractor-guided-engineering-template`
- 确保mission-driver使用AGE模板中的实现（路径配置正确）