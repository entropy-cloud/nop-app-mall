# Development Log - 2026-06-16

### 2026-06-16（Plan 1 Phase 3 + Phase 5 实施完成：退款状态机/事务边界/DDL 索引）

- 执行 Plan 1 Phase 3（退款金额校验与状态机闭环），闭环 5 个审计发现：P0-4 退款金额无校验、09-06 订单 202/203 死代码、01-6 售后退款不归还券、05-2/05-3 事务边界/吞异常、09-13 通知在事务内
- 改动：`LitemallAftersaleBizModel.apply`（金额校验 amount>0 且 ≤actualPrice）/`refund`（防御再校验 + PAY→203 状态推进 + returnCoupon + notification afterCommit）；`LitemallOrderBizModel`（submit/pay/ship 三处 notification → `txn().afterCommit`）；`LitemallGrouponBizModel`（refundGrouponOrder 收集失败列表+汇总告警 + notification afterCommit；expireGroupons 传失败列表）；新增 `ERR_AFTERSALE_AMOUNT_EXCEED`
- 关键 Decision：**payService.refund 保留 @BizMutation 事务内**（资金耦合，throw→回滚保证 DB/外部一致 + outRefundNo 幂等）；**只有 notificationService 移 afterCommit**（fire-and-forget）。驳回"全部移 afterCommit"方案——会导致外部退款失败时 DB 已提交、钱未退的资损。调和了审计 05-2 与 05-3 的表面互斥
- 验证：新增 `testApplyAmountExceedsActualPrice`（amount=99999/0 均拒绝）；Order/Aftersale/Groupon/Cart/CouponUser **33 测试全过，零回归**；package BUILD SUCCESS
- 执行 Plan 1 Phase 5（DDL 索引同步）：从 ORM 解析 31 索引生成 `deploy/sql/{mysql,postgresql,oracle}/_create_index.sql`（mysql/oracle 大写、pg 小写，各 31 CREATE INDEX），独立文件防 regen 覆盖；修正 `module-boundaries.md` "31 index 已同步 DDL"虚假声明（实际根因：平台 ddl.xlib CreateTables 不调 AddIndex）
- owner doc：`order-and-cart.md` 退款状态机加实现说明（未发货售后退款 201→203、202 暂无写入点、团购超时 201→204）
- Skill：复用 nop-backend-dev/nop-testing/nop-orm-modeler；逐方法 selfcheck；refund/batchApprove/batchReject 未同步 ILitemallAftersaleBiz 接口是预存在债务（审计 05-1），本 Phase 仅改 refund body 未扩接口
- **Plan 1 剩余 Protected Area 待 human approval**：Phase 1（支付回调推进 + pay 凭证校验，project-context ask-first）、Phase 4（goods→orderGoods cascade 移除，ORM AI 阻断）

### 2026-06-16（Plan 1 Phase 2 实施完成：库存完整性）

- 执行 `docs/plans/2026-06-16-1800-p0-fundamental-integrity-fix-plan.md` Phase 2（库存扣减与回滚完整性），闭环 3 个审计 P0/P1：P0-3 超卖、P0-5 GOODS_MISS 未发货退款不回滚库存、11-2 cancel/定时并发双倍回滚
- 改动：`LitemallOrderBizModel.submit`（reduceStock 返回值校验防超卖）、`cancel`/`cancelExpiredOrders`（条件 UPDATE 原子状态转移）、`LitemallAftersaleBizModel.refund`（库存回滚条件改 `orderStatus==PAY || type==GOODS_REQUIRED`）、新增 `LitemallOrderMapper.updateStatusIfMatch` + `LitemallOrder.sql-lib.xml` EQL
- **关键技术发现（记入 lessons 候选）**：对 Nop **session-managed 实体**做 EQL 条件 UPDATE（`<eql sqlMethod="execute">`）会被路由到内存实体而非执行 SQL，返回 affected=0。正确模式：(1) 单实体场景**先条件 UPDATE 后 `get()` 载入**（cancel）；(2) 批量场景**收集 ID → `dao().clearEntitySessionCache()` 解托管 → 逐 ID 条件 UPDATE → 载入**（cancelExpiredOrders）
- Decision：cancel 并发治理锁定**条件 UPDATE 选项 A**（选项 B version 列触发 ORM Protected，驳回）
- 验证：新增 `testCancelExpiredOrders`（断言 count≥1 + order→103）；Order/Aftersale/Cart/Groupon 22 测试全过；**10 个 auth 失败经 git stash 确认为预存在失败**（审计 02-01 userType 字典冲突，clean baseline 同样失败 `非法的字典项:0`），与本 Phase 无关。ExecutorService 真并发压测 Deferred（需独立并发测试基建）
- Skill：`nop-backend-dev`（读 concurrency-and-transactions/service-layer/selfcheck/safe-api）+ `nop-testing`；逐方法 selfcheck
- Protected Area：Phase 1（支付 ask-first）、Phase 4（ORM cascade）仍待 human approval；Phase 2 非 Protected

### 2026-06-16（基于审计拟制 3 个修复计划）

- 基于多维审计 132 条发现，按风险层级拟制 3 个开发计划（每个 = 一个结果表面，独立闭合）：
  - `docs/plans/2026-06-16-1800-p0-fundamental-integrity-fix-plan.md`：资金链路与数据完整性 P0 修复（7 根因：回调推进/pay 凭证/超卖/退款金额/GOODS_MISS/cascade/DDL 索引 + 紧耦合状态机闭环）。**含 Protected Area**（Phase 1 支付 ask-first、Phase 4 ORM cascade）
  - `docs/plans/2026-06-16-1800-p1-domain-logic-hardening-plan.md`：电商域逻辑加固（并发治理/越权收口/优惠价格校验/团购状态机/前端呈现）。**含 Protected Area**（Phase 1/3/4 ORM 变更）
  - `docs/plans/2026-06-16-1800-p1-governance-credibility-plan.md`：治理可信度（闭合审计回补/技能真值源/文档批量刷新/Phase 11-13 状态归真/测试基线）
- Plan audit：3 轮对抗式独立 subagent 审计。Plan1/Plan2 经 3 轮达 Round3 连续 clean（0 blocker/0 major），Plan3 经 2 轮达 Round2 clean。关键修订：Plan1 调和 05-2/05-3 事务边界架构互斥（payService 保留事务内、只 notification 移 afterCommit）；Plan2 补全 ORM Protected Area 标注；Plan3 修正 Phase1 baseline（LitemallStatBizModel 幻影类名，多数方法已有 RPC 测试，真实缺口收窄为 MallNotificationService）
- 下一步：Protected Area 项需 human approval 双解锁后方可 implementation；implementation 前需独立专项复核 7 个 P0 根因（审计 summary 自评建议）

### 2026-06-16（多维深度审计执行）

- 执行全项目多维深度审计（全量 13 维度），归档 `docs/audits/2026-06-16-1744-multi-dim-audit-full-project/`（13 维度文件 + summary.md）。依据 `docs/skills/multi-dimensional-audit-prompt.md`；基线编译 BUILD SUCCESS、roadmap 14 阶段全 done
- 统计：132 条发现（P0×10 去重为 7 根因 / P1×64 / P2×41 / P3×17）。主 agent 对全部 P0 直读源码独立复核成立
- P0 根因（投产阻塞）：微信回调不推进订单状态（WxPayNotifyResource 丢弃 outTradeNo）、pay() 无凭证校验、reduceStock 返回值未检查致超卖、售后退款金额无校验、GOODS_MISS 未收货退款不回滚库存、DDL 三方言缺全部 31 索引（module-boundaries "已同步"虚假，根因平台 ddl.xlib CreateTables 不调 AddIndex）、goods↔orderGoods 双向 cascadeDelete
- 系统性问题三层面：(1) 支付/资金链路不可靠；(2) 并发治理全面缺位（全仓无 version 乐观锁）；(3) "已完成"状态可信度问题（Phase 11/13 部分完成却标 done、2 个计划闭合审计虚假勾选、高频 context 文档批量漂移）
- 维度评级：09 电商域逻辑 Poor、13 文档一致 Poor，其余 Moderate；综合判断"工程骨架完整但不宜直接投产"
- 审计盲区：未跑 mvnw test（采信日志绿）、Round 2 深挖未执行、平台层根因未深追、并发缺陷未压测、整体结论未经第二独立 subagent 复核（建议对 7 个 P0 根因做独立专项复核后再启动修复）
- Skill：本次为审计执行，未涉及代码生成；多维审计 prompt 手册作为方法路由

### 2026-06-16

- 执行 `docs/plans/2026-06-16-1000-storefront-pay-flow-plan.md`（前台支付流程闭环）。Phase 14 微信支付后端已就绪，本计划交付前台唯一支付消费者页面 + 4 处入口接线 + 路由注册，使 prepay/codeUrl/轮询对前台可达
- 新建 `app-mall-web/.../_vfs/app/mall/pages/mall/pay/pay.page.yaml`（路由 `/storefront-pay`）：订单加载 → 零金额/非零金额/异常三分支路由；非零分支 `prepay` 获取 `codeUrl` → amis 原生 `qr-code` 渲染二维码 → 轮询 `queryPayment(outTradeNo=orderSn)`；保留「模拟支付完成」按钮（示例模式 `enabled=false` 必备）
- Decision：二维码渲染采用 amis 原生 `qr-code` 渲染器（`amis-react19/.../renderers/QRCode.tsx`，alias `qr-code`/`qrcode`）——纯前端、零新依赖、无后端端点。经 `QRCode.tsx` 源码核验 props（`value`/`codeSize`/`level`）
- Decision：模拟支付轮询停止采用 `actionType: ajax` + `redirect`（跳转卸载组件停止 interval，项目已验证模式 `checkout.page.yaml:197`），替代计划原述的 `paidLocally`+onEvent+setValue 链——理由：amis 无 `goto` action，项目无 onEvent 先例，redirect-unmount 同样满足「至少一种停止机制」且更可靠。真实模式仍用 `stopAutoRefreshWhen: tradeState==SUCCESS`
- 接线 4 处「立即付款」按钮从 `pay` ajax 改为跳转 `/storefront-pay`：`order-result.page.yaml`、`order-detail.page.yaml`、`order-list.page.yaml`（全部 tab + 待付款 tab，列表上下文用 `${item.id}`）
- 路由注册：`app-mall.action-auth.xml` storefront TOPM 新增 `storefront-pay`（orderNo 825，`/app/mall/pages/mall/pay/pay.page.yaml`）
- 实付金额用 `${data.actualPrice}`（实体字段，含积分/团购抵扣也准确），不复用历史页面 `goodsPrice+freightPrice-couponPrice` 的手工算法
- Owner doc：`docs/design/order-and-cart.md` 新增「前台支付消费者」小节并更新微信支付流程第 3 步（原「未实现 AMIS 二维码组件」已过时）；`docs/design/app-overview.md` 商城前台列表新增「订单支付页」
- 验证（全绿）：`./mvnw.cmd package -DskipTests -pl app-mall-app -am` BUILD SUCCESS；e2e `npx playwright test` **38 passed**（基线 37 + 新增 `mall/pay/pay` 页面渲染冒烟），`PageProvider__getPage` 返回 `type:page` 无错误
- Skill：`nop-frontend-dev`（Phase 1-3，已读 application-project-defaults/view-and-page-customization/delta-customization/prefer-delta-over-direct-modification 四篇必读）、`nop-testing`（Phase Final，已读 e2e-testing.md）
