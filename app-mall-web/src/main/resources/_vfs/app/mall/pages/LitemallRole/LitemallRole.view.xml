<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_LitemallRole.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
        <grid id="list"/>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view"/>
        <form id="edit">
            <layout>
                id[Id]
                name[角色名称]
                relatedUserList_ids
                desc[角色描述]
                enabled[是否启用]
            </layout>
        </form>

        <form id="add"/>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker"/>
    </pages>
</view>