<?xml version="1.0" encoding="UTF-8" ?>
<sql-lib x:schema="/nop/schema/orm/sql-lib.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <sqls>
        <eql name="reduceFlashSaleSessionStock" sqlMethod="execute">
            <arg name="sessionId"/>
            <arg name="num"/>

            <source>
                update LitemallFlashSaleSession
                set sessionStock = sessionStock - ${num}, updateTime = now()
                where id = ${sessionId} and sessionStock >= ${num}
            </source>
        </eql>
    </sqls>
</sql-lib>
