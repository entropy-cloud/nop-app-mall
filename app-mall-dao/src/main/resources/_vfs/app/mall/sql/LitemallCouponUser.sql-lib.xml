<?xml version="1.0" encoding="UTF-8" ?>
<sql-lib x:schema="/nop/schema/orm/sql-lib.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <sqls>
        <eql name="updateStatusIfUnused" sqlMethod="execute">
            <arg name="id"/>
            <arg name="orderId"/>

            <source>
                update LitemallCouponUser
                set status = 1, usedTime = now(), orderId = ${orderId}, updateTime = now()
                where id = ${id} and status = 0
            </source>
        </eql>
    </sqls>
</sql-lib>
