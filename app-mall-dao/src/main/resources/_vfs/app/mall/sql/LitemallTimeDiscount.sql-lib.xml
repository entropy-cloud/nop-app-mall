<?xml version="1.0" encoding="UTF-8" ?>
<sql-lib x:schema="/nop/schema/orm/sql-lib.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <sqls>
        <eql name="reduceTimeDiscountStock" sqlMethod="execute">
            <arg name="discountId"/>
            <arg name="num"/>

            <source>
                update LitemallTimeDiscount
                set stockLimit = stockLimit - ${num}, updateTime = now()
                where id = ${discountId} and stockLimit >= ${num}
            </source>
        </eql>
    </sqls>
</sql-lib>
